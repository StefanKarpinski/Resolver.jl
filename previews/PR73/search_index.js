var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Diagnostics","page":"API","text":"What a Diagnosis is made of. All of it is plain data, so a caller can render its own report without asking the resolver anything further.","title":"Diagnostics"},{"category":"section","location":"api/#Internals","page":"API","text":"","title":"Internals"},{"category":"function","location":"api/#Resolver.resolve","page":"API","text":"resolve(data, prob::Problem; by = identity, diagnose = true)\n    -> Union{Dict{P,V}, Diagnosis{P,V}, Nothing}\nresolve(data, reqs; compat = ..., pins = ..., by = identity, diagnose = true)\n\nResolve the requirements reqs against the package universe described by data, returning the optimal solution as a dict mapping each needed package to its chosen version, or a Diagnosis when the requirements are not jointly satisfiable. The solution covers every requirement and is exactly the dependency closure of the requirements under its own chosen versions.\n\nA Diagnosis says which requirements cannot hold together, why, and what the user could change to make them; showing it prints the report. Pass diagnose = false for the bare nothing instead, which is what a caller that only wants the verdict should do — the diagnosis costs several more solves and one resolve per fix it offers.\n\nA Problem additionally constrains the admissible versions with user compat bounds and pins; the answer is the one the same universe with those versions deleted would give. The requirements-and-keywords form builds one, so resolve(data, reqs; compat, pins) and resolve(data, Problem(reqs; compat, pins)) are the same call.\n\nThe returned solution is the layered solution: requirements are optimized first, in the priority order induced by by (each package maximized to its best feasible version given the choices already made), then the dependencies of the chosen versions, layer by layer along the dependency graph. See the manual's Theory section for the precise semantics and the guarantees it carries.\n\ndata may be a DepsProvider, a dict of PkgData, a dict of PkgInfo, or a SAT instance. The SAT method takes bare requirements and so has no constraints to attribute a failure to: it answers nothing, and accepts restore::Bool = true instead of diagnose — when true the instance's clauses are restored before returning so the instance can be reused, and restore = false is faster for single-use instances.\n\nThe other methods accept one more keyword:\n\norder: the version preference ordering, as a callable mapping a package to a lt comparator over its versions (\"is preferred to\"). The default, nothing, means the order the universe already lists them in — which for a T1 artifact (see pkg_info) is the canonical one the registry state fixes. The ordering is not a constraint: it selects among the valid solutions rather than changing which ones are valid, so it is a parameter here instead of part of the Problem, and one artifact serves every ordering.\nThe universe is built out of interchangeability classes — sets of versions nothing in the registry can tell apart (see pkg_info) — so order decides two things: which class the answer picks, and which member of that class it names, namely the best one the query admits.\n\n\n\n\n\n","title":"Resolver.resolve"},{"category":"function","location":"api/#Resolver.issatisfiable","page":"API","text":"issatisfiable(data, reqs; compat = ..., pins = ...) -> Bool\n\nCompute resolve(data, reqs; ..., diagnose = false) !== nothing more efficiently, with a single satisfiability check instead of a full optimizing resolve. Use this when you want to know whether the requirements can be satisfied but don't need an actual solution — or a report on why not.\n\ndata may be a DepsProvider, a dict of PkgData, a dict of PkgInfo, or a SAT instance, and a Problem may be passed in place of the requirements and keywords, as with resolve. The by and order keywords are not accepted since they affect which solution resolve picks, not whether one exists. The SAT method leaves the instance unchanged, so it can be reused.\n\n\n\n\n\n","title":"Resolver.issatisfiable"},{"category":"type","location":"api/#Resolver.Problem","page":"API","text":"Problem(reqs; compat = ..., pins = ..., excludes = ...)\n\nA resolution problem: the required packages reqs, plus the constraints that say which versions are admissible. A Problem carries everything that determines satisfiability; orderings (the by priority order, the order version rank order) are resolve parameters instead.\n\nThree kinds of constraint, in two shapes:\n\ncompat: per package, the set of allowed versions (queried with in).\npins: per package, the one version it is held at.\nexcludes: the exclusion kinds — \"no prereleases\" is the one the resolver's own tooling uses. An exclusion kind says something about versions rather than about particular packages, and so comes as a kind => predicate pair, where predicate(p, v) is true for the versions that kind forbids and kind is a symbol naming it.\n\nConstraints for packages that don't exist, and constraints that exclude nothing, are allowed and have no effect.\n\nEvery constraint here is a constraint on a shared package universe rather than a deletion from a private one: that is what lets a single T1 artifact (see pkg_info) serve queries that admit different things, and what lets diagnostics eventually name the kind that ruled a version out.\n\n\n\n\n\n","title":"Resolver.Problem"},{"category":"type","location":"api/#Resolver.Diagnostics.Diagnosis","page":"API","text":"Diagnosis{P,V}\n\nWhat resolve returns instead of a solution when the requirements cannot be satisfied: the independent Conflicts, each with the chain of facts that explains it, and a menu of verified Fixes, each of which repairs the whole problem.\n\nshow(io, MIME(\"text/plain\"), d) prints the report; the fields are plain data, so a caller that wants a different report can build one from them without asking the resolver anything further.\n\nPass diagnose = false to resolve for the bare nothing when only the verdict is wanted.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Diagnosis"},{"category":"module","location":"api/#Resolver.Diagnostics","page":"API","text":"Resolver.Diagnostics\n\nWhy a resolve failed and what would make it succeed. diagnose puts further satisfiability questions to the instance a failed resolve leaves behind and answers with a Diagnosis: the independent Conflicts — sets of requirements that cannot hold together, each with a chain of Facts about the packages that says why — and a menu of Fixes, each a set of Actions the user could carry out together with the solution that carrying them out yields.\n\nA Diagnosis is plain data, so a caller can render its own report from one without asking the resolver anything further; show(io, MIME(\"text/plain\"), d) and action_phrase are the report this module writes.\n\nThese names are this module's API. Resolver exports Diagnosis, since it is what resolve answers with when the requirements cannot be satisfied, and re-exports none of the others.\n\n\n\n\n\n","title":"Resolver.Diagnostics"},{"category":"type","location":"api/#Resolver.Diagnostics.Conflict","page":"API","text":"Resolver.Diagnostics.Conflict{P,V}(reqs, chain)\n\nOne independent reason the query fails: the requirements reqs, which cannot hold together, and chain, a short sequence of Facts about the packages that says why. Every fact in the chain is load-bearing — take any one of them away and the rest of the conflict dissolves.\n\nConflicts within a Diagnosis share no requirement, so each is a separate thing to fix.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Conflict"},{"category":"type","location":"api/#Resolver.Diagnostics.Fix","page":"API","text":"Resolver.Diagnostics.Fix{P,V}(actions, solution)\n\nOne repair of the whole problem: a set of Actions to carry out, and solution, what resolving with them carried out actually yields. The solution is a real resolve of the modified problem, so the versions in it are the versions the user would get.\n\nNo fix's actions are a superset of another's, and no two fixes ask for the same things.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Fix"},{"category":"type","location":"api/#Resolver.Diagnostics.Action","page":"API","text":"Resolver.Diagnostics.Action{P}(kind, pkg)\n\nOne thing the user could change about pkg:\n\n:drop — stop requiring it;\n:compat — relax the allowed-version set constraining it;\n:unpin — unpin it;\nanything else — admit the versions that exclusion kind forbids, for this package (:prerelease, say).\n\nEvery kind but :drop relaxes one of the Problem's constraint sources, and carrying out the action means taking that source off that package. Kinds are named for what the user does, which for a pin is to take it off.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Action"},{"category":"function","location":"api/#Resolver.Diagnostics.action_phrase","page":"API","text":"Resolver.Diagnostics.action_phrase(action) :: String\n\nAn Action as an imperative the user could carry out — \"drop requirement Foo\", \"relax your compat on Bar\".\n\n\n\n\n\n","title":"Resolver.Diagnostics.action_phrase"},{"category":"type","location":"api/#Resolver.Diagnostics.Fact","page":"API","text":"Resolver.Diagnostics.Fact\n\nOne statement in a conflict's chain: something true of the packages that helps explain why the requirements cannot hold together. The two kinds are Requirement — \"you require this package\" — and Availability — \"this is what your constraints leave of the package's versions\".\n\nFacts are plain data, so a caller can render its own report from a Diagnosis without asking the resolver anything further.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Fact"},{"category":"type","location":"api/#Resolver.Diagnostics.Requirement","page":"API","text":"Resolver.Diagnostics.Requirement{P}(pkg)\n\nThe fact that the query requires pkg.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Requirement"},{"category":"type","location":"api/#Resolver.Diagnostics.Availability","page":"API","text":"Resolver.Diagnostics.Availability{P,V}(pkg, members, excluded)\n\nA package's whole offering, and what the query's own constraints make of it: the versions of pkg the resolve could have chosen, each paired with the constraints that rule that version out. A conflict's chain carries one of these for every package the query took versions away from, so that a reader can see why the package could not supply what the conflict needed.\n\nmembers — every version of pkg the resolve had to choose between, in the order the package lists them (best first). Empty when the package has no installable version at all, which is a fact about the registry rather than about this query.\nexcluded — per member, the constraint sources that forbid it: :compat for an allowed-version set the version is not in, :pin for a pin at another version, and its own symbol for each exclusion kind. A member with no source is one this query admits.\n\nThe whole of the offering is here, so what the fact leaves the resolver is what it does not exclude: the package is unavailable exactly when every member has a source, and relaxing the sources of any one member is enough to give that version back.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Availability"},{"category":"function","location":"api/#Resolver.Diagnostics.unavailable","page":"API","text":"Resolver.Diagnostics.unavailable(fact::Availability) :: Bool\n\nWhether the fact leaves its package with no version at all — every version it offers excluded, or none to offer in the first place.\n\n\n\n\n\n","title":"Resolver.Diagnostics.unavailable"},{"category":"function","location":"api/#Resolver.Diagnostics.diagnose","page":"API","text":"Resolver.Diagnostics.diagnose(sat, prob, info; by, order) :: Diagnosis\n\nWhy the resolve of prob against the instance sat failed, and what would make it succeed. info is the T1 artifact (see pkg_info) the instance's universe was prepared from; each fix is verified by resolving the modified problem against it, with the same by and order the failed resolve used, and the solution that comes back is the fix's witness.\n\nsat must be the instance exactly as the failed query posed it — the deactivation frame in place and no clause added since — and is left that way.\n\n\n\n\n\n","title":"Resolver.Diagnostics.diagnose"},{"category":"type","location":"api/#Resolver.PkgInfo","page":"API","text":"PkgInfo{P,V}\n\nOne package's slice of the universe. The conflict matrix has one row per interchangeability class — a set of versions nothing in the registry can tell apart — and the partition saying which versions those are is carried with it.\n\nversions — every version the package offers, in canonical (provider) order. Untouched by the ordering a query asks for: the rank order is a property of the classes, and it is the rows that move.\nclasses / members — the partition, in both directions. classes[i] is the matrix row version i belongs to; members[c] is class c's version indices, ascending. Both are stored because both directions are hot; anything that drops or renumbers classes rebuilds them together.\ndepends — the packages some class depends on, sorted; one matrix column each, in that order.\ninteracts — per partner package, the offset of its block of class columns in this matrix.\nconflicts — (m+1) × (n+1) bits for m classes and n conflict columns: the dependency columns followed by one block of partner classes per partner, with the last row and column holding the in-universe flags (see BitKernels.jl for the layout).\n\nVersions share a class when nothing in the registry can tell them apart — see version_classes. Since class members are indistinguishable by construction, a user constraint cannot split a class; all it can do is empty one, which is why constraints never enter these matrices (see prepare_pkg_info).\n\nThe two per-class bits\n\nA class carries two independent facts, and the matrix holds only one of them:\n\nin-universe — \"this class is still part of the problem\". This is the flag row and column of conflicts. The passes that delete clear it (mark_installable!, mark_reachable!, mark_necessary!), and drop_unmarked! reads it and nothing else.\nactivated — \"this query admits some member of this class\". This is per-resolve state, not matrix state: it is reps[c] != 0 on the Universe built from this artifact.\n\nDeactivation never implies deletion: an emptied class keeps its row, its column in every partner, and its dependency columns. Keeping the two bits apart is what makes that possible, and BitKernels.jl's header spells out why one bit could not do both jobs.\n\n\n\n\n\n","title":"Resolver.PkgInfo"},{"category":"function","location":"api/#Resolver.pkg_info","page":"API","text":"pkg_info(data, reqs = all packages; filter = true) :: Dict{P,PkgInfo{P,V}}\n\nThe T1 artifact: the dependency closure of reqs, encoded as one PkgInfo per package — one conflict matrix per package, indexed by its interchangeability classes (version_classes, collapse_classes!) — plus the one piece of preprocessing that depends on the registry alone: dropping the versions that cannot be installed whatever else is chosen, because they depend on a package with no installable version at all, applied repeatedly until nothing more drops (mark_installable!; the literature calls this arc consistency). Nothing here depends on the version ordering, on user compat or pins, or (beyond the closure) on the requirements, so the result can be computed once and shared by, or cached across, arbitrarily many resolves.\n\nfilter = false skips that prune, leaving the conflict matrices saying exactly what the data spells out.\n\nRequirement-specific and constraint-specific shrinking — reachability and redundancy elimination — happens per resolve instead, on the universe the query's representatives make of this one: see prepare_pkg_info.\n\n\n\n\n\n","title":"Resolver.pkg_info"},{"category":"function","location":"api/#Resolver.version_classes","page":"API","text":"version_classes(X, m) :: Vector{Int}\n\nThe interchangeability partition of the m versions whose conflicts matrix is X, as a class id per version — ids numbered 1, 2, … in the version order of each class's first member.\n\nPer the manual's Theory section (lemma on interchangeable versions), two versions of a package are interchangeable when they have the same dependency set and every compatibility entry in the problem — their own and every other package's — constrains them equally. In PkgInfo terms that is exactly row equality, and the test is purely local to X: the dependency columns carry the dependency set, and the interaction blocks carry every compatibility entry that mentions the package, incoming as much as outgoing, because pkg_info records each conflict symmetrically in both partners' matrices. (The incoming half is not optional — another package's bound can admit one of two versions and not the other, and then they are genuinely distinguishable.)\n\nThe partition is a property of the registry alone: it is independent of the requirements, of the version ordering, and of any user constraints. That is what puts it on the T1 side of the boundary, and what lets a PkgInfo's matrix be indexed by it: pkg_info computes the partition from rows it builds one per version, then emits the matrix with one row per class (collapse_classes!). Choosing which member of a class stands for it is the part that does depend on the query, and that is per-resolve state (see class_ranking).\n\nThe scan is word-parallel over columns: one pass computes, for every adjacent pair of versions at once, whether their rows differ anywhere, which yields the classes that are contiguous runs of versions — nearly all of them in practice, since dependency sets and compat bounds vary in blocks. A second pass hashes one row per run and merges runs whose rows compare equal, so the result is the exact row-equality partition, hence invariant under reordering versions.\n\n\n\n\n\n","title":"Resolver.version_classes"},{"category":"function","location":"api/#Resolver.class_members","page":"API","text":"class_members(classes, m = maximum(classes)) :: Vector{Vector{Int}}\n\nInvert a class-id-per-version vector into a version-index-per-class one. m is the number of classes, needed only when the last ones might be empty — which they never are for a partition, but the argument keeps the inverse total.\n\n\n\n\n\n","title":"Resolver.class_members"},{"category":"function","location":"api/#Resolver.collapse_classes!","page":"API","text":"collapse_classes!(info)\n\nThe last step of building an artifact. pkg_info starts with a row per version, because that is what the partition is computed from; this computes it (version_classes) and rebuilds every matrix with one row per class of the package and one column per class of each partner, which is the shape everything downstream reads.\n\nThe collapse is lossless in both directions. A class's members have identical rows by definition, so the class's row is any member's row. And members of a partner class have identical columns here: a conflict is recorded symmetrically in both partners' matrices, so partner rows that agree everywhere agree in particular about this package, and the columns they index are equal. Deleting duplicate columns cannot separate rows that agreed, so the partition this indexes is also exactly the partition it was computed from — collapsing does not have to be iterated to a fixpoint to be correct, only to be coarsest, and coarser is not what is claimed here.\n\nCalled once, at the end of pkg_info.\n\n\n\n\n\n","title":"Resolver.collapse_classes!"},{"category":"type","location":"api/#Resolver.Universe","page":"API","text":"Universe{P,V}\n\nThe universe a single resolve is run against: one PkgInfo per package, plus reps — per package, per class, the index into that package's versions of the member the class stands for under this query, or 0 when the query admits none of them.\n\ninfo carries the package universe (pkg_info) and reps carries everything the query determines about it. A user constraint acts by forbidding members of classes. Class members are indistinguishable to everything in the registry, so forbidding some of them only changes which one stands for the class; forbidding all of them leaves the class empty, hence deactivated — it cannot be selected.\n\nDeactivated classes are not deleted. They stay in the matrices as the columns a later relaxation of whatever emptied them would need, and the passes read the deactivation the way they read a conflict with an always-present version: reachability continues its prefix past a deactivated class while still following its dependencies, redundancy elimination neither deletes one nor lets one license a deletion, and the SAT instance forbids each of them with one relaxable unit clause. BitKernels.jl's header spells out how a class's in-universe flag in the matrix and its activated state here divide the work.\n\nWhy deactivation is read off reps\n\nreps has to exist regardless: a class's representative is what names the version in the answer, and it is what the class competes at when the classes are ranked. A class the query admits no member of simply has no representative, so \"unavailable for this query\" costs nothing extra to represent. A second flag bit in the matrix would work equally well; it would just encode a fact reps already carries.\n\nWhy the query's state is separate from the artifact\n\nThe invariant worth protecting is that the artifact info is query-independent: it is a function of the registry alone, PkgInfoFiles writes and reads it, and one of them answers every query, whatever that query constrains or prefers. reps is the opposite — one query's worth of state, recomputed per resolve.\n\nKeeping the two in separate values is what makes that invariant checkable rather than merely intended. Were reps a field of PkgInfo, the persisted type would contain query state: serialization would have to know to skip it, and a freshly loaded artifact would carry a field whose only honest value is \"not computed yet\" — with 0, which means deactivated, as its natural uninitialized value, so forgetting to fill it in yields a universe where nothing can be selected rather than an error. Whatever shape this ends up taking, that is the property it has to preserve: nothing a query decides may be reachable from the thing that gets cached and shared.\n\n\n\n\n\n","title":"Resolver.Universe"},{"category":"function","location":"api/#Resolver.prepare_pkg_info","page":"API","text":"prepare_pkg_info(info, prob, [info′]; order = nothing) :: Universe\n\nThe per-resolve half of preprocessing, run on a T1 artifact (see pkg_info) to produce the universe the SAT instance is built over. Two steps:\n\nRank each package's classes for this query (class_ranking): pick the member each class stands for and lay the classes out in the order those members put them in. Under the canonical ordering and no constraints this is the layout the artifact already has, and costs nothing.\nFilter the result for reachability and redundancy against the actual requirements and constraints (filter_pkg_info!).\n\nFiltering has to come second: both of its passes are stated in terms of the rank order, so the layout has to exist first.\n\nThe user's constraints reach the matrices through nothing but step 1's representatives. A constrained query and an unconstrained one produce the same matrices, up to the class reordering their representatives induce; the whole of what a constraint does is empty classes, and an empty class is deactivated rather than deleted, split or rewritten. That is what makes a class's deactivation the only thing there is to relax.\n\nThe result is a fresh universe — info is left untouched, so a T1 artifact stays reusable across resolves — unless the caller passes its own scratch dict as info′, or info itself when it owns it (which reordering rules out, since it rebuilds the matrices).\n\n\n\n\n\n","title":"Resolver.prepare_pkg_info"},{"category":"function","location":"api/#Resolver.version_permutations","page":"API","text":"version_permutations(info, order) :: Union{Nothing, Dict{P, Vector{Int}}}\n\nPer package, the permutation that puts its versions into the ordering order asks for: perm[r] is the index, in the T1 version list, of the r-th best version. Only the packages that actually need reordering get an entry, and the whole result is nothing when none of them do — so the canonical order, which is the one the T1 artifact carries, costs nothing.\n\norder is nothing for the canonical order, or a callable mapping a package to a lt comparator over its versions (lt(u, v) = \"u is preferred to v\"). The ordering is the one query knob that is not a constraint: it does not change which solutions are valid, only which valid solution is optimal. That is why it is a resolve parameter rather than part of the Problem, and why the T1 artifact can be built and cached without knowing it (see the manual's Theory section, on order-free deletion and on interchangeability classes being ordering-independent while the representative choice is not).\n\nThis is the ordering of versions; what the universe is laid out in is the ordering of classes it induces, which is class_ranking's business.\n\n\n\n\n\n","title":"Resolver.version_permutations"},{"category":"function","location":"api/#Resolver.class_ranking","page":"API","text":"class_ranking(info, prob, perms) :: (reps, perms′)\n\nWhat a query makes of an artifact's classes, before anything is materialized: per package, which member each class stands for (reps, a version index per class, 0 for a class with no admissible member) and how the classes have to be reordered to be in rank order (perms′, an entry per package that needs it, nothing when none does).\n\nRepresentative. A class's members are indistinguishable to the registry, so the only thing a user constraint can do to a class is forbid some of its members. What is left, the class admits; the best of those in the active version ordering — perms, see version_permutations — represents it. A class admitting nothing is empty, hence deactivated, and has no representative.\n\nRank. A class ranks where the member it stands for ranks, because that is the version choosing it yields: a class whose best member is forbidden competes at its best admissible member, and can therefore fall behind classes it outranks in the artifact. An empty class ranks where its best member would, the position it would compete from if the constraint that emptied it were lifted. Every class's key is a distinct version rank, so this is a total order.\n\nBoth outputs are per-resolve state: the partition they index is the registry's, but which member stands for a class, and hence how the classes are ordered, belongs to the query.\n\n\n\n\n\n","title":"Resolver.class_ranking"},{"category":"function","location":"api/#Resolver.find_reachable","page":"API","text":"find_reachable(info, reqs) :: Dict{P, Int}\n\nThis function finds a minimal \"reachable\" subset of packages and classes that could appear in pareto-optimal solutions to version resolution for the given set of required \"root\" packages, using the following recursive logic:\n\nP in reqs => P[1] reachable\nP[i] reachable & P[i] depends on D => D[1] reachable\nP[i] reachable & P[i] conflicts w. reachable => P[i+1] reachable\nD[end] conflicts w. reachable & P[i] depends on D => P[i+1] reachable\n\nThe optional deact argument gives, per package, a mask of its deactivated classes — the ones this query admits no member of. Such a class cannot be selected, so the package cannot be installed at it and the prefix has to continue past it, exactly as it continues past a class conflicting with something present:\n\nP[i] reachable & P[i] deactivated => P[i+1] reachable\n\nA deactivated class's dependencies are followed all the same, which is what keeps the cone behind it in the universe: it is precisely the cone a relaxation of whatever emptied the class would need to find still there.\n\nThe function returns a dictionary mapping packages to the maximum class index of that package that could be reached in an optimal solution. If a package cannot appear in an optimal solution, it will not appear in this dictionary.\n\n\n\n\n\n","title":"Resolver.find_reachable"},{"category":"function","location":"api/#Resolver.exclusion_masks","page":"API","text":"exclusion_masks(info, prob) :: AbstractDict{P, BitVector}\n\nThe versions of each package that prob's constraints forbid, as a mask over that package's version list in info. Only packages with a constraint that actually excludes something get an entry, so packages nothing constrains — the overwhelming majority, absent an exclusion kind — cost nothing downstream.\n\nThis is the only place a constraint is ever evaluated, and what reads it is class_ranking, which turns it into representatives. Masks are index-based, so they are read against the version list they were built from.\n\n\n\n\n\n","title":"Resolver.exclusion_masks"},{"category":"section","location":"theory/diagnostics/#Diagnosing-an-unsatisfiable-resolve","page":"Diagnosing an unsatisfiable resolve","text":"When the requirements cannot be satisfied there is more to say than that they cannot: which of them cannot hold together, why, and what a user could change so that they can. Every one of those answers comes from putting further satisfiability questions to the instance the resolve failed on, and this page states what licenses them.\n\nEverything here is short, and class space (see the previous page) is why: a user constraint's whole effect on the universe is the set of classes it empties, so there is exactly one kind of thing to relax and it is already a unit fact of the instance.","title":"Diagnosing an unsatisfiable resolve"},{"category":"section","location":"theory/diagnostics/#Relaxations","page":"Diagnosing an unsatisfiable resolve","text":"A query is a set of requirements R together with constraints, and what the constraints do to the universe is empty a set E of classes — a class the query admits no member of cannot be selected, and that is the whole of it.\n\nA relaxation of the query is a pair (R E) with R subseteq R and E subseteq E: it requires only R and leaves empty only E, so the requirements outside R are dropped and the classes in E setminus E are given back. The query itself is (R E), and a solution of the relaxation is one covering R and using no class in E.\n\nnote: Observation (relaxations are assumption subsets)\nIn the instance built for the query, \"p is installed\" is the unit assumption p and \"class c of p is empty\" is the unit assumption lnot pc. Every other clause — dependency edges, registry conflicts, the structural at-most-one — is the registry's and is the same for every relaxation. So the relaxation (R E) is satisfiable exactly when the instance is satisfiable assuming p  p in R cup lnot pc  pc in E, and asking is one solve.\n\nA report is written a package at a time — what has become of a package's versions is one thing to say, and which of its constraints to relax is one thing to do — so the questions are asked a package at a time too, over one variable per package implying all of that package's assumptions. Defining such a variable is a conservative extension: it occurs positively only in what is assumed, so every model of the instance extends to one of the instance with the definition, and the questions stay inside the class this page covers.","title":"Relaxations"},{"category":"section","location":"theory/diagnostics/#The-filtered-universe-answers-for-every-relaxation","page":"Diagnosing an unsatisfiable resolve","text":"The instance a resolve fails on is built over a universe that has been filtered — reachability and redundancy elimination, run with the query's constraints in force (filter_pkg_info!). The questions above are asked of that instance, so what has to hold is that filtering for the query did not delete anything a relaxation of the query would need.\n\nnote: Proposition (filtering answers for every relaxation)\nLet F be the universe obtained by filtering the raw universe for the query (R E). For every relaxation (R E), F has a solution of that relaxation iff the raw universe does.Proof. One direction is immediate: F's classes are a subset of the raw universe's, so its solutions are raw solutions.For the other, take a raw solution S of the relaxation. Reachability continues a package's prefix past a class the query emptied, since such a class is not somewhere the package can be installed. So the last kept class of an unsaturated package is not in E — had it been, the prefix would have continued past it — and is therefore admitted by the query, hence by the relaxation. Theorem 3's construction now applies verbatim: take last kept classes for unsaturated packages and S's own classes for saturated ones (which keep all of theirs), and Theorem 2's invariants make the splice conflict-free and dependency-closed. It covers R, and it uses no class in E: the unsaturated half uses none in E at all, and the saturated half takes its classes from S.Redundancy elimination is handled by substitution, as in Theorem 6. It deletes a class only when a better class has a subset of its registry constraints — its dependency and conflict columns — and that comparison never mentions the query's constraints, so it holds for the relaxation unchanged. The dominating class is a candidate of the pass, and candidates exclude the classes the query emptied, so the relaxation admits it. Domination chains terminate at a class that survives, because reachability keeps prefixes. ∎\n\nBoth halves turn on the same design decision: a class the query empties is deactivated, not deleted. It keeps its row, its column in every partner, and its dependency columns, which is exactly what a question about giving it back needs to find still there.","title":"The filtered universe answers for every relaxation"},{"category":"section","location":"theory/diagnostics/#Versions-come-from-a-resolve,-not-from-the-instance","page":"Diagnosing an unsatisfiable resolve","text":"The proposition is about satisfiability, and satisfiability is all the questions above ask. A report also says what the user would get, and that is a different quantity: the layered answer, which depends on the version ordering.\n\nThe ordering a universe is laid out in belongs to the query. A class ranks where its best admitted member ranks (class_ranking), and a relaxation admits more members — so under a relaxation a class can compete at a better version than it competes at here, and both filtering passes, which are stated in terms of the rank order, would be reasoning about a differently ordered universe. A version read off the failed instance is therefore a version of the query's universe, not of the relaxation's.\n\nSo no version in a report is ever read off the failed instance. A fix is carried out on the Problem — the requirements it names dropped, the constraint sources it names taken off the packages they name — and the modified problem is resolved against the T1 artifact (pkg_info), where the relaxation's own representatives and its own class layout are computed from scratch. The solution that comes back is what the report shows, and it is the solution the user will get, because it is the same call the user's next resolve makes.\n\nThat a fix resolves at all is the proposition above plus monotonicity: relaxing a constraint source admits at least the version the repair asked for, and admitting more versions only adds models.","title":"Versions come from a resolve, not from the instance"},{"category":"section","location":"theory/diagnostics/#Attribution-needs-no-license","page":"Diagnosing an unsatisfiable resolve","text":"Why a class is empty is not a question about the instance. A constraint forbids versions, a class is empty when every member of it is forbidden, and which sources forbid which version is read straight off the Problem (exclusion_sources, class_exclusions). No solver is involved, so nothing has to be preserved by anything:\n\nmathrmrepspc = 0\nquadLongleftrightarrowquad\ntextevery member of class  c text of  p text has an excluding source\n\nThis is the one place a report can name a user's constraint at all — registry compatibility is in the conflict matrices, where it is indistinguishable from any other conflict — and it is why a story can say which bound emptied a class without asking anything.","title":"Attribution needs no license"},{"category":"section","location":"theory/setting/#Setting","page":"Setting","text":"This page fixes the objects and notation used throughout the theory section. Everything downstream — the semantics the resolver implements, the correctness proofs for its filtering, and the Pareto-front analysis — is stated in these terms.","title":"Setting"},{"category":"section","location":"theory/setting/#The-resolution-problem","page":"Setting","text":"A resolution problem consists of:\n\na finite set of packages mathcalP;\nfor each package p a finite, totally ordered list of versions, written by rank: rank 1 is the best version, rank 2 the next, and so on (the meaning of \"best\" is supplied by the caller — newest, oldest, closest-to-installed — the theory only uses the order);\nfor each version pv a dependency set mathrmdeps(p v) subseteq mathcalP — note that dependencies are attached to versions, and different versions of the same package may have different dependency sets, a fact that turns out to carry a lot of weight;\na symmetric conflict relation on version pairs (in the package data this arises from compatibility constraints: pv conflicts with qw when w is outside pv's compatibility bound for q, or vice versa);\na set of requirements mathrmreqs subseteq mathcalP.\n\nA solution is a partial assignment s of versions to a support set mathrmsupp(s) subseteq mathcalP such that:\n\ncoverage: mathrmreqs subseteq mathrmsupp(s);\ndependency closure: for every p in mathrmsupp(s), mathrmdeps(p s(p)) subseteq mathrmsupp(s);\nconflict freedom: no two assigned versions conflict.\n\nWrite mathrmrank_s(p) for the rank of s(p).","title":"The resolution problem"},{"category":"section","location":"theory/setting/#Tightness-and-pruning","page":"Setting","text":"The closure C(s) of a solution is the least set containing mathrmreqs and closed under following dependencies of s's chosen versions:\n\nC(s) = bigcup_k L_k qquad\nL_0 = mathrmreqs quad\nL_k+1 = L_k cup bigcup_q in L_k mathrmdeps(q s(q))\n\nA solution is tight if mathrmsupp(s) = C(s) — it contains nothing that isn't reachable from the requirements along its own chosen dependency edges. R^* denotes the set of tight solutions. Pruning pi(s) restricts s to C(s).\n\nnote: Lemma (pruning)\npi(s) is a tight solution with s's versions, and C(pi(s)) = C(s).Proof. Coverage and dependency closure hold because the closure construction adds exactly the dependencies of its members; conflict freedom is inherited from s; and the closure computation only ever inspects versions of packages already in the closure, which agree with s. ∎\n\nTwo structural facts about tight solutions recur constantly:\n\nSupport is a function of the versions. For a tight solution, the support is determined by the version assignment (it is the closure), so two distinct tight solutions with nested supports must already differ on a version within the smaller support.\nJunk. A valid solution need not be tight — it may carry packages (\"junk\") not justified from the requirements. Junk matters because the resolver works with a SAT solver, and SAT models are exactly the valid solutions, junk included.","title":"Tightness and pruning"},{"category":"section","location":"theory/setting/#The-SAT-encoding","page":"Setting","text":"The resolver encodes a problem instance with one boolean variable per package and one per version:\n\npackage implies some version: p Rightarrow bigvee_i pi;\nversion implies its package: pi Rightarrow p;\nversions are mutually exclusive: lnot pi lor lnot pj;\ndependencies: pi Rightarrow q for each q in mathrmdeps(pi);\nconflicts: lnot pi lor lnot qj for conflicting pairs.\n\nWith requirement unit clauses added, the models of this formula are precisely the valid solutions covering the requirements (with junk allowed). All constraints are dependency edges (positive, from versions to packages) and pairwise version conflicts (negative) — a structural fact several proofs exploit: any infeasibility ultimately traces to pairwise conflicts, because dependencies alone can always be satisfied by including more packages.\n\nBefore solving, the problem is shrunk by filtering — reachability analysis and redundancy elimination — whose correctness is the subject of the next page.","title":"The SAT encoding"},{"category":"section","location":"#Resolver.jl","page":"Home","text":"A package version resolver built on a real SAT solver (PicoSAT). Given package data — versions, dependencies, and compatibility constraints — and a set of required packages, resolve returns a single optimal solution mapping each needed package to a version, or a Diagnosis — which requirements cannot hold together, why, and a menu of verified fixes — when they cannot be satisfied.\n\nWhat \"optimal\" means, precisely, and why the resolver's aggressive problem filtering provably does not change the answer, is worked out in the Theory section:\n\nSetting — the resolution problem, solutions, tightness, and the SAT encoding.\nThe layered solution & filtering — the semantics the resolver implements, and proofs that filtering preserves both satisfiability and the answer.\nPareto front optimality — an alternative, purely declarative notion of optimality that was fully worked out and ultimately set aside; its theorems and counterexamples illuminate why the layered semantics and its filter fit together as well as they do.\nDiagnosing an unsatisfiable resolve — what licenses the questions a Diagnosis is assembled from, and why the versions it shows come from a resolve rather than from the instance.\n\nFor the package's motivation and project status, see the README.","title":"Resolver.jl"},{"category":"section","location":"theory/front/#Pareto-front-optimality","page":"Pareto front optimality","text":"This page documents a fully worked-out alternative semantics for the resolver — necessity on the Pareto front — including its theorems, its pure-SAT implementation, and the reasons it was ultimately set aside. It is a digression from what the package implements, but a productive one: its impossibility results explain why the layered semantics and the filter fit together, its proof techniques power the theorems on the previous page, and it closes the question of a purely declarative optimality criterion rather than leaving it open. A reference implementation lives on the front-necessity branch.","title":"Pareto front optimality"},{"category":"section","location":"theory/front/#Motivation","page":"Pareto front optimality","text":"The layered solution is defined by a procedure. One can ask for something more declarative: a criterion picking out the optimal solution using only the solution set — no layers, no reference to the dependency walk, ideally nothing but satisfiability queries. The natural candidate is a necessity descent: repeatedly pin the highest-priority package that is necessary — present in every remaining solution — at the best version available. Necessity, however, is a universal property of a solution set, so its meaning depends on which set is quantified over:\n\nAll tight solutions (raw necessity): too weak — a package needed by every sensible solution counts as avoidable if one strictly-dominated solution technically avoids it, e.g. by taking a requirement's worst version merely because that version happens to drop a dependency edge. This distorts the descent, and it also forbids problem reduction entirely, which is ruinous at registry scale.\nModels of the filtered problem: fast, but then the answer depends on filtering details — sharpening or dulling the filter changes the result, which is unacceptable as a semantics.\n\nThe canonical middle ground is necessity over the Pareto front: quantify over the solutions that are not strictly worse than others.","title":"Motivation"},{"category":"section","location":"theory/front/#The-dominance-order","page":"Pareto front optimality","text":"For tight solutions s t:\n\nt trianglerighteq s quadiffquad\nmathrmsupp(t) supseteq mathrmsupp(s) quad\nmathrmrank_t(p) le mathrmrank_s(p)  forall p in mathrmsupp(s)\n\nwith strict improvement at some p in mathrmsupp(s) (coverage- monotone version dominance). This is a strict partial order on the finite set R^* of tight solutions, so every solution is either maximal or strictly dominated by a maximal one. The front F is the set of maximal elements.\n\nBoth design choices in trianglerighteq are forced:\n\nOrientation. An order in which leaner solutions dominate cannot work: the solutions that falsely break necessity are support-minimal (a requirement alone at a bad version), and a support-minimal solution has no leaner competitor, so no leaner-dominates order can remove one. Also, since a tight solution's support is a function of its versions, \"leaner at equal quality\" never occurs — leanness always trades against version quality, and the order must price the trade. trianglerighteq prices it as: a bigger solution wins only by covering everything at least as well and strictly better somewhere.\nJunk is excluded by the space, not the order. With the supseteq orientation, unjustified packages would ride along for free, so the candidate space must be the tight solutions. Tightness (well-founded justification, the same notion as stable models in answer-set programming) is not expressible by any dominance relation over versions — it is not monotone in the version lattice. Empirically, defining the front over junk-tolerant solutions changes answers on 92 of 7,464 small instances.\n\nThe front-necessity semantics is the forced descent over F: repeatedly pin the highest-priority package present in every front member consistent with the pins so far, at the best version among them, until no unpinned package is common.","title":"The dominance order"},{"category":"section","location":"theory/front/#Theorem-A:-pin-exactness","page":"Pareto front optimality","text":"The linchpin of everything that follows.\n\nnote: Theorem A\nLet pins be the pin state after any number of descent rounds. If f in F contains every pinned package at rank ≤ its pinned rank, then f agrees exactly with the pins.Proof. Suppose not; among pinned packages where f is strictly better, let hat q be the one pinned earliest, at rank hat r. On packages pinned before hat q, f is ≤ with no strict improvement, hence equal — so f was in the remaining front set when hat q was pinned. But then hat r = minmathrmrank_g(hat q) le mathrmrank_f(hat q)  hat r. ∎\n\nnote: Corollary A1\nIf t in F and t trianglerighteq s (or t = s) for any s agreeing with the pins, then t agrees exactly with the pins: dominating a pin-consistent solution can never buy pin-violating quality.","title":"Theorem A: pin exactness"},{"category":"section","location":"theory/front/#Theorem-B:-the-answer-is-a-front-member","page":"Pareto front optimality","text":"note: Theorem B\nThe descent terminates with the remaining front set equal to exactly mathrmpins; in particular the answer is itself a valid, tight, undominated solution.Proof. Requirements are in every front member, so they get pinned. If q is pinned, every remaining f has q at the pinned version, so mathrmdeps(q mathrmpins(q)) is common and gets pinned — the pins are dependency-closed. Now take any final survivor f and walk its closure: it starts at requirements (pinned), and whenever a closure node u is pinned, f(u) = mathrmpins(u), so the next closure nodes are pinned too. Hence C(f) subseteq mathrmdom(mathrmpins), and by tightness mathrmsupp(f) = C(f) subseteq mathrmdom(mathrmpins) subseteq mathrmsupp(f) — so f = mathrmpins. ∎\n\nThe same argument shape, one level down, shows the layered answer is trianglerighteq-undominated: a dominator would have to agree with every layered pin before its earliest strict improvement, contradicting that pin's optimality. So the layered solution always lies on the front; front-necessity differs only in which front point it selects (230 of 7,464 small instances) — front-necessity makes priority the only ordering principle among necessary packages, where the layered order lets requirements outrank higher-priority dependencies.","title":"Theorem B: the answer is a front member"},{"category":"section","location":"theory/front/#Theorem-C:-a-pure-SAT-implementation-exists","page":"Pareto front optimality","text":"Front-necessity is implementable against a SAT instance that encodes validity only — no tightness encoding, which matters because eager tightness encodings blow up (rooted-chain/level constructions), the naive support encoding admits unrooted cycles, and lazily this is exactly answer-set programming's unfounded-set problem. Instead, two cheap operations run outside the solver: pruning pi (a closure computation) and support comparison. The key sub-results:\n\nWitness transfer. A model lacking p prunes to a tight solution lacking p; conversely every tight solution is a model. So junk never distorts the existential side of a necessity query.\nDominator transfer. A tight solution s has a tight strict dominator iff some model d covers mathrmsupp(s) at ranks ≤ with a strict improvement and passes the prune-check pi(d) supseteq mathrmsupp(s) — junk can fake domination whose tight core doesn't actually cover s, and the prune-check filters exactly those fakes.\nChain-top admissibility. Climbing dominators terminates at a front member, which by Corollary A1 agrees exactly with the pins even though intermediate steps may not.\nCone blocking. When a chain-top t contains p, everything trianglelefteq t is either dominated or is t itself, so one clause excludes the entire cone without losing any witness.\nGreedy construction. In the implementation, candidates are constructed rather than searched for: the layered greedy runs under per-query assumptions, and a junk-free greedy answer is provably Pareto-maximal within its query space (the Theorem A argument again). Restricting all queries to supported models — one clause per package: present implies depended-upon — loses no tight solution and reduces junk to dependency cycles.\n\nWith these, the descent's two oracle types (is p in every pin-exact front member? what is the best rank among them?) are decided exactly, by sequences of ordinary SAT solves. Correctness of the composed pipeline was additionally verified against a brute-force implementation of the definition on roughly 19,500 instance/order pairs with zero deviations.","title":"Theorem C: a pure-SAT implementation exists"},{"category":"section","location":"theory/front/#Theorem-D:-no-version-level-reduction-is-faithful","page":"Pareto front optimality","text":"Here is the decisive negative result. One would like to run the necessity descent on a reduced problem — that is how the layered semantics gets its speed. For front-necessity this fails not just for the actual filter but in principle:\n\nnote: Theorem D\nThere is a problem on which the forced descent over models of the exact front-support reduction — keep a version iff some front member uses it, the strongest version-level reduction that could possibly be faithful — differs from the forced descent over the front itself.Proof. Requirements a, priority p  a  d  b, version 1 best:package dependencies conflicts\na a1 to d, a2 to b \nb b1 to p \nd d1 to p, d2 to  d1 otimes p1\np p1 to d, p2 to  R^* has four members and the front is F = f_1 f_2 with f_1 = a2 b1 p1 d2 and f_2 = a1 d1 p2 — both contain p, so the front descent pins p first at rank 1 and lands on f_1. But every version above appears in some front member, so the exact front-support reduction keeps everything — including the recombinant c = a1 d2 (f_2's a with f_1's d), a tight solution missing p. In the reduced problem p is no longer common; the descent pins a first, and rank-minimization at a eliminates f_1: the reduced answer is f_2 ne f_1. (Verified computationally.) ∎\n\nThe moral: necessity is a property of the solution set, and a version universe only bounds that set from above — recombinations of individually front-supported versions can erase necessity. Universal queries survive only when asked against the front itself. Note the contrast with Theorem 4 of the layered page: the layered semantics needs a reduction to preserve one solution; front-necessity would need it to preserve the entire front and nothing that changes commonality — and even the latter is unattainable at the version level. Such counterexamples require engineered structure (two dependency routes, a recombination orphaning the necessity package, a conflict pinching the diagonal): none arose in 12,000 random 4-package instances, which is also why smaller empirical sweeps had suggested the reduction was safe.","title":"Theorem D: no version-level reduction is faithful"},{"category":"section","location":"theory/front/#Why-the-filter-cannot-serve-the-front","page":"Pareto front optimality","text":"The actual reachability filter fails front-preservation in an even more basic way: it drops front members outright. On the Theorem D instance it keeps only a1 d1 d2 p1 p2 — never reaching a2, deleting b — yet f_1 uses both. The filter's designed invariant is \"a worse version is only chosen under conflict pressure against the better version,\" and a1 is never conflicted. But f_1 takes a2 for a different kind of reason: under a1, the only dependency edge that can justify p is d1 to p, and p1 otimes d1 — so on the a1 route the better p1 is unreachable-as-justified. Switching to a2 reroutes justification through b and frees p1. The downgrade at a is paid for by an upgrade at p — an opportunity-cost trade, mediated not by any conflict against a but by version-varying dependency sets rewiring the justification topology.\n\nThat is precisely the boundary of the filter's intuition:\n\nnote: Fragment lemma\nIf every version of each package has the same dependency set, then on the front every non-best version is blocked by a conflict with a co-installed package — the filter's invariant, as a theorem.Proof (one step). Let s in F with q at a non-best version and v better. The in-place upgrade s = sq mapsto v has the same support, and — dependency sets being version-independent — the same justification edges, hence remains tight. If s were conflict-free it would strictly dominate s. So some conflict between qv and a member of s blocks it. ∎\n\nPointwise (layered) optimality never makes opportunity-cost trades, which is why conflict-driven degradation covers everything it can ever choose (Theorem 5 of the layered page); the front additionally contains the trade-mediated points, and front-necessity quantifies over exactly those.","title":"Why the filter cannot serve the front"},{"category":"section","location":"theory/front/#Why-it-was-set-aside","page":"Pareto front optimality","text":" layered front-necessity\ncanonical, filter-free definition yes (Theorems 4–6) yes (by construction)\ndeclarative (procedure-free) statement no — defined by the trace yes\nSAT query character existential, answer-witnessed universal (necessity, dominance)\nadmissible reductions any preserving one solution none at the version level (Theorem D)\nregistry performance milliseconds–seconds small: ms; DataFrames: ~85 ms; DifferentialEquations (26k versions, unreduced): intractable\n\nThe costs are intrinsic, not implementation slack: universal queries must run against the unreduced problem (Theorem D), every semantically-avoidable candidate needs a witness construction plus a dominance query, and witness certificates are invalidated by later pins. Even under optimistic assumptions the descent is an order of magnitude or more behind the layered implementation — and end users do not observe the difference between the two Pareto points the semantics select. What the exploration contributes is what this page records: the impossibility theorems that close the declarative route, the proof machinery reused for the layered guarantees, and a precise understanding of what the filter does and does not preserve — satisfiability (provably), the layered answer (provably), the front (provably not).","title":"Why it was set aside"},{"category":"section","location":"theory/layered/#The-layered-solution-and-filtering","page":"The layered solution & filtering","text":"This page defines the semantics resolve implements — the layered solution — and proves the two guarantees that make the implementation trustworthy:\n\nfiltering preserves satisfiability — a solvable problem never becomes unsolvable by filtering; and\nfiltering preserves the answer — the solution computed on the filtered problem equals the layered solution of the raw problem, so the semantics is independent of filtering details.\n\nThe second result is the important one: it means the layered solution has a canonical, filter-free definition, and the filter is licensed by a theorem rather than trusted as part of the spec. It also yields a general contract under which future, more aggressive reductions are automatically safe.","title":"The layered solution and filtering"},{"category":"section","location":"theory/layered/#The-layered-solution","page":"The layered solution & filtering","text":"Fix a problem, requirements, and a strict total priority order prec on packages (from the by argument). The layered trace is the following deterministic procedure over the problem's models (= valid solutions covering the requirements, junk allowed):\n\nIf no model exists, the answer is unsatisfiable (nothing).\nOtherwise set the current layer L = mathrmreqs and start with no pins.\nFor each q in L in prec-order: pin q at the best rank feasible given the pins so far — the minimum mathrmrank_M(q) over models M that extend all current pins.\nLet the next layer be the not-yet-visited dependencies of the versions just pinned; if nonempty, go to 3.\nThe answer mathrmLay(D mathrmreqs prec) is the set of pins.\n\nnote: Theorem 1 (well-definedness)\nThe layered trace is deterministic — it depends only on the problem, the requirements, and the priority order, not on which models a solver happens to produce — and its answer is a tight solution.Proof. Each step-3 choice is a minimum over a set determined by the problem and the current pins; layers are determined by pinned versions' dependency sets; so the trace is a deterministic recursion. Feasibility is maintained at every pin (the minimizing model extends the pins), so the final pin set extends to a model; the pins are exactly the dependency closure of the requirements under the pinned versions (layer construction), hence a tight solution. ∎\n\nThe layered solution is pointwise optimal: each package, taken in layer order and priority order, is at the best version compatible with the choices already made. It is also Pareto-optimal in the sense made precise on the next page — no tight solution beats it on every package — though Pareto-optimality is a strictly weaker statement than the trace itself, which pins the unique answer.","title":"The layered solution"},{"category":"section","location":"theory/layered/#Filtering","page":"The layered solution & filtering","text":"Real problems are shrunk before solving by two passes (filter_pkg_info!, run per resolve from prepare_pkg_info):\n\nReachability (find_reachable) computes, per package, a version prefix to keep, by a fixpoint of conflict-driven degradation:\n\nrequirements' first versions are reachable;\nthe first version of each dependency of a reachable version is reachable;\nif a reachable version conflicts with a reachable version, both packages' prefixes extend one further;\nif every version of a package has been pushed past (the package saturates — meaning conflicts could make it uninstallable), packages depending on it have their prefixes pushed past the depending versions — and a saturated package keeps all its versions.\n\nRedundancy elimination (mark_necessary!) then deletes any version qj for which some better version qi (i  j) has a subset of its constraints — every dependency and every (surviving) conflict of qi is also one of qj — iterated to a fixpoint.\n\nThe intuition behind reachability is: an optimal solution only settles for a worse version for a reason, and the reason is a conflict. That intuition is exactly right for the layered (pointwise) semantics, and the theorems below make it precise. (It is not right for Pareto-front reasoning — see the next page — which is one of the sharpest ways to see the difference between the two notions of optimality.)","title":"Filtering"},{"category":"section","location":"theory/layered/#Theorem-2:-the-reach-fixpoint-invariant","page":"The layered solution & filtering","text":"Everything rests on one observation about what the reachability fixpoint guarantees. Call a kept version last if it is the highest-rank (worst) kept version of an unsaturated package.\n\nnote: Theorem 2 (fixpoint invariant)\nAt the reachability fixpoint, the last kept version pr(p) of every unsaturated package p:conflicts with no kept version of any package (saturated packages' versions included); and\ndepends on no saturated package.Proof. If pr(p) conflicted with a kept qk, then whichever of the two was processed later scans the other (already reachable) and fires the degradation rule, extending p's prefix past r(p) — contradicting that r(p) is last. If pr(p) depended on a saturated package, the saturation rule pushes dependents past the depending version, likewise extending the prefix. ∎\n\nIn other words: the cascade pushes prefixes exactly until the \"all-worst-kept\" assignment becomes self-consistent. That immediately gives the first correctness result.","title":"Theorem 2: the reach fixpoint invariant"},{"category":"section","location":"theory/layered/#Theorem-3:-filtering-preserves-satisfiability","page":"The layered solution & filtering","text":"note: Theorem 3\nIf the raw problem has a solution covering the requirements, so does the filtered problem. (The converse is trivial, since filtered models are a subset of raw models.)Proof. Let S be a raw solution. First suppose no requirement's dependency chain touches a saturated package. Build T as the dependency closure from the requirements choosing every package's last kept version. By invariant (2) this closure never enters a saturated package; by invariant (1) every chosen version conflicts with nothing kept, so T is conflict-free; it is dependency-closed by construction and covers the requirements. T is a filtered solution.In general, splice: for saturated packages use S's own versions — all kept, since saturation drops nothing, and S is dependency-closed so saturated dependencies of S-members are assigned in S — and for unsaturated packages use last kept versions. Conflicts between an unsaturated choice and anything vanish by invariant (1); conflicts among the saturated choices are absent because they come from the valid S. Dependency edges are satisfied branch by branch: unsaturated members' dependencies are unsaturated by invariant (2) and get last-kept versions; saturated members' dependencies are resolved inside S or by last-kept substitutes (dependency edges are package-level, so any assigned version satisfies them). ∎\n\nFor the redundancy pass, satisfiability is preserved by direct substitution: any solution using a deleted qj maps to a solution using its dominating qi (fewer dependencies to satisfy, fewer conflicts to avoid), and domination chains terminate at a surviving version because reachability keeps prefixes — a better version is never deleted while a worse one survives.","title":"Theorem 3: filtering preserves satisfiability"},{"category":"section","location":"theory/layered/#Theorem-4:-reduction-invariance-—-the-optimization-license","page":"The layered solution & filtering","text":"The next theorem is the general principle; the filter's correctness is an instance of it.\n\nnote: Theorem 4 (reduction invariance)\nLet D be obtained from D by deleting versions (so mathrmmodels(D) subseteq mathrmmodels(D)), and suppose the layered solution mathrmLay(D) survives in D — every version it uses is kept. Then mathrmLay(D) = mathrmLay(D).Proof. By induction along the trace, both runs make identical pins. At each step the choice for q is the best rank feasible given the (identical) pins. Deleting models can only make ranks harder to achieve, so the D-best is no better than the D-best. And the D-best is witnessed in D by mathrmLay(D) itself: it extends all current pins (its values on pinned packages are exactly the pins, by induction), assigns q its D-best rank (that is what the raw trace pinned), and survives in D by hypothesis. So the choices agree; identical pins induce identical layers. The satisfiable/unsatisfiable branch also agrees, since mathrmLay(D) witnesses satisfiability in both. ∎\n\nThis is the \"decoupling\" result: the layered semantics is defined once, on the raw problem, and any reduction whatsoever is licensed by exhibiting a single surviving solution — the answer. Two consequences:\n\nCertification. Answer preservation can be checked post hoc, cheaply: resolve the filtered problem, then verify each pin against the raw problem — feasibility is free (the answer is itself a raw model), and optimality of each pin is one unsatisfiable solve (\"no raw model with these pins and a better version of q\") per pinned package. A resolver can offer machine-checked raw-canonical answers for the cost of building the raw instance plus one probe per package.\nHeadroom. Future reductions can be far more aggressive than the current filter — anything that keeps the answer is safe, and the certification above detects violations per-resolve rather than trusting the reduction globally.\n\nThe deep reason a theorem this convenient is available is that every SAT query the layered trace makes is existential (\"is a better version feasible?\") and is witnessed by the final answer, which survives the reduction. Universal queries — \"is this package in every solution?\" — do not enjoy this: deleting models changes their truth value. That asymmetry is the fundamental obstruction explored on the next page.","title":"Theorem 4: reduction invariance — the optimization license"},{"category":"section","location":"theory/layered/#Theorem-5:-the-filter-preserves-the-answer","page":"The layered solution & filtering","text":"It remains to show the actual filter satisfies Theorem 4's hypothesis.\n\nnote: Theorem 5 (reach keeps the layered answer)\nEvery version chosen by the raw layered trace is kept by reachability filtering.Proof. By induction along the trace; assume all pins so far are kept versions. Consider the step for package q, and suppose first q is unsaturated. We claim q's last kept version qr(q) is feasible given the pins, so the best feasible rank — the trace's choice — is at most r(q), i.e. a kept version. To see feasibility, construct a model: take the pins (jointly feasible by the trace's invariant, and kept by induction), add qr(q), and complete with the last-kept dependency closure of everything present. By invariant (1) of Theorem 2, qr(q) and every last-kept version conflict with nothing kept — in particular not with the pins or each other; by invariant (2) the completion never needs a saturated package; pins' dependencies are satisfied by pinned or last-kept packages. The result is a model containing the pins and qr(q). If instead q is saturated, all its versions are kept and there is nothing to prove. ∎\n\nnote: Theorem 6 (redundancy keeps the layered answer)\nThe layered trace never chooses a version deleted by redundancy elimination.Proof. Suppose the trace chose qj and qj is dominated by a kept qi with i  j. Any model witnessing qj's feasibility given the pins converts to one witnessing qi's: swap the version; qi's dependencies are a subset of qj's (already present) and its conflicts a subset (already avoided). So qi was feasible and better, contradicting that the trace chose j. Domination chains terminate at a kept version, and the argument applies to whichever kept dominator terminates the chain. ∎\n\nComposing: the raw answer survives reachability (Theorem 5), so by Theorem 4 the reach-filtered problem has the same layered answer; that answer survives redundancy elimination (Theorem 6 applied on the reach-filtered problem), so by Theorem 4 again the fully filtered problem has the same layered answer. The implementation's answer is the raw layered solution.","title":"Theorem 5: the filter preserves the answer"},{"category":"section","location":"theory/layered/#Iterating-the-filter","page":"The layered solution & filtering","text":"It is natural to ask whether a single reachability pass followed by a single redundancy pass leaves anything on the table — whether iterating the two would prune more. The answer illuminates what each pass can and cannot see.\n\nReachability alone is a closure operation:\n\nnote: Proposition (reachability is idempotent)\nRe-running reachability on its own output keeps everything.Proof. The reach set is a least fixpoint, built by a derivation whose steps mention only versions that end up kept: requirements' first versions, first versions of kept dependencies, and conflict/saturation extensions among reachable versions. Reachability deletes only version suffixes and whole packages, none of which appear in any derivation step, so the entire derivation replays on the filtered problem and re-derives every kept version. ∎\n\nThe composite filter, however, is not idempotent: redundancy elimination can invalidate reachability's reasons. A minimal example:\n\nnote: Example (redundancy changes reachability)\nTake requirements p q r with two versions of each of p, q, r, one version of s, the single dependency p1 to s (p2 depends on nothing), and conflicts q1 otimes r1, q2 otimes r1, q2 otimes p1.Reachability: q1 otimes r1 extends both packages, making q2 reachable; then q2 otimes p1 extends p, so p2 is kept — that conflict is p2's only justification. Redundancy: q2's constraints strictly contain q1's, so q2 is deleted. But the now-orphaned p2 survives: domination by p1 fails because p1's dependency on s has no counterpart in p2. Re-running reachability on the filtered problem would drop p2, whose reason to exist is gone.\n\nThe general shape is: a deleted version was another package's only reason to degrade, and the orphaned version escapes the domination test. Two things can shield an orphan. One is a dependency-set difference between it and its better sibling, as above — version-varying dependency sets again, the same dividing line as the fragment lemma. The other is saturation: saturation pushes are dependency-driven and invisible to the domination test, and deleting versions can unsaturate a package on a re-run, vaporizing the pushes that justified its dependents' worse versions.\n\n(A note on the domination test: it ignores conflicts whose partner version was itself dropped by reachability — such conflicts constrain no model over the kept versions, so ignoring them is licensed by exactly the swap argument of Theorem 6. Counting them would spuriously block domination and orphan far more versions than the two genuine mechanisms above.)\n\nNone of this threatens correctness. By Theorem 4, any number of additional passes preserves the layered answer, so iterating the filter to a mutual fixpoint is perfectly legal — and since the passes are cheap, that is what the implementation does: redundancy elimination first (it needs no reachability marks, and on registry-scale problems it does most of the shrinking), then reachability and redundancy alternating until neither deletes anything. The returns diminish fast: a further round prunes anything at all in only about 0.007% of deliberately conflict-dense random instances with version-independent dependencies and about 1% with version-varying ones, and what it prunes is marginal. Essentially all of the reduction is captured by one reachability pass and one redundancy pass; the remaining rounds just collect the stragglers because they cost next to nothing.","title":"Iterating the filter"},{"category":"section","location":"theory/layered/#Preprocessing-for-caching","page":"The layered solution & filtering","text":"Everything above concerns one resolve. When many resolves share a registry state, the question becomes which artifacts can be computed once and reused — and the answer is organized by what each artifact depends on: the registry content, the version ordering, and the requirements. Three results pin down the tiers.\n\nnote: Proposition (filtering for a larger requirement set)\nLet mathrmreqs subseteq mathrmreqs. The filter run with requirements mathrmreqs keeps every version of mathrmLay(D mathrmreqs), so by Theorem 4 mathrmLay(F_mathrmreqs(D) mathrmreqs) = mathrmLay(D mathrmreqs).Proof. Reachability is monotone in its seed set on a fixed problem — every derivation from mathrmreqs is a derivation from mathrmreqs — so the mathrmreqs-reach keeps a superset of the mathrmreqs-reach, which keeps the answer (Theorem 5). Redundancy elimination is requirement-agnostic outright: Theorem 6's swap argument converts any witness model containing a dominated version into one containing its dominator without touching coverage, so on whatever problem it runs, the layered trace of every requirement set avoids the versions it deletes. Each round of the iterated filter therefore preserves the answer, and Theorem 4 applies after each. ∎\n\nnote: Corollary (whole-registry prefiltering)\nFiltering once with every package required yields a reduced problem on which any requirement set resolves to its raw answer, with satisfiability verdicts preserved (unsatisfiability trivially, since models only shrink; satisfiability because the answer survives). Filtering is, in this sense, a monotone function of the requirements, and the all-requirements filter is its safe upper envelope.\n\nThe prefiltered problem still depends on the version ordering — both reachability's prefixes and redundancy's better-dominates-worse are stated in terms of it. How much preprocessing can be shared across orderings has an exact answer:\n\nnote: Proposition (order-free deletion is exactly model-freeness)\nA version can be deleted without changing the layered answer for every version ordering and every requirement set iff it appears in no model.Proof. If a version appears in no model, deleting it leaves the model set — and hence every trace — untouched. Conversely, if pv appears in a model M, order p's versions with v first and require p: the trace pins p at the best feasible rank, and M witnesses rank 1, so the answer contains pv and deleting it changes the answer. ∎\n\nSo the strongest ordering-independent filter is exactly the removal of model-free versions (of which the arc-consistency test — a version one of whose dependencies has no compatible version left — is a cheap sound approximation, and per-version SAT probes are the complete one). One more piece of ordering-independent structure exists:\n\nnote: Lemma (interchangeable versions)\nCall two versions of a package interchangeable when they have the same dependency set and every compatibility entry in the problem — their own and every other package's — constrains them equally (equivalently: their constraint rows are identical). Interchangeable versions can be collapsed to whichever of them the active ordering ranks best, preserving that ordering's layered answers: the collapsed versions are dominated by the representative (identical constraints are in particular a subset), so Theorem 6's argument applies on the raw problem and Theorem 4 finishes. The classes are ordering-independent; only the representative choice is not.Beware that a package's own dependencies and compat matching is not sufficient: another package's compatibility bound can admit one of the two versions and not the other, and then they are genuinely distinguishable — solutions needing the excluded pairing must pick the right one. The equal-incoming-constraints half of the definition is what rules this out.\n\nTogether these give a three-tier cache: per registry state (parsed data, the model-free set, interchangeability classes), per ordering (the whole-registry prefilter), and per resolve (requirement-specific filtering, which is fast and not worth caching). A non-standard ordering — say, preferring versions closest to an installed manifest — invalidates only the middle tier, which is orders of magnitude cheaper to rebuild than the first.\n\nThe implementation draws the line between the first tier and the rest at pkg_info, whose output — conflict matrices indexed by the classes (version_classes), with the versions that cannot be installed whatever else is chosen already dropped — is exactly the registry-only artifact, and it merges the second and third tiers into one per-resolve pass, prepare_pkg_info, since the middle tier is cheap enough not to be worth storing.\n\nThe artifact is indexed by those classes throughout: a PkgInfo's conflict matrix has one row per class and one column per partner class. Building it takes one row per version first, since that is what the partition is computed from, and then merges them (collapse_classes!). Nothing is lost in the merge: members of a class have equal rows by definition, and — since conflicts are recorded symmetrically — members of a partner class index equal columns, so the merged matrix states exactly the same conflicts.\n\nWhat the per-resolve pass then does is choose, per class, which member it stands for. The user's constraints act only by forbidding members (exclusion_masks): a class keeps whichever of its admissible members the query's ordering ranks best, and competes at that member's rank, which is why the classes have to be laid out in the order those representatives put them in (class_ranking). A class the query admits no member of is deactivated — nothing can choose it — and that, not deletion, is the whole of a constraint's effect. Then the result is filtered (filter_pkg_info!).\n\nDeactivation is deliberately not deletion. A deactivated class stays in the matrices: reachability continues its prefix past one, since a class that cannot be selected is not somewhere a package can be installed, but still follows its dependencies, and redundancy elimination neither deletes one nor lets one license a deletion. What that buys is that the universe a later question about relaxing a constraint would need is still present — and, one level down, that such a question is even well posed, since a constraint cannot make two versions' rows identical: it does not touch the rows, and versions whose rows are identical are one class sharing one column, with no pair of separately deletable objects to fall between.\n\nThe ordering is a resolve parameter rather than part of the problem, which is what makes one artifact serve every query: it selects among the valid solutions instead of changing which solutions are valid. What does change the model set — the user's compat bounds and pins, and the admission of prerelease versions — is a Problem, and enters as constraints on the artifact's versions rather than as deletions from it. So there is one T1 artifact per registry state, full stop: every version the registry state offers is in it, in canonical order and in its class, and each query says which of them it will accept and in what order it prefers them.\n\n\"Every version the registry state offers\" is meant strictly, and it is where yanked versions come in. Yankedness is not a query fact but part of what the registry state says — a yanked version is one the registry has struck, so it is not on offer — and the default artifact is built with those versions filtered out at the provider (see bin/Registries.jl). That is a deletion, not a constraint, and it is the right shape: a resolve then cannot produce a yanked version or suggest one as an alternative, with nothing left to enforce per query. The cost is that --allow-yanked, the query that wants them back, is the one query the baked artifact does not serve: it asks for a universe the registry state does not describe, and so rebuilds T1. That is rare and cheap next to the alternative, which is carrying struck versions through every layer and every diagnostic in order to forbid them again at the end.\n\nwarning: Formalization boundary\nTheorems 2, 3, 5, and 6 are proved against the rule set as stated above, which is a faithful but hand-made reading of find_reachable and mark_necessary!; the correspondence of code to rules is checked by review and by the test suite, not mechanically. The package's tests compare the implementation against a brute-force reference that implements the layered trace on raw data across hundreds of thousands of random instances — a direct, ongoing empirical check of Theorems 4–6 — and, whenever the implementation reports unsatisfiable, verify by enumeration that no raw solution covers the requirements (Theorem 3).","title":"Preprocessing for caching"}]
}
