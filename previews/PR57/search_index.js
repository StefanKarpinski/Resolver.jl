var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Diagnosing-unsatisfiability","page":"API","text":"An unsatisfiable resolve returns a Diagnosis rather than a bare \"no\". Its parts are plain data, so a caller can render its own report — see the guide — and the theory that licenses computing them is on the diagnostics theory page.","title":"Diagnosing unsatisfiability"},{"category":"section","location":"api/#Rendering-a-report-yourself","page":"API","text":"The Diagnosis retains everything; every opinionated choice in the default output is a documented function, with show as merely its first client. See the guide.","title":"Rendering a report yourself"},{"category":"section","location":"api/#Comparing-solutions","page":"API","text":"","title":"Comparing solutions"},{"category":"section","location":"api/#Explaining-a-surprising-success","page":"API","text":"A resolve can succeed and still leave a package below the best version its universe admits. holdbacks says what held it there.","title":"Explaining a surprising success"},{"category":"section","location":"api/#Internals","page":"API","text":"","title":"Internals"},{"category":"function","location":"api/#Resolver.resolve","page":"API","text":"resolve(data, prob::Problem; with = ..., without = ..., diagnose = true)\n    -> Union{Dict{P,V}, Diagnosis{P,V}, Nothing}\nresolve(data, reqs; by = identity, diagnose = true, compat = ..., pins = ...)\n\nResolve the requirements reqs against the package universe described by data, returning the optimal solution as a dict mapping each needed package to its chosen version. The solution covers every requirement and is exactly the dependency closure of the requirements under its own chosen versions.\n\nWhen the requirements are not jointly satisfiable the result is a Diagnosis — the independent conflicts, the facts that tell each one's story, and a menu of verified fixes — which show renders as a report. Pass diagnose = false to skip computing it and get nothing instead, which is all a caller that only wants the verdict needs and costs nothing to produce.\n\nA Problem additionally constrains the admissible versions with user compat bounds and pins; the answer is the one the same universe with those versions deleted would give. The requirements-and-keywords form builds one, so resolve(data, reqs; compat, pins) and resolve(data, Problem(reqs; compat, pins)) are the same call.\n\nThe returned solution is the layered solution: requirements are optimized first, in the priority order induced by by (each package maximized to its best feasible version given the choices already made), then the dependencies of the chosen versions, layer by layer along the dependency graph. See the manual's Theory section for the precise semantics and the guarantees it carries.\n\ndata may be a DepsProvider, a dict of PkgData, a dict of PkgInfo, or a SAT instance. The SAT method also accepts restore::Bool = true: when true the instance's clauses are restored before returning so the instance can be reused; restore = false is faster for single-use instances.\n\nThe other methods accept two more keywords:\n\norder: the version preference ordering, as a callable mapping a package to a lt comparator over its versions (\"is preferred to\"). The default, nothing, means the order the universe already lists them in — which for a T1 artifact (see pkg_info) is the canonical one the registry state fixes. The ordering is not a constraint: it selects among the valid solutions rather than changing which ones are valid, so it is a parameter here instead of part of the Problem, and one artifact serves every ordering.\ngroup::Bool = true: whether to collapse each package's interchangeable versions to one representative before solving (see prepare_pkg_info). Grouping is an optimization and cannot change the answer; group = false is the escape hatch that says so.\n\nGoals\n\nwith and without say what the resolve is for, beyond satisfying prob:\n\nresolve(info, prob; with = \"CUDA\")                        # present\nresolve(info, prob; with = \"DataFrames\" => v\"1.8.2\")      # present, at a version\nresolve(info, prob; without = \"FillArrays\")               # absent\nresolve(info, prob; with = [\"CUDA\" => spec(\"5\"), \"MKL\"], without = \"GLMakie\")\n\nwith takes a package, a pkg => versions pair (any in-supporting set, or a bare version), or a collection of either; without takes a package or a collection. Together they express a conjunction.\n\nThe return contract is unchanged: the optimal solution satisfying prob and the goal, a Diagnosis of why it cannot be had, or nothing under diagnose = false — which with a goal is the cheap feasibility probe. (Cheaper still, when the versions are not wanted either: issatisfiable.)\n\nWhat separates with = \"CUDA\" from adding CUDA to prob.reqs is one sentence: prob's constraints are negotiable — a diagnosis may propose relaxing any of them — and the goal is the fixed ask, so \"drop requirement CUDA\" can never appear as a fix.\n\nA goal is answered on a sub-instance built from the T1 artifact rather than on the per-resolve one (see prepare_goal_info), because the per-resolve filter prunes exactly the escape routes a goal needs. When data is a provider or a PkgData dict the closure is widened to cover the goal's packages; a caller-supplied PkgInfo artifact is used as it stands, so a goal naming a package outside it reads as unsatisfiable.\n\n\n\n\n\n","title":"Resolver.resolve"},{"category":"function","location":"api/#Resolver.issatisfiable","page":"API","text":"issatisfiable(data, prob::Problem; with = ..., without = ...) :: Bool\nissatisfiable(data, reqs; with = ..., without = ..., compat = ..., pins = ...)\n\nCan prob be satisfied — and, with a goal, satisfied while getting what the goal asks for? One solve, no optimizing descent, no explanation: the cheapest question in the API, for the caller that is asking it in a loop.\n\nThis is the bottom rung of a three-tier ladder, all three answering the same question with more or less work:\n\ncall cost answer\nissatisfiable(data, prob; with) one solve Bool\nresolve(data, prob; with, diagnose = false) one solve + descent the solution, or nothing\nresolve(data, prob; with) + explanation the solution, or a Diagnosis\n\nNothing distinguishes them but effort — the verdicts agree — so a UI greying out an upgrade button uses the first, a caller that wants the versions uses the second, and only the one that will actually show a report pays for the third.\n\n\n\n\n\n","title":"Resolver.issatisfiable"},{"category":"type","location":"api/#Resolver.Problem","page":"API","text":"Problem(reqs; compat = ..., pins = ..., excludes = ...)\n\nA resolution problem: the required packages reqs, plus the constraints that say which versions are admissible. A Problem carries everything that determines satisfiability; orderings (the by priority order, the order version rank order) are resolve parameters instead.\n\nThree kinds of constraint, in two shapes:\n\ncompat: per package, the set of allowed versions (queried with in).\npins: per package, the one version it is held at.\nexcludes: the admission knobs — \"no prereleases\", \"no yanked versions\" — which say something about versions rather than about particular packages, and so come as kind => predicate pairs, where predicate(p, v) is true for the versions that source forbids and kind is a symbol naming it.\n\nConstraints for packages that don't exist, and constraints that exclude nothing, are allowed and have no effect.\n\nEvery constraint here is a constraint on a shared package universe rather than a deletion from a private one: that is what lets a single T1 artifact (see pkg_info) serve queries that admit different things, and what lets diagnostics eventually name the kind that ruled a version out.\n\n\n\n\n\n","title":"Resolver.Problem"},{"category":"type","location":"api/#Resolver.Diagnosis","page":"API","text":"Diagnosis(reqs, conflicts, fixes, versions)\n\nWhy a resolution problem is unsatisfiable: the conflicts are independent, the fixes are global (each one repairs every conflict at once), and versions records the version list of every package the report mentions, so a renderer can compress version sets into ranges.\n\nAn empty fixes means there is nothing to propose: every correction would have to drop a requirement that dropping does not help, which happens when only an upstream release could resolve the conflict.\n\n\n\n\n\n","title":"Resolver.Diagnosis"},{"category":"type","location":"api/#Resolver.Conflict","page":"API","text":"Conflict(reqs, chain, upstream, goal = false)\n\nOne independent reason the problem is unsatisfiable: reqs is a minimal set of requirements that cannot be satisfied together, chain tells the story as an ordered list of facts, and upstream lists bounds whose relaxation would resolve this conflict.\n\ngoal says whether the caller's with/without ask is part of this conflict — true when reqs alone is satisfiable and only the goal makes it fail, which is what the report's header says out loud. A conflict with goal true and no reqs is the goal against the user's own constraints.\n\n\n\n\n\n","title":"Resolver.Conflict"},{"category":"type","location":"api/#Resolver.Fix","page":"API","text":"Fix(actions, solution)\n\nA verified minimal correction: relaxing every fact in actions — dropping a Requirement, loosening a UserCompat, lifting a Pin — makes the problem solvable, and solution is the resolution it then produces.\n\n\n\n\n\n","title":"Resolver.Fix"},{"category":"type","location":"api/#Resolver.UpstreamFix","page":"API","text":"UpstreamFix(bound, solution)\n\nA conflict that a new upstream release could fix on its own: were bound relaxed, the conflict would go away and solution is what would resolve.\n\n\n\n\n\n","title":"Resolver.UpstreamFix"},{"category":"type","location":"api/#Resolver.Requirement","page":"API","text":"Requirement(pkg)\n\nThe user asked for pkg. Relaxing it means dropping the requirement.\n\n\n\n\n\n","title":"Resolver.Requirement"},{"category":"type","location":"api/#Resolver.Uninstallable","page":"API","text":"Uninstallable(pkg)\n\npkg has no installable version at all — every version of it depends, transitively, on a package with no versions. Nothing the user can relax fixes this; the only correction is to stop requiring pkg.\n\n\n\n\n\n","title":"Resolver.Uninstallable"},{"category":"type","location":"api/#Resolver.UserCompat","page":"API","text":"UserCompat(pkg, allowed, [label])\n\nThe user's compat bound on pkg, as the list of that package's versions it admits. Relaxing it means loosening (or deleting) the bound.\n\nlabel says what kind of restriction it was, for reports: :compat (the default) reads as \"your compat restricts …\", :requested as \"you requested …\". A caller sets it through Problem's labels, because \"your compat\" is wrong when the bound came from Pkg.add(name, version) rather than a compat section. Unknown labels get a neutral phrasing rather than an error.\n\n\n\n\n\n","title":"Resolver.UserCompat"},{"category":"type","location":"api/#Resolver.Pin","page":"API","text":"Pin(pkg, version)\n\npkg is pinned at version. Relaxing it means unpinning.\n\n\n\n\n\n","title":"Resolver.Pin"},{"category":"type","location":"api/#Resolver.Admission","page":"API","text":"Admission(kind, pkg, forbidden)\n\nAn admission knob ruling versions of pkg out: kind names the source (:prerelease, :yanked, …) and forbidden lists the versions it excludes. Unlike a compat bound or a pin, a knob is stated about versions rather than about a package, so the same one appears once per package it touches.\n\nRelaxing it means admitting that class of version — passing --allow-pre, say.\n\n\n\n\n\n","title":"Resolver.Admission"},{"category":"type","location":"api/#Resolver.Bound","page":"API","text":"Bound(pkg, versions, dep, allowed)\n\nA third-party compatibility bound: the listed versions of pkg are incompatible with every version of dep outside allowed. Only an upstream release can relax one.\n\nThe resolver's package info records compatibility symmetrically — a conflict between pkg@v and dep@w is one fact, not two directed ones — so a Bound names an incompatible pair of version groups rather than saying which side declared the bound. See the manual's diagnostics page.\n\n\n\n\n\n","title":"Resolver.Bound"},{"category":"type","location":"api/#Resolver.Dependency","page":"API","text":"Dependency(pkg, versions, dep, admissible)\n\nThe listed versions of pkg all depend on dep, so choosing pkg at any of them drags dep in. admissible is true when versions is the subset the query's own constraints leave available rather than the whole list, which is the difference between \"all versions\" and \"all admissible versions\" in a report.\n\nThis is the fact a without goal's story is made of: nothing conflicts with anything, the banned package is simply unavoidable. Only a chain of forced edges is ever emitted — a path some other version choice could route around explains nothing, which is what separates this from printing every dependency path.\n\n\n\n\n\n","title":"Resolver.Dependency"},{"category":"function","location":"api/#Resolver.report","page":"API","text":"report(io, d::Diagnosis; max_upstream = 3, demote_incidental = true,\n                         trim_witnesses = true, labels = nothing)\n\nRender d as the report show prints, with the opinionated parts turned into arguments. show(io, MIME(\"text/plain\"), d) is report(io, d) and nothing else, so every default here is a documented choice rather than a hidden one.\n\nmax_upstream — how many upstream suggestions to print before the \"(N more …)\" line. They are ranked (see rank_upstream!), so the tail is the least plausible; typemax(Int) prints all of them.\ndemote_incidental — fold the Bounds whose incidental flag is set into the one-line \"(likewise for …)\" aside instead of giving each a bullet. They are needed for the conflict to be minimal but they only close off versions the query already excludes.\ntrim_witnesses — restrict each \"→ allows:\" line to the requirements and the packages some story names. A verified solution is the whole transitive closure; the contested packages are what a fix meaningfully changes, and the complete assignment is on Fix.solution either way.\nlabels — override the source wording per package, as Problem's labels does at diagnosis time: :requested reads \"you requested X at 1.7\", :compat (the default) \"your compat restricts X to 1.7\". Unknown labels render neutrally. Pass this when the wording is a property of the caller rather than of the problem.\n\nA client that wants a different layout entirely builds it from the structured Diagnosis with the same sentence helpers this uses — render_fact, render_action and blame_phrase — or reads the Fact fields and writes its own sentences.\n\n\n\n\n\n","title":"Resolver.report"},{"category":"function","location":"api/#Resolver.render_fact","page":"API","text":"render_fact(io, f::Fact, d)\n\nWrite one story bullet's sentence for f — \"you require A\", \"your compat restricts B to 1.7\", \"A (all versions) works with C only at 1\". d is whatever carries the version lists to compress against: a Diagnosis or a Holdback.\n\nOne method per Fact kind, and the sentence layer of the default report. A client that wants these sentences under its own layout — Pkg's indentation, its own terminal markup — calls this per fact instead of report; a client that wants its own sentences reads the Fact fields (pkg, versions, dep, allowed, incidental, …), which are everything the sentence is generated from.\n\n\n\n\n\n","title":"Resolver.render_fact"},{"category":"function","location":"api/#Resolver.render_action","page":"API","text":"render_action(f::Fact) :: String\n\nThe imperative form of relaxing f, as a fix's menu line reads it: \"drop requirement A\", \"relax your compat on B\", \"unpin C\", \"allow prereleases of D\". The counterpart of blame_phrase, which is the same fact as a noun.\n\n\n\n\n\n","title":"Resolver.render_action"},{"category":"function","location":"api/#Resolver.blame_phrase","page":"API","text":"blame_phrase(f::Fact) :: String\n\nThe noun form of the change f stands for — \"your compat on Statistics\", \"your pin on Foo\" — for sentences that name a culprit rather than issue an instruction (\"held back by your compat on Statistics\"). The counterpart of render_action.\n\n\n\n\n\n","title":"Resolver.blame_phrase"},{"category":"function","location":"api/#Resolver.rank_upstream!","page":"API","text":"rank_upstream!(ups, info) -> ups\n\nOrder upstream suggestions by how plausible they are to act on, most first.\n\nTwo keys, in order:\n\nHow current the blamed versions are. A release that relaxes a bound its newest versions carry is a thing a maintainer might actually do; asking them to reach back and loosen a range that only ancient versions had is not. Measured as the best rank the incompatibility touches on either side — versions are listed best-first, so smaller is newer.\nHow good the result is. Between two plausible asks, the one that gets you newer packages is the better suggestion. Measured on the witness, which is an optimal layered solution, over the two packages named.\n\nTies break on the package names, so the order is deterministic.\n\nrank_upstream!(ups, d::Diagnosis) reads the version lists off the diagnosis, which is the form a client that only has the report's structured data can use. Every UpstreamFix carries its bound and a verified solution, so a client with better knowledge — Pkg knows which packages are actively maintained — is free to filter first, or to sort by its own criteria and ignore this one.\n\n\n\n\n\n","title":"Resolver.rank_upstream!"},{"category":"function","location":"api/#Resolver.superseded","page":"API","text":"superseded(v, versions) :: Bool\n\nIs v a prerelease that a release has already replaced — a version x.y.z-… for which x.y.z itself is in versions?\n\nTelling someone to accept 1.2.3-alpha1 when 1.2.3 exists is not advice, so admitting prereleases is only ever suggested for versions this returns false for. The test is deliberately restricted to the same base version: the generalization that a newer release supersedes an older prerelease across base versions was considered and left out, pending evidence that it is what people want (an unreleased 2.0.0-rc1 is a real thing to want even when 1.9.0 is out).\n\nVersion types with no notion of a prerelease are never superseded, so this costs nothing outside the VersionNumber world.\n\n\n\n\n\n","title":"Resolver.superseded"},{"category":"function","location":"api/#Resolver.changes","page":"API","text":"changes(sol₁, sol₂) :: Vector{Change}\n\nWhat sol₂ does differently from sol₁, one Change per package that moved, appeared or disappeared, sorted by package.\n\nThis is how a feasible goal query gets its price tag: resolve answers without = \"TranscodingStreams\" with a Dict rather than a complaint, and the \"avoidable, but it costs you CSV 0.10.15 → 0.5.26\" rendering is this diff against the plain resolve. Both solutions are the caller's already; nothing here needs the resolver.\n\n\n\n\n\n","title":"Resolver.changes"},{"category":"type","location":"api/#Resolver.Change","page":"API","text":"Change(pkg, from, to)\n\nOne package's difference between two solutions: from is nothing when the package is new, to is nothing when it is gone, and otherwise both are versions and they differ.\n\n\n\n\n\n","title":"Resolver.Change"},{"category":"function","location":"api/#Resolver.holdbacks","page":"API","text":"holdbacks(info, prob, sol, [pkgs]; by = identity, max_probes = 8)\n\nExplain the packages in pkgs that sol resolved below their best admissible version, as a Holdback each; packages that are at their best, or whose version is merely the one the priority order settled on, produce nothing.\n\n\"Best\" is the newest version prob's admission knobs allow: compat bounds and pins are what a fix relaxes, so they do not bound it, but a prerelease no query would accept is not an option being missed. A package with no admissible version at all is an unsatisfiable problem's business and produces nothing here.\n\ninfo must be the prepared universe the solution came from (see prepare_pkg_info) and prob its problem. The instance this builds is its own, so nothing the caller holds is disturbed.\n\nA holdback whose only remedy is something no fix can propose — an incompatibility that only an upstream release could dissolve — is reported with no fix rather than with an invented one.\n\nmax_probes bounds how many packages are probed, since each costs a handful of solves over the whole instance; the result's unexamined says how many candidates that left over, and rendering it says so.\n\n\n\n\n\n","title":"Resolver.holdbacks"},{"category":"type","location":"api/#Resolver.Holdback","page":"API","text":"Holdback(pkg, resolved, best, chain, fix, versions)\n\nWhy pkg resolved to resolved rather than to best, the newest version of it the problem's admission settings allow.\n\nchain tells the story in the same Fact vocabulary an unsatisfiable diagnosis uses, and fix — when there is one — is a verified correction whose solution says what pkg would resolve to instead. fix is nothing when nothing the caller is allowed to relax would help, which is a real answer and not a failure: a version held back by bounds only an upstream release could move is held back for a reason the report should state rather than paper over.\n\nversions records the version lists the report needs, as in Diagnosis.\n\n\n\n\n\n","title":"Resolver.Holdback"},{"category":"type","location":"api/#Resolver.Holdbacks","page":"API","text":"Holdbacks(held, unexamined)\n\nWhat holdbacks returns: the explanations, as an ordinary vector of Holdback — iterate it, index it, isempty it — plus unexamined, the number of candidate packages the probe budget stopped it from looking at.\n\nExplaining a package costs several solves over the whole instance, so the budget is real and a caller that silently dropped the rest would be misreporting. The count is carried rather than implied, and show says it out loud.\n\nunexamined counts packages that resolved below their best version and were never probed — not packages known to be held back. Whether a constraint is actually to blame is exactly what a probe decides, so claiming more than that would be guessing.\n\n\n\n\n\n","title":"Resolver.Holdbacks"},{"category":"function","location":"api/#Resolver.pkg_info","page":"API","text":"pkg_info(data, reqs = all packages; filter = true) :: Dict{P,PkgInfo{P,V}}\n\nThe T1 artifact: the dependency closure of reqs, encoded as one PkgInfo per package — conflict matrices, plus the preprocessing that depends on the registry alone, namely the arc-consistency prune (mark_installable!) and the interchangeability partition (version_classes). Nothing here depends on the version ordering, on user compat or pins, or (beyond the closure) on the requirements, so the result can be computed once and shared by, or cached across, arbitrarily many resolves.\n\nfilter = false skips the arc-consistency prune, leaving the conflict matrices exactly as the data spells them out.\n\nRequirement-specific and constraint-specific shrinking — reachability and redundancy elimination — happens per resolve instead, after the classes have been refined and collapsed: see prepare_pkg_info.\n\n\n\n\n\n","title":"Resolver.pkg_info"},{"category":"function","location":"api/#Resolver.version_classes","page":"API","text":"version_classes(X, m) :: Vector{Int}\n\nThe interchangeability partition of the m versions whose conflicts matrix is X, as a class id per version — ids numbered 1, 2, … in the version order of each class's first member.\n\nPer the manual's Theory section (lemma on interchangeable versions), two versions of a package are interchangeable when they have the same dependency set and every compatibility entry in the problem — their own and every other package's — constrains them equally. In PkgInfo terms that is exactly row equality, and the test is purely local to X: the dependency columns carry the dependency set, and the interaction blocks carry every compatibility entry that mentions the package, incoming as much as outgoing, because pkg_info records each conflict symmetrically in both partners' matrices. (The incoming half is not optional — another package's bound can admit one of two versions and not the other, and then they are genuinely distinguishable.)\n\nThe partition is a property of the registry alone: it is independent of the requirements, of the version ordering, and of any user constraints. That is what puts it on the T1 side of the boundary, and what lets the collapse to class representatives be a per-resolve step (see class_representatives).\n\nThe scan is word-parallel over columns: one pass computes, for every adjacent pair of versions at once, whether their rows differ anywhere, which yields the classes that are contiguous runs of versions — nearly all of them in practice, since dependency sets and compat bounds vary in blocks. A second pass hashes one row per run and merges runs whose rows compare equal, so the result is the exact row-equality partition, hence invariant under reordering versions.\n\n\n\n\n\n","title":"Resolver.version_classes"},{"category":"function","location":"api/#Resolver.prepare_pkg_info","page":"API","text":"prepare_pkg_info(info, prob, [info′]; group = true, order = nothing)\n\nThe per-resolve half of preprocessing, run on a T1 artifact (see pkg_info) to produce the universe the SAT instance is built over. Three steps, in this order:\n\nLay out each package's versions in the ordering the query wants (version_permutations), which is a no-op for the canonical order.\nCollapse each interchangeability class, refined by prob, to its best member in that ordering (class_representatives).\nFilter the collapsed universe for reachability and redundancy against the actual requirements and constraints (filter_pkg_info!).\n\nFiltering has to come last: both of its passes are stated in terms of the version ordering, and both get cheaper the fewer versions there are. The layout and the collapse share one materialization with it: the collapse is handed over as marks rather than as a rebuilt universe, so the filter's own first drop_unmarked! performs both (see copy_marked!).\n\nThe result is a fresh dict — info is left untouched, so a T1 artifact stays reusable across resolves — unless the caller passes its own scratch dict as info′, or info itself when it owns it (which reordering rules out, since it rebuilds the matrices). group = false skips the collapse, which is the ungrouped path the tests compare against.\n\n\n\n\n\n","title":"Resolver.prepare_pkg_info"},{"category":"function","location":"api/#Resolver.version_permutations","page":"API","text":"version_permutations(info, order) :: Union{Nothing, Dict{P, Vector{Int}}}\n\nPer package, the permutation that puts its versions into the ordering order asks for: perm[r] is the index, in the T1 version list, of the r-th best version. Only the packages that actually need reordering get an entry, and the whole result is nothing when none of them do — so the canonical order, which is the one the T1 artifact carries, costs nothing.\n\norder is nothing for the canonical order, or a callable mapping a package to a lt comparator over its versions (lt(u, v) = \"u is preferred to v\"). The ordering is the one query knob that is not a constraint: it does not change which solutions are valid, only which valid solution is optimal. That is why it is a resolve parameter rather than part of the Problem, and why the T1 artifact can be built and cached without knowing it (see the manual's Theory section, on order-free deletion and on interchangeability classes being ordering-independent while the representative choice is not).\n\n\n\n\n\n","title":"Resolver.version_permutations"},{"category":"function","location":"api/#Resolver.class_representatives","page":"API","text":"class_representatives(info, prob, perms) :: Dict{P, BitVector}\n\nPer package, a mask of the versions that survive collapsing its interchangeability classes: the T1 classes (version_classes) refined by prob, then one representative per refined class.\n\nRefinement. User compat and pins are the only constraints that can tell two members of a T1 class apart, because they are stated on version values instead of on the registry's rows — so splitting each class by prob's exclusion_masks is enough, and it costs nothing for the packages the user did not constrain. Read as the virtual package of Problem's docstring, this is just row equality again, over one more conflict column.\n\nRepresentative. The best member in the active rank order — the lowest-indexed one when the order is canonical, and otherwise the one perms (see version_permutations) ranks first.\n\nCollapsing is answer-preserving: identical constraint rows are in particular a subset of each other, so a class member is dominated by its representative and the redundancy theorem's swap argument applies (see the manual's Theory section, lemma on interchangeable versions). Representatives are versions, and the members dropped are exactly the ones the layered answer never chooses, so nothing has to be mapped back afterwards.\n\n\n\n\n\n","title":"Resolver.class_representatives"},{"category":"function","location":"api/#Resolver.find_reachable","page":"API","text":"find_reachable(info, reqs) :: Dict{P, Int}\n\nThis function finds a minimal \"reachable\" subset of package and versions that could appear in pareto-optimal solutions to version resolution for the given set of required \"root\" packages, using the following recursive logic:\n\nP in reqs => P[1] reachable\nP[i] reachable & P[i] depends on D => D[1] reachable\nP[i] reachable & P[i] conflicts w. reachable => P[i+1] reachable\nD[end] conflicts w. reachable & P[i] depends on D => P[i+1] reachable\n\nThe optional excl argument gives, per package, a mask of versions the user forbade. Those are the conflict rows of the always-present virtual package that represents the user constraints, so a reachable excluded version fires the same degradation rule as any other conflict:\n\nP[i] reachable & P[i] excluded => P[i+1] reachable\n\nThe function returns a dictionary mapping packages to the maximum version index of that package that could be reached in an optimal solution. If a package cannot appear in an optimal solution, it will not appear in this dictionary.\n\n\n\n\n\n","title":"Resolver.find_reachable"},{"category":"function","location":"api/#Resolver.exclusion_masks","page":"API","text":"exclusion_masks(info, prob) :: AbstractDict{P, BitVector}\n\nThe versions of each package that prob's constraints forbid, as a mask over that package's version list in info. Only packages with a constraint that actually excludes something get an entry, so packages nothing constrains — the overwhelming majority, absent an admission knob — cost nothing downstream.\n\nMasks are index-based, so they must be recomputed whenever versions are dropped and renumbered (see filter_pkg_info!).\n\n\n\n\n\n","title":"Resolver.exclusion_masks"},{"category":"function","location":"api/#Resolver.prepare_goal_info","page":"API","text":"prepare_goal_info(info, prob, goal; order = nothing) :: Dict{P,PkgInfo{P,V}}\n\nThe goal sub-instance: the T1 artifact info, collapsed to interchangeability-class representatives and arc-consistent, but not filtered for reachability or redundancy.\n\nThose two passes are what the per-resolve preparation (prepare_pkg_info) adds, and they prune exactly the escape routes a goal needs: reachability keeps only the versions an optimal solution could use, and redundancy drops a version a better one dominates — either can delete the only version of a package that avoids (or reaches) the package the goal is about. See the manual's diagnostics theory page on goal safety.\n\nWhat survives is licensed: arc consistency deletes only versions no model contains, which can witness no goal; and interchangeability classes never merge across different dependency sets, so a package that avoids X can never hide inside a class whose representative needs it. Versions a goal names by value are kept out of the collapse besides, since a class sibling is not the version that was asked for.\n\n\n\n\n\n","title":"Resolver.prepare_goal_info"},{"category":"type","location":"api/#Resolver.Goal","page":"API","text":"Goal(with, without)\n\nA resolve's fixed ask: with names packages that must be present — each as pkg => versions where versions is nothing for \"any version\" and otherwise anything in answers for — and without names packages that must be absent.\n\nBuilt from resolve's with / without keywords rather than constructed directly; nothing about it is exported.\n\n\n\n\n\n","title":"Resolver.Goal"},{"category":"function","location":"api/#Resolver.diagnose","page":"API","text":"diagnose(sat, reqs; by = identity, max_fixes = 8) :: Diagnosis\n\nExplain why reqs cannot be satisfied on sat, and list verified fixes.\n\nMust be called on an instance whose satisfiability check has just failed, with no clauses added since: the frame holding the user-constraint selectors is popped for the duration and re-asserted afterwards, so the instance is left exactly as it was found and can be reused.\n\n\n\n\n\n","title":"Resolver.diagnose"},{"category":"type","location":"api/#Resolver.Relaxation","page":"API","text":"Relaxation\n\nThe knobs a diagnosis may turn on the failed instance: rel is every group, reqs the requirement groups a fix may drop, and user the constraint groups a fix may relax.\n\nuser is one group per constrained package, holding all of its selectors – its whole exclusion column. Switching a whole column off is a column-closed relaxation whatever the sources in it, which is the granularity Theorem D1 licenses and the only one anything here asks for.\n\nNo group is assumed implicitly: a group left out of an assumption set is free, and the solver will happily switch it off, so each caller names exactly the context its query means to hold.\n\ngoal is the exception, and the only one: the goal's selector literals, which every probe holds on whether or not it says so. A goal is the query's fixed ask, so no MUS may shrink it away and no fix may propose dropping it — it is not a relaxable group, just an assumption the machinery carries.\n\n\n\n\n\n","title":"Resolver.Relaxation"},{"category":"function","location":"api/#Resolver.bound_story","page":"API","text":"bound_story(info, prob, creqs, users, by) -> (facts, upstream, kept)\n\nBound-level detail for one conflict cluster: which package-pair incompatibilities and which of the user's own constraints are needed to make creqs unsatisfiable, and which incompatibilities a single upstream release could dissolve.\n\nRuns on a sub-instance restricted to creqs' dependency closure, relaxing conflicts one pair at a time — the finest granularity Theorem D1 covers. The deletion order is actionability-biased: incompatibilities only an upstream release can move go first, so a conflict tellable either way keeps the explanation the user can act on. kept is the subset of users that survived; it is nothing when the closure was too big to be worth the solves, and the caller has to shrink the user groups itself.\n\ngoal puts the caller's fixed ask into every probe here as well, and widens the closure to cover the packages it names. A goal that is more than a presence ask also turns off the sub-instance's reachability and redundancy passes, which are not goal-safe (see prepare_goal_info) — the sub-instance is then bigger, and the closure budget may decline it, which is the honest outcome rather than a wrong one.\n\n\n\n\n\n","title":"Resolver.bound_story"},{"category":"section","location":"theory/diagnostics/#Diagnosing-unsatisfiability","page":"Diagnosing unsatisfiability","text":"When a resolve fails, the resolver explains why: which requirements clash, which constraints are implicated, and what relaxations would make the problem solvable. Every one of those questions is answered by more SAT queries on the very instance that just failed — the prepared production instance, with the user's constraints turned back into assumptions.\n\nThat is a strong claim, because the instance was built for one purpose (to resolve one problem) and is now being asked a family of different questions. This page works out exactly how far it is allowed. The answer is not \"always\": preparation is stable under relaxations of a certain granularity and demonstrably unstable below it, and that boundary is what determines the units a diagnosis is allowed to blame.\n\nThree results:\n\nRequirement-subset queries are already licensed by the existing requirement-monotonicity proposition.\nRelaxation stability (new here) covers switching off constraint groups — but only column-closed ones. A companion proposition shows the finer granularity really does fail, with a four-version counterexample.\nClosure exactness licenses answering a cluster's questions on a sub-instance restricted to that cluster's dependency closure.","title":"Diagnosing unsatisfiability"},{"category":"section","location":"theory/diagnostics/#Relaxations","page":"Diagnosing unsatisfiability","text":"Fix a problem D as in the setting, with requirements mathrmreqs. Designate a finite family G of relaxable conflict groups: each g in G is a set of conflicting version pairs. Conflicts in no group are permanent.\n\nA relaxation is a pair sigma = (mathrmreqs_0 G_0) with mathrmreqs_0 subseteq mathrmreqs and G_0 subseteq G. The relaxed problem Dsigma has the same packages, versions and dependency sets as D; its requirements are mathrmreqs_0; and its conflict relation is the permanent conflicts together with bigcup_g in G_0 g. Groups may overlap: a pair asserted by two groups survives until both are switched off, which is exactly what the selector-guarded encoding does.\n\nTwo families of group matter in practice:\n\nUser-constraint groups. A compat bound or a pin is read as a virtual package with one always-present version whose conflict rows are the versions the constraint forbids (see Problem). Switching one off is \"what if the user relaxed this?\".\nThird-party bound groups. A registry compatibility declaration contributes a rectangle of conflicting pairs. Switching one off is \"what if upstream released a version that loosened this?\".\n\nWrite mathrmLay(D sigma) for the layered solution of Dsigma (undefined when Dsigma is unsatisfiable), and F for preparation — everything between the raw universe and the SAT instance, run once, with the full requirement set and every group active. Following pkg_info and prepare_pkg_info, that is four steps:\n\narc consistency, deleting versions one of whose dependencies has no versions at all;\nthe interchangeability collapse, keeping one representative of each class of versions nothing can tell apart (refined by sigma's own exclusions — see below);\nreachability, keeping a prefix of each package's versions; and\nredundancy elimination, deleting dominated versions,\n\nwith 3 and 4 alternating to a fixpoint. Each deletes versions and nothing else, which is what lets them be treated uniformly below.","title":"Relaxations"},{"category":"section","location":"theory/diagnostics/#Requirement-subsets","page":"Diagnosing unsatisfiability","text":"The existing proposition on filtering for a larger requirement set and its corollary already say that filtering seeded with mathrmreqs preserves the layered answer — and hence the satisfiability verdict — of every mathrmreqs_0 subseteq mathrmreqs. That licenses the requirement-level half of a diagnosis: clustering the requirements into independent conflicts, and testing whether dropping some of them helps. Nothing new is needed.\n\nWhat it does not cover is the other half — lifting a compat bound, a pin or a registry bound. Those change the conflict relation, which is what the preparation's reachability prefixes and domination tests are computed against — and, in the case of the collapse, what its class representatives are chosen from.","title":"Requirement subsets"},{"category":"section","location":"theory/diagnostics/#Reachability-is-stable;-redundancy-is-the-delicate-half","page":"Diagnosing unsatisfiability","text":"note: Lemma R (reachability is monotone in seeds and conflicts)\nLet D and D have the same packages, versions and dependency sets, with conflict relation otimes subseteq otimes and requirement seeds mathrmreqs subseteq mathrmreqs. Then r(p) le r(p) for every package p, where r is the reach prefix map and saturation counts as +infty.Proof. The reach set is the least fixpoint of the derivation system of Filtering: seed requirements' first versions; first versions of kept dependencies; conflict-driven degradation of both sides of a conflict between reachable versions; and the saturation rule pushing dependents of a saturated package. Every rule instance of the primed system is a rule instance of the unprimed one — the seeds are a subset, the conflict premises are a subset, and the dependency premises are identical. So every primed derivation replays verbatim, and the primed least fixpoint is contained in the unprimed one. ∎\n\nThe exclusion rule for user constraints (\"a reachable excluded version can never be a resting point\") is the ordinary conflict rule applied to the virtual package's always-present version, so the same argument covers it. Lemma R asks nothing of the shape of the removed conflicts: it holds for arbitrary group removal.\n\nRedundancy elimination does not. It deletes qj when some better kept qi has a subset of its constraints, and a relaxation that clears a constraint of qi without clearing the corresponding constraint of qj destroys the inclusion. Whether that can happen depends entirely on the shape of the group:\n\nnote: Definition (column-closed group)\nRedundancy compares two versions of a package column by column of that package's constraint matrix: one column per dependency, one per (textpartner package textpartner version) pair, and one for the user-constraint virtual package. A group g is column-closed if switching it off clears, in every package's matrix, a set of whole columns — never part of one.\n\nTwo natural granularities are column-closed, and they are the coarsest sensible ones:\n\nPer package, for user constraints. All of a package's user constraints together — its compat bound and its pin — are exactly its exclusion column.\nPer interacting pair, for third-party bounds. All conflicts between packages p and q are exactly q's whole column block in p's matrix and p's whole block in q's.\n\nnote: Theorem D1 (relaxation stability)\nSuppose every group in G is column-closed. Then for every relaxation sigma,mathrmLay(F(D) sigma) = mathrmLay(D sigma)both sides being undefined together. In particular F(D)sigma is satisfiable iff Dsigma is.Proof. If Dsigma is unsatisfiable, so is F(D)sigma: its models are a subset. So assume Dsigma is satisfiable. We show each pass of preparation — each of which deletes versions from D, and hence induces the same deletion on Dsigma — keeps every version of the current sigma-answer, and conclude with Theorem 4 applied to Dsigma after each pass.Arc consistency. The pass deactivates a version exactly when one of its dependencies has no version at all, iterated to a fixpoint. The criterion mentions no conflicts and no requirements, so it is sigma-independent, and such a version appears in no model of Dsigma either — dependency edges must be satisfied whatever the conflict relation.The collapse. The pass keeps, of each interchangeability class, the member the active order ranks best — where the classes are the registry's row-equality classes (version_classes) refined by the exclusion mask, so that a class splits into its forbidden and its admissible members and each half keeps a representative (class_representatives).Two things have to hold. First, the deletion is answer-preserving on the unrelaxed problem: class members have identical constraint rows, hence dominate each other, so Theorem 6 applies — this is the interchangeability lemma, and it is why the pass is legitimate at all.Second, it survives sigma. Let L be a class of Dsigma's partition — the partition of the relaxed problem, refined by sigma's exclusion mask. Two claims:L is a union of classes of the unrelaxed refined partition. The registry-level classes coarsen under sigma (removing conflict columns can only merge row-equality classes, never split them), and the exclusion refinement coarsens too, because sigma clears the mask for whole packages at a time — the mask is one column, and column-closure is exactly the hypothesis that sigma takes all of it or none of it.\nHence the best member of L in the active order is the best member of one of those finer classes, and is therefore kept.So every sigma-class retains its best member, and the members that are gone are dominated by it under sigma (they are in the same sigma-class, so their rows agree there too). Theorem 6 applied to Dsigma says the sigma-layered trace never chooses them.Note where column-closure did the work: were sigma allowed to clear part of a package's exclusion column, the refinement could split a sigma-class between a forbidden and an admissible half whose representative is the wrong one, and the best sigma-admissible member could be gone. That is the collapse's version of Proposition D1′ below, and it has the same cause.Reachability. Let r be the prefix map the pass computes with the full seed set and every group active, and r_sigma the map it would compute on Dsigma. By Lemma R, r_sigma le r. By Theorem 5 applied to Dsigma, every version of the sigma-answer lies within r_sigma, hence within r.Redundancy elimination. The pass deletes qj when some kept qi with i  j satisfies Xic Rightarrow Xjc for every active column c. Because every group is column-closed, passing to sigma clears a set C_sigma of whole columns — the same columns for i and for j. Restricting an implication that holds columnwise to a subset of the columns leaves it holding, so qj is dominated in Dsigma too, and Theorem 6 applied to Dsigma says the sigma-layered trace never chooses it. The test's dropped-partner exemption — conflicts whose partner version reachability already deleted are ignored — is sigma-independent, since which versions were deleted is decided by the full-constraint filter and not by sigma; it drops the same columns on both sides.Iteration. Each pass preserves the sigma-answer of the problem it runs on, so Theorem 4 applies after each and the answers chain — through the collapse as much as through the filter proper, the collapse being handed to the filter as marks and materialized by its first deletion round rather than as a separate rebuild — exactly as they do in the unrelaxed composition at the end of Theorem 6. ∎","title":"Reachability is stable; redundancy is the delicate half"},{"category":"section","location":"theory/diagnostics/#Below-column-closure-it-is-false","page":"Diagnosing unsatisfiability","text":"It is tempting to relax at the granularity of the sources a report would like to name: this compat bound as against this pin, this one compat declaration as against another. Neither is column-closed — a constraint source clears part of a package's exclusion column, and a single compat declaration clears part of a partner's block — and neither is safe.\n\nwarning: Proposition D1′ (finer relaxations are not stable)\nRelaxing user constraints per source, or third-party bounds per declaration, does not preserve satisfiability verdicts. One package with two versions suffices for the first.Counterexample (source granularity). A single package p with two versions and no dependencies. The user's compat bound on p admits only p2; the user's pin holds p at p1. Requirement p.Together the two sources forbid everything, so p's exclusion column is full: p1 and p2 have identical constraint rows, p1 dominates, and redundancy deletes p2. The filtered instance carries only p1.Now take sigma: drop the pin, keep the compat. The raw problem is satisfiable, at p2. The filtered instance no longer has p2 and reports unsatisfiable. ∎Counterexample (declaration granularity). Packages p and q, two versions each, p2 depending on q; q1 declares compat on p admitting only p1, and q2 declares compat on p admitting nothing. Those are two declarations, hence two groups. Dropping only q2's leaves q1's in force, clearing part of p's column block in q's matrix and not the rest — and the same collapse follows.The general shape is what the proof needs: sigma cleared a cell of the dominating version without clearing the corresponding cell of the dominated one, and the domination evaporated.Neither is a rare corner. Sweeping small random instances over every relaxation:preparation group granularity σ swept wrong\nfull source + declaration 212,664 1.358%\nfull package + declaration 916,020 0.401%\nfull, collapsed package + pair 629,740 0\nfull, uncollapsed package + pair 629,740 0\nfull, collapsed, reordered package + pair 629,740 0\nreach only source + declaration 3,177,516 0The operative rows are the middle three and the last: column-closure is what makes the difference; the collapse neither helps nor hurts, in the canonical version order or a reversed one; and redundancy elimination is the entire source of the instability.\n\nThe two results localize the damage precisely: by Lemma R, reachability is stable at every granularity, so the entire instability is redundancy elimination — the last row of the table above is the empirical face of Lemma R. A diagnosis that needed source-level relaxations could therefore buy them back, at the cost of a second, redundancy-free filtered universe. The implementation does not: it stays at column-closed granularity and keeps one instance.","title":"Below column-closure it is false"},{"category":"section","location":"theory/diagnostics/#What-transfers,-and-at-what-granularity","page":"Diagnosing unsatisfiability","text":"note: Corollary D2 (cores, corrections and enumeration transfer)\nLet Sigma be the lattice of relaxations over a column-closed family G, and mathrmsat  Sigma to 01 the satisfiability verdict. Theorem D1 says the verdict functions of D and F(D) are the same function on Sigma. Therefore, computed on F(D): a minimal unsatisfiable relaxation is one of D; a minimal correction set is one of D; and an enumeration of either is complete for D exactly when it is complete for F(D).Proof. All three notions are defined purely in terms of mathrmsat on Sigma, and the two verdict functions coincide. ∎\n\nBecause Theorem D1 preserves the layered answer and not merely the verdict, the solution reported alongside a fix — obtained by re-resolving the filtered instance with the fix's groups switched off — is the answer the raw problem would give under that fix, not an artifact of filtering.\n\nThe price of column-closure is paid in the report's vocabulary, and it is small:\n\nA fix relaxes all of a package's constraints at once — compat bound, pin and admission knobs together. For the overwhelming majority of packages, which carry one source, that is the same thing as naming it. When a package carries several, the fix names each of them that still forbids a version the instance has; the ones that forbid nothing there are exactly the ones the relaxation does not depend on, so leaving them unnamed keeps the advice both minimal and honest.\nA bound-level conflict is blamed on an incompatible pair of packages rather than on one compat declaration, and an upstream suggestion names both sides. That was going to be true anyway: package info records compatibility symmetrically, so which side declared a bound is not recoverable from the filtered instance (see the boundary note below).\n\nwarning: Relaxations only\nEvery diagnostic query must be a relaxation over a column-closed family. Assuming a subset of the requirements and a subset of the selector variables is one; adding a clause is not.This is why fix enumeration is done MARCO-style with assumption-subset queries — force-keeping a previously dropped group and re-running the greedy pass — rather than by adding a blocking clause bigvee_g in mathrmMCS s_g over the selectors after each fix. A blocking clause is an added constraint; the resulting instance is outside the sigma-family and Theorem D1 licenses nothing about it.","title":"What transfers, and at what granularity"},{"category":"section","location":"theory/diagnostics/#Closure-exactness","page":"Diagnosing unsatisfiability","text":"Bound-level questions are asked per cluster, on a sub-instance covering only the packages that cluster can reach. That restriction is exact.\n\nnote: Lemma D3 (closure exactness)\nFix requirements R_0 and let C be the least set containing R_0 and closed underp in C Longrightarrow bigcup_v mathrmdeps(p v) subseteq C— the union over all of p's versions, not just a chosen one. Let DC be D restricted to C: packages outside C deleted, dependencies and conflicts among C unchanged. Then D has a solution covering R_0 iff DC does, and the tight solutions covering R_0 are literally the same set.Proof. (⇒) Let s be a solution of D covering R_0 and pi(s) its pruning, a tight solution with support C(s) (pruning lemma). Then C(s) subseteq C: R_0 subseteq C, and if p in C(s) then mathrmdeps(p s(p)) subseteq bigcup_v mathrmdeps(p v) subseteq C. So pi(s) lives inside C and is a solution of DC — coverage, closure and conflict-freedom all inherited.(⇐) Let t be a solution of DC covering R_0. Read as a partial assignment on D that assigns nothing outside C, it is still a solution: coverage is unchanged; dependency closure holds because every dependency of an assigned version lies in C and is assigned by t; conflict freedom is inherited, since t assigns no version outside C to conflict with. ∎At the level of the SAT encoding the extension is even more transparent: every clause an outside package occurs in is guarded by one of its own literals — dependency clauses lnot pi lor q, conflict clauses lnot pi lor lnot qj — so setting the whole package false satisfies them vacuously. Outside packages are never forced, because only dependency edges force packages and every edge out of C stays inside C.\n\nComposing with Theorem D1: a per-cluster sub-instance built from the filtered universe F(D), restricted to the closure of the cluster's requirements, and equipped with selectors for the column-closed groups it contains, answers every relaxation query for that cluster exactly as the raw problem would.\n\nOne honest caveat: C unions over all versions' dependency sets, so it is generally larger than the support of any single solution — and for a cluster rooted at a hub package it can be a substantial fraction of the universe. Exactness is not the same as smallness.","title":"Closure exactness"},{"category":"section","location":"theory/diagnostics/#Goal-safety:-which-universe-answers-which-question","page":"Diagnosing unsatisfiability","text":"Everything above licenses relaxation queries on the prepared universe F(D) — and every one of those queries asks about optimal solutions, which is what preparation preserves. A goal does not.\n\nA goal query asks whether some solution has a property: that a package is present, absent, or at a named version. It does not ask about optimal solutions, and the two preparation passes that shrink hardest each delete versions on the grounds that no optimal solution uses them.\n\nwarning: Reachability and redundancy are not goal-safe\nTake A to B, B31 to X, B23 to varnothing, with no conflicts anywhere and R_0 = A. Reach seeds B31 and nothing degrades it, so F(D) has only B31 and every solution of F(D) contains X. In D the solution A1 B23 avoids it. So on F(D) the goal lnot X is unsatisfiable and on D it is satisfiable: not a worse answer, a wrong one.Redundancy fails the same way in the other direction. If A2 (better) depends on nothing and A1 depends on X, then A2 dominates A1 and elimination deletes it — after which the goal X present is unsatisfiable, though A1 X witnesses it in D.Both are Lemma R and the redundancy theorem behaving exactly as stated. Neither ever claimed to preserve non-optimal solutions, and a goal is a question about the whole solution set.\n\nWhat is goal-safe is the T1 half of preparation, and it is safe for a reason, not by luck:\n\nnote: Proposition G (T1 preserves every goal's witnesses)\nLet T_1(D) be D with (i) arc-consistency applied, and (ii) each package's versions collapsed to one representative per interchangeability class, plus any versions named by value. Then a solution of D witnessing a presence or absence goal exists iff one of T_1(D) does, and a solution assigning a named version v exists iff one of T_1(D) does.Proof. (i) Arc consistency deletes pv only when pv occurs in no solution at all — it depends, transitively, on a package with no installable version. A version in no solution witnesses no goal, and deleting it removes no solution.(ii) Two versions are in one interchangeability class when their rows in the conflict matrix are equal, and the matrix's columns include the dependency columns. So class members have identical dependency sets: swapping pv for its representative pv in a solution changes nothing about which packages the solution contains, and preserves conflict-freedom by row equality (Theorem 3's swap argument). A solution containing (or avoiding) X therefore maps to one containing (or avoiding) X. A package that avoids X can never hide inside a class whose representative needs it, because that would be two rows differing in a dependency column.For a goal naming a version by value the swap argument is exactly what does not apply — the sibling is indistinguishable to every constraint but it is not the version asked for — so those versions are excluded from the collapse and survive as representatives of their own. ∎\n\nComposing Proposition G with Lemma D3: a goal query runs on T_1(D) restricted to the closure of R_0 together with the goal's own packages. That is prepare_goal_info plus the closure the T1 artifact is already built over. The sub-instance is bigger than the per-resolve one — the two hardest passes are off — which is the price of asking a different question.\n\nTwo consequences worth stating plainly:\n\nBetter-version goals are the exception, and they are why the holdback probe needs none of this. Reach keeps version prefixes: if the resolve landed on pi, every version better than pi is present in F(D) by construction. \"Is a better p possible\" is therefore answerable on the instance the resolve already has in hand, which is what makes the automatic julia check nearly free.\nThe bound-level sub-instances follow the same rule. With a goal in force, bound_story's per-cluster instance keeps the filter only when the goal is a pure presence ask — whose packages it has already made requirements of, so reach keeps their prefix. Anything else turns the two passes off there too, and the closure budget may then decline the sub-instance; the report stops at requirement level rather than answering from the wrong universe.","title":"Goal safety: which universe answers which question"},{"category":"section","location":"theory/diagnostics/#What-the-implementation-does-with-this","page":"Diagnosing unsatisfiability","text":"The production instance asserts the user-constraint selectors as unit clauses inside a single sat_push frame. One sat_pop turns every user constraint into an assumable selector, converting the failed instance into the diagnostic instance in place — no rebuild, no second copy of the universe.\nDiagnosis relaxes user constraints per package, never per source, and third-party bounds per interacting pair, never per declaration. That is Theorem D1's hypothesis, and Proposition D1′ is what happens if it is ignored. \"Per package\" covers every source at once — the compat bound, the pin, and every admission knob (:prerelease, :yanked, …) that touches the package — because they all write into the one exclusion column.\nClustering and fix enumeration run on the converted instance with assumption-only queries.\nEach fix's reported solution comes from re-resolving the same instance with the fix's groups switched off inside a temporary frame; the answer half of Theorem D1 makes those versions the real ones.\n\nwarning: Formalization boundary\nTheorem D1 and Lemma R are proved against the same hand-made rule set as Theorems 2–6 on the previous page — a faithful reading of find_reachable and mark_necessary!, checked against the code by review and by the test suite rather than mechanically. Lemma D3 is proved against the abstract setting, and its SAT-level restatement against the encoding described there.Everything on this page has a direct empirical check, in the same spirit as the brute-force reference-resolve tests that back Theorems 4–6. The suite sweeps small random instances and, for every relaxation sigma — every subset of the requirements crossed with every subset of the constraint groups, not a sample — asserts that resolving the filtered instance under sigma gives exactly what a brute-force reference resolve of the raw data with those groups deleted gives, verdict and answer alike. It asserts stability at package-and-pair granularity, reproduces Proposition D1′'s counterexample at source granularity — with the collapse on and off, and in a non-canonical version order — and confirms Lemma R by sweeping a reach-only universe at the finer granularity where full preparation fails. A separate testset pins the property the collapse case of the proof turns on: that refining classes by the exclusion mask leaves a forbidden version in the instance for every kind of source, so switching that source off can bring it back.One representational caveat, orthogonal to the theorems: package info records compatibility symmetrically, so by the time a problem reaches the filtered instance the resolver no longer knows which side of an incompatibility declared it. Bound facts in a diagnosis therefore name an incompatible pair of version groups rather than a directed \"X requires Y at …\" declaration, and upstream suggestions name both sides.","title":"What the implementation does with this"},{"category":"section","location":"diagnostics/#Diagnostics","page":"Diagnostics","text":"A resolve that fails does not just say so. resolve returns the Diagnosis itself, and printing it gives a report:\n\njulia> resolve(data, [\"A\", \"B\"])\nUnsatisfiable — 1 conflict, 2 fixes:\nConflict 1: A, B cannot be satisfied together.\n  • you require A\n  • you require B\n  • A (all versions) works with C only at 1\n  • B (all versions) works with C only at 2\n\nVerified fixes:\n  1. drop requirement B\n     → allows: A 1, C 1\n  2. drop requirement A\n     → allows: B 1, C 2\n\nUpstream fixes:\n  • a release of A or C relaxing their compat on each other\n    → allows: A 1, B 1, C 2\n  • a release of B or C relaxing their compat on each other\n    → allows: A 1, B 1, C 1\n\nThree sections, in decreasing order of how much you can do about them.\n\nConflicts are independent: each is a minimal set of requirements that cannot be satisfied together, and fixing one does nothing for the others. The bullets tell that conflict's story, ordered from the requirements outward along the dependency graph.\n\nVerified fixes are global — each one repairs every conflict at once — and minimal: none of them relaxes a strict superset of what another does. Each was checked by re-resolving, and the versions after → allows: are what you would actually get, restricted to the packages the conflicts implicate. (The complete assignment is on the Fix, for programmatic use.)\n\nUpstream fixes are the ones you cannot make yourself: a single new release loosening one incompatibility would resolve that conflict. A tangled conflict can produce a dozen of these, so they are ranked — by how current the blamed versions are, then by how good the result is — and at most three are printed, with a count of the rest. The structured Diagnosis keeps them all.\n\nA story line may also be followed by an indented (likewise for …). Those are the facts a minimal conflict needs in order to close off versions your own constraints already exclude: real, but not what you are reading for.","title":"Diagnostics"},{"category":"section","location":"diagnostics/#The-API","page":"Diagnostics","text":"sol = resolve(data, reqs; compat, pins)\nif sol isa Diagnosis\n    show(stdout, MIME(\"text/plain\"), sol)  # the report above\n    for fix in sol.fixes\n        # fix.actions  :: Vector{Fact}\n        # fix.solution :: Dict{P,V}\n    end\nend\n\nEverything is plain data, so a caller — Pkg, say — can render its own report instead. The pieces:\n\ntype what it is\nDiagnosis requirements, conflicts, fixes, and the version lists to render against\nResolver.Conflict one independent conflict: its requirement cluster, its fact chain, its upstream fixes\nResolver.Fix a set of Facts to relax, and the resolution that follows\nResolver.UpstreamFix an incompatibility a new release could dissolve, and what would follow\n\nand the Fact kinds a chain or a fix is built from: Resolver.Requirement, Resolver.Uninstallable, Resolver.Pin, Resolver.UserCompat, Resolver.Admission, Resolver.Bound.\n\nAn Resolver.Admission is an admission knob — the excludes of a Problem, which is where \"no prereleases\" lives. Those are constraints like any other, so they get facts and fixes like any other:\n\nConflict 1: Wine_jll cannot be satisfied.\n  • you require Wine_jll\n  • every version of Wine_jll is a prerelease, which is not allowed\n\nVerified fixes:\n  1. allow prereleases of Wine_jll\n     → allows: julia 1.11.9, Wine_jll 7.0.0-rc3+1\n  2. drop requirement Wine_jll\n     → allows: julia 1.11.9\n\nYankedness is deliberately not one of them: see below.","title":"The API"},{"category":"section","location":"diagnostics/#Asking-for-something:-goals","page":"Diagnostics","text":"resolve takes two more keywords, and they change the question rather than the problem:\n\nresolve(info, prob; with = \"AnthropicClient\")          # can I have this?\nresolve(info, prob; with = \"DataFrames\" => v\"1.8.2\")   # ... at this version?\nresolve(info, prob; without = \"FillArrays\")            # can I avoid this?\nresolve(info, prob; with = [\"CUDA\", \"MKL\"], without = \"FillArrays\")\n\nThe return contract does not change: the optimal solution satisfying the problem and the goal, or a Diagnosis of what it would take.\n\nresolve(info, prob; with = \"AnthropicClient\")     # project already has BulkSMS\n\nUnsatisfiable — 1 conflict, 1 fix:\nConflict 1: the goal and BulkSMS cannot be satisfied together.\n  • you require BulkSMS\n  • AnthropicClient (all versions) works with HTTP only at 1.0.1–1.11.0\n  • BulkSMS (all versions) works with HTTP only at 0.8.19\nVerified fixes:\n  1. drop requirement BulkSMS\n     → allows: AnthropicClient 0.1.0, HTTP 1.11.0\n\nThe whole difference between with = \"AnthropicClient\" and adding it to prob.reqs is one line of that report: the problem's constraints are negotiable, and a fix may propose relaxing any of them; the goal is the fixed ask, so \"drop requirement AnthropicClient\" can never be offered. The header says which conflicts the goal is part of.\n\nA without goal's story is a chain of forced dependencies rather than of incompatible pairs — nothing conflicts with anything, the package is simply unavoidable:\n\nConflict 1: the goal and Makie cannot be satisfied together.\n  • you require Makie\n  • Makie (all versions) requires GeometryBasics\n  • GeometryBasics (all admissible versions) requires FillArrays\n\nOnly forced edges appear: a path some other version choice could route around explains nothing, which is what separates this from pkg> why FillArrays printing every path there is. And when the package is avoidable, there is no conflict at all — resolve returns the optimal FillArrays-free solution, and changes against the plain resolve is how you price it.","title":"Asking for something: goals"},{"category":"section","location":"diagnostics/#Three-tiers-of-effort","page":"Diagnostics","text":"Computing a diagnosis costs several extra solves, and so does the optimizing descent. Three calls answer the same question with more or less work:\n\ncall cost answer\nissatisfiable(data, prob; with) one solve Bool\nresolve(data, prob; with, diagnose = false) one solve + descent the solution, or nothing\nresolve(data, prob; with) + explanation the solution, or a Diagnosis\n\nNothing but the effort separates them; the verdicts agree. A UI greying out an upgrade button wants the first, a caller that needs the versions the second, and only the one that will actually print a report pays for the third.","title":"Three tiers of effort"},{"category":"section","location":"diagnostics/#Which-universe-answers-a-goal","page":"Diagnostics","text":"A goal query runs on a sub-instance built from the T1 artifact, not on the per-resolve one, because the per-resolve filter prunes exactly the escape routes a goal needs. A→B, B@3.1→X, B@2.3→∅, no conflicts: reachability keeps only B@3.1, so the prepared instance calls X unavoidable while the raw problem avoids it via B@2.3. The theory page proves what survives — arc consistency and the interchangeability collapse are goal-safe, reachability and redundancy are not — and the counterexample above is a test.","title":"Which universe answers a goal"},{"category":"section","location":"diagnostics/#Rendering-your-own-reports","page":"Diagnostics","text":"The Diagnosis retains everything. Every opinionated choice in the output above is a documented function, and the default show is merely its first client — no policy parameter was passed into the resolver to produce it, which is the point of the design. Three tiers of control:\n\nTier 1 — the default. show(io, MIME(\"text/plain\"), d). Zero configuration; what bin/resolve.jl prints.\n\nTier 2 — the knobs. report is what show calls, with its choices as arguments:\n\nreport(io, d;\n       max_upstream = 3,          # cap + \"(N more …)\"; typemax(Int) for all\n       demote_incidental = true,  # fold the \"likewise …\" facts into an aside\n       trim_witnesses = true,     # \"→ allows:\" lists the contested packages\n       labels = Dict(\"DataFrames\" => :requested))   # source wording\n\nTier 3 — your own layout, our sentences (or neither). The helpers show is built from are public:\n\nresult = resolve(info, prob)\nif result isa Diagnosis\n    for c in result.conflicts\n        print_header(c.reqs, c.goal)\n        for f in c.chain\n            f isa Resolver.Bound && f.incidental && continue  # drop, not demote\n            println(io, \"  • \", sprint(Resolver.render_fact, f, result))\n        end\n    end\n    fixes = result.fixes                       # already verified, already optimal\n    ups = filter(u -> maintained(u.bound.pkg), result.upstream)  # your knowledge\n    Resolver.rank_upstream!(ups)\nend\n\nrender_fact writes one bullet's sentence; render_action writes a fix's imperative (\"relax your compat on B\") and blame_phrase the same fact as a noun (\"your compat on B\"). A client that wants its own sentences reads the Fact fields instead — they are everything the sentences are generated from.\nrank_upstream! sorts suggestions by how current the blamed versions are: a new release relaxing its latest bound is plausible, resurrecting an ancient range is not. Every UpstreamFix carries its bound and a verified solution, so a client with better knowledge — Pkg knows which packages are maintained — re-sorts or filters by its own criteria and ignores ours.\nBound.incidental is set during MUS construction when every version on the bound's own side is already excluded by the user's constraints: the fact's only job is closing off versions they cannot have anyway. Minimality is preserved by the remaining facts for the versions that matter, so a renderer may drop them outright.\nsuperseded is the prerelease-supersession predicate fix enumeration already consults; it is public so a client filtering its own suggestion lists applies the same policy.\nchanges diffs two solutions into Change(pkg, from, to), which is how a feasible goal query gets its price tag.","title":"Rendering your own reports"},{"category":"section","location":"diagnostics/#Modelling-platform-packages","page":"Diagnostics","text":"Some packages are not the user's choice: julia is present in every resolution whatever anyone wants, and \"drop requirement julia\" is not advice. That is a modelling question, not a policy knob — model such a package as a dependency edge, never as a requirement. bin/resolve.jl does: the registry provider gives every package version a dependency on julia, so julia is in the solution because things need it, and cannot be dropped because nothing \"requires\" it in the sense a fix could undo. Witness lists stay quiet about it for the same reason: a report names the requirements and the packages some story blames, and a dependency edge is neither.\n\nThe one knob left is wording. Problem(…; labels) gives a compat entry a source flavour, so a bound from Pkg.add(name, version) reads \"you requested DataFrames at 1.7\" and \"relax the version you requested for DataFrames\" instead of calling it project compat. Unknown labels render neutrally.\n\nOne suggestion is filtered without being asked for: admitting prereleases of a package is only proposed when the prerelease you would end up with has not been superseded by a release of the same base version — nobody should be told to install 1.2.3-alpha1 when 1.2.3 is out.","title":"Modelling platform packages"},{"category":"section","location":"diagnostics/#Modelling-withdrawn-versions","page":"Diagnostics","text":"A yanked version is the other thing no report should ever propose — and the way to guarantee that is not a policy flag either. Model a withdrawn version by leaving it out of the universe: the provider filters yanked versions before the resolver sees them, so no solution contains one and no fix can name one, by construction rather than by rule. bin/resolve.jl does exactly this, with --allow-yanked[=<pkgs>] rebuilding the universe with them back in for anyone who wants them.\n\nWhat pre-filtering costs is a story, and that is recovered at the rendering layer rather than by putting the constraint back. A bound admitting only yanked versions admits nothing at all, and \"no versions\" is not the answer anyone wants, so the provider keeps the set it dropped as explanation data:\n\nConflict 1: libsodium_jll cannot be satisfied.\n  • you require libsodium_jll\n  • your compat restricts libsodium_jll to no versions\n\nVerified fixes:\n  1. relax your compat on libsodium_jll\n     → allows: julia 1.11.9, libsodium_jll 1.0.21+0\nNote: the versions your compat on libsodium_jll admits are yanked (1.0.22+0);\n--allow-yanked accepts them anyway.","title":"Modelling withdrawn versions"},{"category":"section","location":"diagnostics/#Two-things-the-report-will-not-do","page":"Diagnostics","text":"It relaxes a package's constraints together, not one at a time. A compat bound, a pin and an admission knob on the same package are one relaxable unit. That is not conservatism: the three share one row in the resolver's bookkeeping, and relaxing part of it is exactly the counterexample of Proposition D1′ — the answer would be wrong. Packages with a single constraint source, which is almost all of them, are unaffected.\n\nA fix does still name only the sources that rule out a version the instance still has; one that forbids nothing there had no part in the relaxation, so asking you to change it would be noise. That is why the Wine_jll example above names nothing besides the prerelease knob.\n\nIt blames incompatible pairs, not compat declarations. A report says \"A (all versions) works with C only at 1\", not \"A declares compat C = 1\", and an upstream suggestion names both sides. Two reasons, and either alone would be decisive: package info records compatibility symmetrically, so by the time a problem reaches the resolver's instance which side declared a bound is no longer recoverable; and per-declaration relaxation is not column-closed either.","title":"Two things the report will not do"},{"category":"section","location":"diagnostics/#When-the-resolve-*succeeds*-and-still-surprises","page":"Diagnostics","text":"A resolution can be correct and still not be what you expected. A user compat bound on a non-upgradable stdlib steers the julia choice — a julia release is compatible with exactly the stdlib versions it bundles — so a leftover LinearAlgebra = \"~1.10\" quietly pins the whole toolchain to julia 1.10, and nothing in a successful resolve says so.\n\nholdbacks explains that:\n\ninfo = prepare_pkg_info(pkg_info(data, prob), prob)\nsol  = resolve_prepared(info, prob)\nfor h in holdbacks(info, prob, sol, [\"julia\"])\n    show(stdout, MIME(\"text/plain\"), h)\nend\n\njulia resolved to 1.10.11; 1.12.6 is available.\n  • you require LinearAlgebra\n  • your compat restricts LinearAlgebra to 1.10.0–1.10.11\n  • julia 1.12.6 works with LinearAlgebra only at 1.12.0\n  ⇒ held back by your compat on LinearAlgebra.\n  relax your compat on LinearAlgebra → allows: julia 1.12.6, LinearAlgebra 1.12.0\n\nIt is the satisfiable-side sibling of everything above, and reuses it: assume the better version on the instance, get an unsatisfiable answer, and the same biased group-MUS names the responsible constraints. The fix is verified by the same optimizing descent, so its versions are the ones you would really get — Resolver.summarize gives the same thing as one line.\n\nThree things it will not do. It does not report a package whose version is merely what the priority order settled on — if the better version was feasible all along there is no constraint to blame, and inventing one would be worse than silence. It measures \"best\" against the versions the problem admits, not against the raw universe: a prerelease this query would never accept is not an option being missed, and advertising it would bury the bound that is the real story. And when nothing the user set is to blame — the version is held back by a bound only an upstream release could move — it says nothing you can change would move it rather than manufacturing a fix.\n\nFrom the command line, bin/resolve.jl explains julia automatically whenever it lands below its best admissible version — that is the surprise nobody went looking for — as a single note:\n\nNote: julia is held back to 1.10.11 by your compat on LinearAlgebra; relaxing it allows julia 1.12.6.\n\n--explain gives the reasoning for everything that landed below its best version. Explaining one costs several solves, so max_probes bounds how many are probed; the result carries how many candidates that left over and the report ends with (N more packages resolved below their best version, not examined) rather than quietly showing a partial answer.\n\n--explain=<pkg> answers whichever question is not already answered. If the package is below its best version, that is the holdback above. If it is not — because it is at its best, or because it is not in the solution at all — the only question left is whether you can have it, which is the goal with = pkg, and the answer is a price:\n\n$ resolve.jl --explain=Statistics\nYou can have Statistics, at this price:\n  • Statistics 1.11.1 added\n\n--explain=<pkg>@<version> asks the version goal instead, and an impossible one comes back as a full diagnosis rather than a bare refusal.","title":"When the resolve succeeds and still surprises"},{"category":"section","location":"diagnostics/#Why-this-is-trustworthy","page":"Diagnostics","text":"The diagnosis runs on the very SAT instance the resolve failed on — the prepared one, collapsed to interchangeability-class representatives and then filtered, with the user's constraints popped back into assumptions. That instance was built for a different question, so the theory page proves it answers this family of questions correctly: satisfiability verdicts and layered answers are preserved under every relaxation of the kind a diagnosis performs. The verified solutions attached to fixes are a consequence of the stronger, answer-preserving half.\n\nThe collapse is part of what has to be justified, not a complication to be waved away: it deletes versions, and diagnosis then asks questions in which some of the constraints that motivated those deletions are switched off. Refining the classes by the exclusion mask before choosing representatives is what keeps a forbidden version in the instance, so that relaxing its constraint can bring it back.\n\nBound-level detail needs to relax conflicts the production instance keeps as hard clauses, so it runs on a small sub-instance restricted to the conflict's dependency closure. That restriction is exact, not an approximation (Lemma D3). It costs a solve per incompatibility, so very large closures are skipped rather than paid for: the report then stops at requirement level, which for a hub package's conflict is usually the whole story anyway.","title":"Why this is trustworthy"},{"category":"section","location":"diagnostics/#From-the-command-line","page":"Diagnostics","text":"bin/resolve.jl diagnoses automatically. An unresolvable project prints the report to stderr, with uuids replaced by package names, and exits nonzero.","title":"From the command line"},{"category":"section","location":"theory/setting/#Setting","page":"Setting","text":"This page fixes the objects and notation used throughout the theory section. Everything downstream — the semantics the resolver implements, the correctness proofs for its filtering, and the Pareto-front analysis — is stated in these terms.","title":"Setting"},{"category":"section","location":"theory/setting/#The-resolution-problem","page":"Setting","text":"A resolution problem consists of:\n\na finite set of packages mathcalP;\nfor each package p a finite, totally ordered list of versions, written by rank: rank 1 is the best version, rank 2 the next, and so on (the meaning of \"best\" is supplied by the caller — newest, oldest, closest-to-installed — the theory only uses the order);\nfor each version pv a dependency set mathrmdeps(p v) subseteq mathcalP — note that dependencies are attached to versions, and different versions of the same package may have different dependency sets, a fact that turns out to carry a lot of weight;\na symmetric conflict relation on version pairs (in the package data this arises from compatibility constraints: pv conflicts with qw when w is outside pv's compatibility bound for q, or vice versa);\na set of requirements mathrmreqs subseteq mathcalP.\n\nA solution is a partial assignment s of versions to a support set mathrmsupp(s) subseteq mathcalP such that:\n\ncoverage: mathrmreqs subseteq mathrmsupp(s);\ndependency closure: for every p in mathrmsupp(s), mathrmdeps(p s(p)) subseteq mathrmsupp(s);\nconflict freedom: no two assigned versions conflict.\n\nWrite mathrmrank_s(p) for the rank of s(p).","title":"The resolution problem"},{"category":"section","location":"theory/setting/#Tightness-and-pruning","page":"Setting","text":"The closure C(s) of a solution is the least set containing mathrmreqs and closed under following dependencies of s's chosen versions:\n\nC(s) = bigcup_k L_k qquad\nL_0 = mathrmreqs quad\nL_k+1 = L_k cup bigcup_q in L_k mathrmdeps(q s(q))\n\nA solution is tight if mathrmsupp(s) = C(s) — it contains nothing that isn't reachable from the requirements along its own chosen dependency edges. R^* denotes the set of tight solutions. Pruning pi(s) restricts s to C(s).\n\nnote: Lemma (pruning)\npi(s) is a tight solution with s's versions, and C(pi(s)) = C(s).Proof. Coverage and dependency closure hold because the closure construction adds exactly the dependencies of its members; conflict freedom is inherited from s; and the closure computation only ever inspects versions of packages already in the closure, which agree with s. ∎\n\nTwo structural facts about tight solutions recur constantly:\n\nSupport is a function of the versions. For a tight solution, the support is determined by the version assignment (it is the closure), so two distinct tight solutions with nested supports must already differ on a version within the smaller support.\nJunk. A valid solution need not be tight — it may carry packages (\"junk\") not justified from the requirements. Junk matters because the resolver works with a SAT solver, and SAT models are exactly the valid solutions, junk included.","title":"Tightness and pruning"},{"category":"section","location":"theory/setting/#The-SAT-encoding","page":"Setting","text":"The resolver encodes a problem instance with one boolean variable per package and one per version:\n\npackage implies some version: p Rightarrow bigvee_i pi;\nversion implies its package: pi Rightarrow p;\nversions are mutually exclusive: lnot pi lor lnot pj;\ndependencies: pi Rightarrow q for each q in mathrmdeps(pi);\nconflicts: lnot pi lor lnot qj for conflicting pairs.\n\nWith requirement unit clauses added, the models of this formula are precisely the valid solutions covering the requirements (with junk allowed). All constraints are dependency edges (positive, from versions to packages) and pairwise version conflicts (negative) — a structural fact several proofs exploit: any infeasibility ultimately traces to pairwise conflicts, because dependencies alone can always be satisfied by including more packages.\n\nBefore solving, the problem is shrunk by filtering — reachability analysis and redundancy elimination — whose correctness is the subject of the next page.","title":"The SAT encoding"},{"category":"section","location":"#Resolver.jl","page":"Home","text":"A package version resolver built on a real SAT solver (PicoSAT). Given package data — versions, dependencies, and compatibility constraints — and a set of required packages, resolve returns a single optimal solution mapping each needed package to a version.\n\nWhen the requirements cannot be satisfied it returns a Diagnosis instead: the independent conflicts, the facts behind each of them, and a menu of verified fixes. See the diagnostics guide.\n\nWhat \"optimal\" means, precisely, and why the resolver's aggressive problem filtering provably does not change the answer, is worked out in the Theory section:\n\nSetting — the resolution problem, solutions, tightness, and the SAT encoding.\nThe layered solution & filtering — the semantics the resolver implements, and proofs that filtering preserves both satisfiability and the answer.\nPareto front optimality — an alternative, purely declarative notion of optimality that was fully worked out and ultimately set aside; its theorems and counterexamples illuminate why the layered semantics and its filter fit together as well as they do.\nDiagnosing unsatisfiability — how far the filtered instance may be re-interrogated once a resolve has failed, and the exact granularity at which that stops being sound.\n\nFor the package's motivation and project status, see the README.","title":"Resolver.jl"},{"category":"section","location":"theory/front/#Pareto-front-optimality","page":"Pareto front optimality","text":"This page documents a fully worked-out alternative semantics for the resolver — necessity on the Pareto front — including its theorems, its pure-SAT implementation, and the reasons it was ultimately set aside. It is a digression from what the package implements, but a productive one: its impossibility results explain why the layered semantics and the filter fit together, its proof techniques power the theorems on the previous page, and it closes the question of a purely declarative optimality criterion rather than leaving it open. A reference implementation lives on the front-necessity branch.","title":"Pareto front optimality"},{"category":"section","location":"theory/front/#Motivation","page":"Pareto front optimality","text":"The layered solution is defined by a procedure. One can ask for something more declarative: a criterion picking out the optimal solution using only the solution set — no layers, no reference to the dependency walk, ideally nothing but satisfiability queries. The natural candidate is a necessity descent: repeatedly pin the highest-priority package that is necessary — present in every remaining solution — at the best version available. Necessity, however, is a universal property of a solution set, so its meaning depends on which set is quantified over:\n\nAll tight solutions (raw necessity): too weak — a package needed by every sensible solution counts as avoidable if one strictly-dominated solution technically avoids it, e.g. by taking a requirement's worst version merely because that version happens to drop a dependency edge. This distorts the descent, and it also forbids problem reduction entirely, which is ruinous at registry scale.\nModels of the filtered problem: fast, but then the answer depends on filtering details — sharpening or dulling the filter changes the result, which is unacceptable as a semantics.\n\nThe canonical middle ground is necessity over the Pareto front: quantify over the solutions that are not strictly worse than others.","title":"Motivation"},{"category":"section","location":"theory/front/#The-dominance-order","page":"Pareto front optimality","text":"For tight solutions s t:\n\nt trianglerighteq s quadiffquad\nmathrmsupp(t) supseteq mathrmsupp(s) quad\nmathrmrank_t(p) le mathrmrank_s(p)  forall p in mathrmsupp(s)\n\nwith strict improvement at some p in mathrmsupp(s) (coverage- monotone version dominance). This is a strict partial order on the finite set R^* of tight solutions, so every solution is either maximal or strictly dominated by a maximal one. The front F is the set of maximal elements.\n\nBoth design choices in trianglerighteq are forced:\n\nOrientation. An order in which leaner solutions dominate cannot work: the solutions that falsely break necessity are support-minimal (a requirement alone at a bad version), and a support-minimal solution has no leaner competitor, so no leaner-dominates order can remove one. Also, since a tight solution's support is a function of its versions, \"leaner at equal quality\" never occurs — leanness always trades against version quality, and the order must price the trade. trianglerighteq prices it as: a bigger solution wins only by covering everything at least as well and strictly better somewhere.\nJunk is excluded by the space, not the order. With the supseteq orientation, unjustified packages would ride along for free, so the candidate space must be the tight solutions. Tightness (well-founded justification, the same notion as stable models in answer-set programming) is not expressible by any dominance relation over versions — it is not monotone in the version lattice. Empirically, defining the front over junk-tolerant solutions changes answers on 92 of 7,464 small instances.\n\nThe front-necessity semantics is the forced descent over F: repeatedly pin the highest-priority package present in every front member consistent with the pins so far, at the best version among them, until no unpinned package is common.","title":"The dominance order"},{"category":"section","location":"theory/front/#Theorem-A:-pin-exactness","page":"Pareto front optimality","text":"The linchpin of everything that follows.\n\nnote: Theorem A\nLet pins be the pin state after any number of descent rounds. If f in F contains every pinned package at rank ≤ its pinned rank, then f agrees exactly with the pins.Proof. Suppose not; among pinned packages where f is strictly better, let hat q be the one pinned earliest, at rank hat r. On packages pinned before hat q, f is ≤ with no strict improvement, hence equal — so f was in the remaining front set when hat q was pinned. But then hat r = minmathrmrank_g(hat q) le mathrmrank_f(hat q)  hat r. ∎\n\nnote: Corollary A1\nIf t in F and t trianglerighteq s (or t = s) for any s agreeing with the pins, then t agrees exactly with the pins: dominating a pin-consistent solution can never buy pin-violating quality.","title":"Theorem A: pin exactness"},{"category":"section","location":"theory/front/#Theorem-B:-the-answer-is-a-front-member","page":"Pareto front optimality","text":"note: Theorem B\nThe descent terminates with the remaining front set equal to exactly mathrmpins; in particular the answer is itself a valid, tight, undominated solution.Proof. Requirements are in every front member, so they get pinned. If q is pinned, every remaining f has q at the pinned version, so mathrmdeps(q mathrmpins(q)) is common and gets pinned — the pins are dependency-closed. Now take any final survivor f and walk its closure: it starts at requirements (pinned), and whenever a closure node u is pinned, f(u) = mathrmpins(u), so the next closure nodes are pinned too. Hence C(f) subseteq mathrmdom(mathrmpins), and by tightness mathrmsupp(f) = C(f) subseteq mathrmdom(mathrmpins) subseteq mathrmsupp(f) — so f = mathrmpins. ∎\n\nThe same argument shape, one level down, shows the layered answer is trianglerighteq-undominated: a dominator would have to agree with every layered pin before its earliest strict improvement, contradicting that pin's optimality. So the layered solution always lies on the front; front-necessity differs only in which front point it selects (230 of 7,464 small instances) — front-necessity makes priority the only ordering principle among necessary packages, where the layered order lets requirements outrank higher-priority dependencies.","title":"Theorem B: the answer is a front member"},{"category":"section","location":"theory/front/#Theorem-C:-a-pure-SAT-implementation-exists","page":"Pareto front optimality","text":"Front-necessity is implementable against a SAT instance that encodes validity only — no tightness encoding, which matters because eager tightness encodings blow up (rooted-chain/level constructions), the naive support encoding admits unrooted cycles, and lazily this is exactly answer-set programming's unfounded-set problem. Instead, two cheap operations run outside the solver: pruning pi (a closure computation) and support comparison. The key sub-results:\n\nWitness transfer. A model lacking p prunes to a tight solution lacking p; conversely every tight solution is a model. So junk never distorts the existential side of a necessity query.\nDominator transfer. A tight solution s has a tight strict dominator iff some model d covers mathrmsupp(s) at ranks ≤ with a strict improvement and passes the prune-check pi(d) supseteq mathrmsupp(s) — junk can fake domination whose tight core doesn't actually cover s, and the prune-check filters exactly those fakes.\nChain-top admissibility. Climbing dominators terminates at a front member, which by Corollary A1 agrees exactly with the pins even though intermediate steps may not.\nCone blocking. When a chain-top t contains p, everything trianglelefteq t is either dominated or is t itself, so one clause excludes the entire cone without losing any witness.\nGreedy construction. In the implementation, candidates are constructed rather than searched for: the layered greedy runs under per-query assumptions, and a junk-free greedy answer is provably Pareto-maximal within its query space (the Theorem A argument again). Restricting all queries to supported models — one clause per package: present implies depended-upon — loses no tight solution and reduces junk to dependency cycles.\n\nWith these, the descent's two oracle types (is p in every pin-exact front member? what is the best rank among them?) are decided exactly, by sequences of ordinary SAT solves. Correctness of the composed pipeline was additionally verified against a brute-force implementation of the definition on roughly 19,500 instance/order pairs with zero deviations.","title":"Theorem C: a pure-SAT implementation exists"},{"category":"section","location":"theory/front/#Theorem-D:-no-version-level-reduction-is-faithful","page":"Pareto front optimality","text":"Here is the decisive negative result. One would like to run the necessity descent on a reduced problem — that is how the layered semantics gets its speed. For front-necessity this fails not just for the actual filter but in principle:\n\nnote: Theorem D\nThere is a problem on which the forced descent over models of the exact front-support reduction — keep a version iff some front member uses it, the strongest version-level reduction that could possibly be faithful — differs from the forced descent over the front itself.Proof. Requirements a, priority p  a  d  b, version 1 best:package dependencies conflicts\na a1 to d, a2 to b \nb b1 to p \nd d1 to p, d2 to  d1 otimes p1\np p1 to d, p2 to  R^* has four members and the front is F = f_1 f_2 with f_1 = a2 b1 p1 d2 and f_2 = a1 d1 p2 — both contain p, so the front descent pins p first at rank 1 and lands on f_1. But every version above appears in some front member, so the exact front-support reduction keeps everything — including the recombinant c = a1 d2 (f_2's a with f_1's d), a tight solution missing p. In the reduced problem p is no longer common; the descent pins a first, and rank-minimization at a eliminates f_1: the reduced answer is f_2 ne f_1. (Verified computationally.) ∎\n\nThe moral: necessity is a property of the solution set, and a version universe only bounds that set from above — recombinations of individually front-supported versions can erase necessity. Universal queries survive only when asked against the front itself. Note the contrast with Theorem 4 of the layered page: the layered semantics needs a reduction to preserve one solution; front-necessity would need it to preserve the entire front and nothing that changes commonality — and even the latter is unattainable at the version level. Such counterexamples require engineered structure (two dependency routes, a recombination orphaning the necessity package, a conflict pinching the diagonal): none arose in 12,000 random 4-package instances, which is also why smaller empirical sweeps had suggested the reduction was safe.","title":"Theorem D: no version-level reduction is faithful"},{"category":"section","location":"theory/front/#Why-the-filter-cannot-serve-the-front","page":"Pareto front optimality","text":"The actual reachability filter fails front-preservation in an even more basic way: it drops front members outright. On the Theorem D instance it keeps only a1 d1 d2 p1 p2 — never reaching a2, deleting b — yet f_1 uses both. The filter's designed invariant is \"a worse version is only chosen under conflict pressure against the better version,\" and a1 is never conflicted. But f_1 takes a2 for a different kind of reason: under a1, the only dependency edge that can justify p is d1 to p, and p1 otimes d1 — so on the a1 route the better p1 is unreachable-as-justified. Switching to a2 reroutes justification through b and frees p1. The downgrade at a is paid for by an upgrade at p — an opportunity-cost trade, mediated not by any conflict against a but by version-varying dependency sets rewiring the justification topology.\n\nThat is precisely the boundary of the filter's intuition:\n\nnote: Fragment lemma\nIf every version of each package has the same dependency set, then on the front every non-best version is blocked by a conflict with a co-installed package — the filter's invariant, as a theorem.Proof (one step). Let s in F with q at a non-best version and v better. The in-place upgrade s = sq mapsto v has the same support, and — dependency sets being version-independent — the same justification edges, hence remains tight. If s were conflict-free it would strictly dominate s. So some conflict between qv and a member of s blocks it. ∎\n\nPointwise (layered) optimality never makes opportunity-cost trades, which is why conflict-driven degradation covers everything it can ever choose (Theorem 5 of the layered page); the front additionally contains the trade-mediated points, and front-necessity quantifies over exactly those.","title":"Why the filter cannot serve the front"},{"category":"section","location":"theory/front/#Why-it-was-set-aside","page":"Pareto front optimality","text":" layered front-necessity\ncanonical, filter-free definition yes (Theorems 4–6) yes (by construction)\ndeclarative (procedure-free) statement no — defined by the trace yes\nSAT query character existential, answer-witnessed universal (necessity, dominance)\nadmissible reductions any preserving one solution none at the version level (Theorem D)\nregistry performance milliseconds–seconds small: ms; DataFrames: ~85 ms; DifferentialEquations (26k versions, unreduced): intractable\n\nThe costs are intrinsic, not implementation slack: universal queries must run against the unreduced problem (Theorem D), every semantically-avoidable candidate needs a witness construction plus a dominance query, and witness certificates are invalidated by later pins. Even under optimistic assumptions the descent is an order of magnitude or more behind the layered implementation — and end users do not observe the difference between the two Pareto points the semantics select. What the exploration contributes is what this page records: the impossibility theorems that close the declarative route, the proof machinery reused for the layered guarantees, and a precise understanding of what the filter does and does not preserve — satisfiability (provably), the layered answer (provably), the front (provably not).","title":"Why it was set aside"},{"category":"section","location":"theory/layered/#The-layered-solution-and-filtering","page":"The layered solution & filtering","text":"This page defines the semantics resolve implements — the layered solution — and proves the two guarantees that make the implementation trustworthy:\n\nfiltering preserves satisfiability — a solvable problem never becomes unsolvable by filtering; and\nfiltering preserves the answer — the solution computed on the filtered problem equals the layered solution of the raw problem, so the semantics is independent of filtering details.\n\nThe second result is the important one: it means the layered solution has a canonical, filter-free definition, and the filter is licensed by a theorem rather than trusted as part of the spec. It also yields a general contract under which future, more aggressive reductions are automatically safe.","title":"The layered solution and filtering"},{"category":"section","location":"theory/layered/#The-layered-solution","page":"The layered solution & filtering","text":"Fix a problem, requirements, and a strict total priority order prec on packages (from the by argument). The layered trace is the following deterministic procedure over the problem's models (= valid solutions covering the requirements, junk allowed):\n\nIf no model exists, the answer is unsatisfiable (nothing).\nOtherwise set the current layer L = mathrmreqs and start with no pins.\nFor each q in L in prec-order: pin q at the best rank feasible given the pins so far — the minimum mathrmrank_M(q) over models M that extend all current pins.\nLet the next layer be the not-yet-visited dependencies of the versions just pinned; if nonempty, go to 3.\nThe answer mathrmLay(D mathrmreqs prec) is the set of pins.\n\nnote: Theorem 1 (well-definedness)\nThe layered trace is deterministic — it depends only on the problem, the requirements, and the priority order, not on which models a solver happens to produce — and its answer is a tight solution.Proof. Each step-3 choice is a minimum over a set determined by the problem and the current pins; layers are determined by pinned versions' dependency sets; so the trace is a deterministic recursion. Feasibility is maintained at every pin (the minimizing model extends the pins), so the final pin set extends to a model; the pins are exactly the dependency closure of the requirements under the pinned versions (layer construction), hence a tight solution. ∎\n\nThe layered solution is pointwise optimal: each package, taken in layer order and priority order, is at the best version compatible with the choices already made. It is also Pareto-optimal in the sense made precise on the next page — no tight solution beats it on every package — though Pareto-optimality is a strictly weaker statement than the trace itself, which pins the unique answer.","title":"The layered solution"},{"category":"section","location":"theory/layered/#Filtering","page":"The layered solution & filtering","text":"Real problems are shrunk before solving by two passes (filter_pkg_info!, run per resolve from prepare_pkg_info):\n\nReachability (find_reachable) computes, per package, a version prefix to keep, by a fixpoint of conflict-driven degradation:\n\nrequirements' first versions are reachable;\nthe first version of each dependency of a reachable version is reachable;\nif a reachable version conflicts with a reachable version, both packages' prefixes extend one further;\nif every version of a package has been pushed past (the package saturates — meaning conflicts could make it uninstallable), packages depending on it have their prefixes pushed past the depending versions — and a saturated package keeps all its versions.\n\nRedundancy elimination (mark_necessary!) then deletes any version qj for which some better version qi (i  j) has a subset of its constraints — every dependency and every (surviving) conflict of qi is also one of qj — iterated to a fixpoint.\n\nThe intuition behind reachability is: an optimal solution only settles for a worse version for a reason, and the reason is a conflict. That intuition is exactly right for the layered (pointwise) semantics, and the theorems below make it precise. (It is not right for Pareto-front reasoning — see the next page — which is one of the sharpest ways to see the difference between the two notions of optimality.)","title":"Filtering"},{"category":"section","location":"theory/layered/#Theorem-2:-the-reach-fixpoint-invariant","page":"The layered solution & filtering","text":"Everything rests on one observation about what the reachability fixpoint guarantees. Call a kept version last if it is the highest-rank (worst) kept version of an unsaturated package.\n\nnote: Theorem 2 (fixpoint invariant)\nAt the reachability fixpoint, the last kept version pr(p) of every unsaturated package p:conflicts with no kept version of any package (saturated packages' versions included); and\ndepends on no saturated package.Proof. If pr(p) conflicted with a kept qk, then whichever of the two was processed later scans the other (already reachable) and fires the degradation rule, extending p's prefix past r(p) — contradicting that r(p) is last. If pr(p) depended on a saturated package, the saturation rule pushes dependents past the depending version, likewise extending the prefix. ∎\n\nIn other words: the cascade pushes prefixes exactly until the \"all-worst-kept\" assignment becomes self-consistent. That immediately gives the first correctness result.","title":"Theorem 2: the reach fixpoint invariant"},{"category":"section","location":"theory/layered/#Theorem-3:-filtering-preserves-satisfiability","page":"The layered solution & filtering","text":"note: Theorem 3\nIf the raw problem has a solution covering the requirements, so does the filtered problem. (The converse is trivial, since filtered models are a subset of raw models.)Proof. Let S be a raw solution. First suppose no requirement's dependency chain touches a saturated package. Build T as the dependency closure from the requirements choosing every package's last kept version. By invariant (2) this closure never enters a saturated package; by invariant (1) every chosen version conflicts with nothing kept, so T is conflict-free; it is dependency-closed by construction and covers the requirements. T is a filtered solution.In general, splice: for saturated packages use S's own versions — all kept, since saturation drops nothing, and S is dependency-closed so saturated dependencies of S-members are assigned in S — and for unsaturated packages use last kept versions. Conflicts between an unsaturated choice and anything vanish by invariant (1); conflicts among the saturated choices are absent because they come from the valid S. Dependency edges are satisfied branch by branch: unsaturated members' dependencies are unsaturated by invariant (2) and get last-kept versions; saturated members' dependencies are resolved inside S or by last-kept substitutes (dependency edges are package-level, so any assigned version satisfies them). ∎\n\nFor the redundancy pass, satisfiability is preserved by direct substitution: any solution using a deleted qj maps to a solution using its dominating qi (fewer dependencies to satisfy, fewer conflicts to avoid), and domination chains terminate at a surviving version because reachability keeps prefixes — a better version is never deleted while a worse one survives.","title":"Theorem 3: filtering preserves satisfiability"},{"category":"section","location":"theory/layered/#Theorem-4:-reduction-invariance-—-the-optimization-license","page":"The layered solution & filtering","text":"The next theorem is the general principle; the filter's correctness is an instance of it.\n\nnote: Theorem 4 (reduction invariance)\nLet D be obtained from D by deleting versions (so mathrmmodels(D) subseteq mathrmmodels(D)), and suppose the layered solution mathrmLay(D) survives in D — every version it uses is kept. Then mathrmLay(D) = mathrmLay(D).Proof. By induction along the trace, both runs make identical pins. At each step the choice for q is the best rank feasible given the (identical) pins. Deleting models can only make ranks harder to achieve, so the D-best is no better than the D-best. And the D-best is witnessed in D by mathrmLay(D) itself: it extends all current pins (its values on pinned packages are exactly the pins, by induction), assigns q its D-best rank (that is what the raw trace pinned), and survives in D by hypothesis. So the choices agree; identical pins induce identical layers. The satisfiable/unsatisfiable branch also agrees, since mathrmLay(D) witnesses satisfiability in both. ∎\n\nThis is the \"decoupling\" result: the layered semantics is defined once, on the raw problem, and any reduction whatsoever is licensed by exhibiting a single surviving solution — the answer. Two consequences:\n\nCertification. Answer preservation can be checked post hoc, cheaply: resolve the filtered problem, then verify each pin against the raw problem — feasibility is free (the answer is itself a raw model), and optimality of each pin is one unsatisfiable solve (\"no raw model with these pins and a better version of q\") per pinned package. A resolver can offer machine-checked raw-canonical answers for the cost of building the raw instance plus one probe per package.\nHeadroom. Future reductions can be far more aggressive than the current filter — anything that keeps the answer is safe, and the certification above detects violations per-resolve rather than trusting the reduction globally.\n\nThe deep reason a theorem this convenient is available is that every SAT query the layered trace makes is existential (\"is a better version feasible?\") and is witnessed by the final answer, which survives the reduction. Universal queries — \"is this package in every solution?\" — do not enjoy this: deleting models changes their truth value. That asymmetry is the fundamental obstruction explored on the next page.","title":"Theorem 4: reduction invariance — the optimization license"},{"category":"section","location":"theory/layered/#Theorem-5:-the-filter-preserves-the-answer","page":"The layered solution & filtering","text":"It remains to show the actual filter satisfies Theorem 4's hypothesis.\n\nnote: Theorem 5 (reach keeps the layered answer)\nEvery version chosen by the raw layered trace is kept by reachability filtering.Proof. By induction along the trace; assume all pins so far are kept versions. Consider the step for package q, and suppose first q is unsaturated. We claim q's last kept version qr(q) is feasible given the pins, so the best feasible rank — the trace's choice — is at most r(q), i.e. a kept version. To see feasibility, construct a model: take the pins (jointly feasible by the trace's invariant, and kept by induction), add qr(q), and complete with the last-kept dependency closure of everything present. By invariant (1) of Theorem 2, qr(q) and every last-kept version conflict with nothing kept — in particular not with the pins or each other; by invariant (2) the completion never needs a saturated package; pins' dependencies are satisfied by pinned or last-kept packages. The result is a model containing the pins and qr(q). If instead q is saturated, all its versions are kept and there is nothing to prove. ∎\n\nnote: Theorem 6 (redundancy keeps the layered answer)\nThe layered trace never chooses a version deleted by redundancy elimination.Proof. Suppose the trace chose qj and qj is dominated by a kept qi with i  j. Any model witnessing qj's feasibility given the pins converts to one witnessing qi's: swap the version; qi's dependencies are a subset of qj's (already present) and its conflicts a subset (already avoided). So qi was feasible and better, contradicting that the trace chose j. Domination chains terminate at a kept version, and the argument applies to whichever kept dominator terminates the chain. ∎\n\nComposing: the raw answer survives reachability (Theorem 5), so by Theorem 4 the reach-filtered problem has the same layered answer; that answer survives redundancy elimination (Theorem 6 applied on the reach-filtered problem), so by Theorem 4 again the fully filtered problem has the same layered answer. The implementation's answer is the raw layered solution.","title":"Theorem 5: the filter preserves the answer"},{"category":"section","location":"theory/layered/#Iterating-the-filter","page":"The layered solution & filtering","text":"It is natural to ask whether a single reachability pass followed by a single redundancy pass leaves anything on the table — whether iterating the two would prune more. The answer illuminates what each pass can and cannot see.\n\nReachability alone is a closure operation:\n\nnote: Proposition (reachability is idempotent)\nRe-running reachability on its own output keeps everything.Proof. The reach set is a least fixpoint, built by a derivation whose steps mention only versions that end up kept: requirements' first versions, first versions of kept dependencies, and conflict/saturation extensions among reachable versions. Reachability deletes only version suffixes and whole packages, none of which appear in any derivation step, so the entire derivation replays on the filtered problem and re-derives every kept version. ∎\n\nThe composite filter, however, is not idempotent: redundancy elimination can invalidate reachability's reasons. A minimal example:\n\nnote: Example (redundancy changes reachability)\nTake requirements p q r with two versions of each of p, q, r, one version of s, the single dependency p1 to s (p2 depends on nothing), and conflicts q1 otimes r1, q2 otimes r1, q2 otimes p1.Reachability: q1 otimes r1 extends both packages, making q2 reachable; then q2 otimes p1 extends p, so p2 is kept — that conflict is p2's only justification. Redundancy: q2's constraints strictly contain q1's, so q2 is deleted. But the now-orphaned p2 survives: domination by p1 fails because p1's dependency on s has no counterpart in p2. Re-running reachability on the filtered problem would drop p2, whose reason to exist is gone.\n\nThe general shape is: a deleted version was another package's only reason to degrade, and the orphaned version escapes the domination test. Two things can shield an orphan. One is a dependency-set difference between it and its better sibling, as above — version-varying dependency sets again, the same dividing line as the fragment lemma. The other is saturation: saturation pushes are dependency-driven and invisible to the domination test, and deleting versions can unsaturate a package on a re-run, vaporizing the pushes that justified its dependents' worse versions.\n\n(A note on the domination test: it ignores conflicts whose partner version was itself dropped by reachability — such conflicts constrain no model over the kept versions, so ignoring them is licensed by exactly the swap argument of Theorem 6. Counting them would spuriously block domination and orphan far more versions than the two genuine mechanisms above.)\n\nNone of this threatens correctness. By Theorem 4, any number of additional passes preserves the layered answer, so iterating the filter to a mutual fixpoint is perfectly legal — and since the passes are cheap, that is what the implementation does: redundancy elimination first (it needs no reachability marks, and on registry-scale problems it does most of the shrinking), then reachability and redundancy alternating until neither deletes anything. The returns diminish fast: a further round prunes anything at all in only about 0.007% of deliberately conflict-dense random instances with version-independent dependencies and about 1% with version-varying ones, and what it prunes is marginal. Essentially all of the reduction is captured by one reachability pass and one redundancy pass; the remaining rounds just collect the stragglers because they cost next to nothing.","title":"Iterating the filter"},{"category":"section","location":"theory/layered/#Preprocessing-for-caching","page":"The layered solution & filtering","text":"Everything above concerns one resolve. When many resolves share a registry state, the question becomes which artifacts can be computed once and reused — and the answer is organized by what each artifact depends on: the registry content, the version ordering, and the requirements. Three results pin down the tiers.\n\nnote: Proposition (filtering for a larger requirement set)\nLet mathrmreqs subseteq mathrmreqs. The filter run with requirements mathrmreqs keeps every version of mathrmLay(D mathrmreqs), so by Theorem 4 mathrmLay(F_mathrmreqs(D) mathrmreqs) = mathrmLay(D mathrmreqs).Proof. Reachability is monotone in its seed set on a fixed problem — every derivation from mathrmreqs is a derivation from mathrmreqs — so the mathrmreqs-reach keeps a superset of the mathrmreqs-reach, which keeps the answer (Theorem 5). Redundancy elimination is requirement-agnostic outright: Theorem 6's swap argument converts any witness model containing a dominated version into one containing its dominator without touching coverage, so on whatever problem it runs, the layered trace of every requirement set avoids the versions it deletes. Each round of the iterated filter therefore preserves the answer, and Theorem 4 applies after each. ∎\n\nnote: Corollary (whole-registry prefiltering)\nFiltering once with every package required yields a reduced problem on which any requirement set resolves to its raw answer, with satisfiability verdicts preserved (unsatisfiability trivially, since models only shrink; satisfiability because the answer survives). Filtering is, in this sense, a monotone function of the requirements, and the all-requirements filter is its safe upper envelope.\n\nThe prefiltered problem still depends on the version ordering — both reachability's prefixes and redundancy's better-dominates-worse are stated in terms of it. How much preprocessing can be shared across orderings has an exact answer:\n\nnote: Proposition (order-free deletion is exactly model-freeness)\nA version can be deleted without changing the layered answer for every version ordering and every requirement set iff it appears in no model.Proof. If a version appears in no model, deleting it leaves the model set — and hence every trace — untouched. Conversely, if pv appears in a model M, order p's versions with v first and require p: the trace pins p at the best feasible rank, and M witnesses rank 1, so the answer contains pv and deleting it changes the answer. ∎\n\nSo the strongest ordering-independent filter is exactly the removal of model-free versions (of which the arc-consistency test — a version one of whose dependencies has no compatible version left — is a cheap sound approximation, and per-version SAT probes are the complete one). One more piece of ordering-independent structure exists:\n\nnote: Lemma (interchangeable versions)\nCall two versions of a package interchangeable when they have the same dependency set and every compatibility entry in the problem — their own and every other package's — constrains them equally (equivalently: their constraint rows are identical). Interchangeable versions can be collapsed to whichever of them the active ordering ranks best, preserving that ordering's layered answers: the collapsed versions are dominated by the representative (identical constraints are in particular a subset), so Theorem 6's argument applies on the raw problem and Theorem 4 finishes. The classes are ordering-independent; only the representative choice is not.Beware that a package's own dependencies and compat matching is not sufficient: another package's compatibility bound can admit one of the two versions and not the other, and then they are genuinely distinguishable — solutions needing the excluded pairing must pick the right one. The equal-incoming-constraints half of the definition is what rules this out.\n\nTogether these give a three-tier cache: per registry state (parsed data, the model-free set, interchangeability classes), per ordering (the whole-registry prefilter), and per resolve (requirement-specific filtering, which is fast and not worth caching). A non-standard ordering — say, preferring versions closest to an installed manifest — invalidates only the middle tier, which is orders of magnitude cheaper to rebuild than the first.\n\nThe implementation draws the line between the first tier and the rest at pkg_info, whose output — conflict matrices, the arc-consistency prune and the classes (version_classes) — is exactly the registry-only artifact, and it merges the second and third tiers into one per-resolve pass, prepare_pkg_info, since the middle tier is cheap enough not to be worth storing. That pass lays each package's versions out in the ordering the query asked for (version_permutations), refines the classes by the user's constraints and collapses each to its best member in that ordering (class_representatives), and filters the result (filter_pkg_info!).\n\nThe ordering is a resolve parameter rather than part of the problem, which is what makes one artifact serve every query: it selects among the valid solutions instead of changing which solutions are valid. What does change the model set — the user's compat bounds and pins, and the admission of prerelease and yanked versions — is a Problem, and enters as constraints on the artifact's versions rather than as deletions from it (see exclusion_masks). So there is one T1 artifact per registry state, full stop: every version the registry state offers is in it, in canonical order, and each query says which of them it will accept and in what order it prefers them.\n\nwarning: Formalization boundary\nTheorems 2, 3, 5, and 6 are proved against the rule set as stated above, which is a faithful but hand-made reading of find_reachable and mark_necessary!; the correspondence of code to rules is checked by review and by the test suite, not mechanically. The package's tests compare the implementation against a brute-force reference that implements the layered trace on raw data across hundreds of thousands of random instances — a direct, ongoing empirical check of Theorems 4–6 — and, whenever the implementation reports unsatisfiable, verify by enumeration that no raw solution covers the requirements (Theorem 3).","title":"Preprocessing for caching"}]
}
