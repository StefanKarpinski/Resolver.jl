var documenterSearchIndex = {"docs":
[{"category":"section","location":"theory/unsat-explanation/#Explaining-an-unsatisfiable-resolve-—-the-complete-theory","page":"Explaining an unsatisfiable resolve","text":"This document is the whole theory of diagnosing and explaining an unsatisfiable resolver query: what a report must contain, what every part of it means, and the proof of every claim the design rests on. It is written to be implemented from. It names no functions and assumes no code; what it assumes of the surrounding resolver is collected in one section (Section 10) as a short list of interfaces.\n\nThe contract, up front. A report for an unsatisfiable query owes the reader three things:\n\nThe fixes. How many independent things are wrong, and for each, a menu of alternatives — such that any combination of choices, one per menu, repairs the query, giving up as little as any repair can.\nThe explanations. For each conflict, an accurate account of where it comes from: which of the user's own facts collide, at which package, and through which of the registry's statements.\nThe witnesses. For each fix, what taking it yields, with enough on the page that the reader can see why the fix works.\n\nWhat a report does not owe is a hand-checkable proof. Every sentence printed must be machine-verified true, and the contradiction must be visible on the page — but the reader's job is to understand, not to re-derive. It also does not owe completeness of every enumeration; it owes honesty about every gap, at three named places (Section 11).\n\nSections 1–2 build the logic. Sections 3–5 are the fix half: repairs, menus, and which reasons each conflict answers for. Sections 6–7 are the explanation half: the pivot form and what it guarantees about the fixes. Sections 8–11 say what to verify, how to word things, what to assume, and where the edges are.","title":"Explaining an unsatisfiable resolve — the complete theory"},{"category":"section","location":"theory/unsat-explanation/#1.-Setting","page":"Explaining an unsatisfiable resolve","text":"There is a finite set of packages; each package p has a finite nonempty set of versions V(p), totally ordered (\"newer\"). Write V⊥(p) = V(p) ∪ {⊥}, where ⊥ means not installed. An installation ι assigns every package an element of V⊥(p).\n\nAll constraints — the registry's and the query's — are clauses in the logic of Section 2. Two actors supply them:\n\nThe registry is a finite, fixed, satisfiable set ℛ of clauses. Each clause of ℛ must be one independently intelligible statement — a dependency edge over a range of the depender's versions, or one compatibility bound between a range of one package's versions and a range of another's. This granularity is a requirement, not a convenience: a clause of ℛ is the unit of blame and the unit of print, so each must be checkable against a single registry entry.\nThe query is a finite set Q of facts, of two kinds:\nRequirement(p) — the clause ⟨p : V(p)⟩: p is installed.\nConstraint(p) — the clause ⟨p : L(p) ∪ {⊥}⟩, where L(p) is the set of versions the query's own constraints (compat, pins, admission policy) leave of p: if p is installed, it is one of these.\n\nEach fact must be a single clause and a single user-performable action: dropping the requirement on p, or lifting one's own constraints on p. Package granularity is the finest grain that stays a user action — no user edit gives back one excluded version of a package while keeping its siblings excluded — and that is why facts are per-package, not per-version.\n\nA withdrawal of W ⊆ Q keeps ℛ ∪ (Q ∖ W). Withdrawing more facts only removes clauses, so satisfiability is monotone in withdrawal: every model survives. We assume throughout that ℛ ∪ Q is unsatisfiable (otherwise there is nothing to report) and that ℛ alone is satisfiable (otherwise the registry is broken, which is a different report). The second assumption guarantees that repairs exist: withdrawing all of Q always works.","title":"1. Setting"},{"category":"section","location":"theory/unsat-explanation/#2.-The-clause-logic","page":"Explaining an unsatisfiable resolve","text":"A literal on package p is a subset of V⊥(p). A clause C assigns a literal C(p) to each package, with C(p) = ∅ for all but finitely many; its support is the set of packages with nonempty literal. An installation satisfies C, written ι ⊨ C, when ι(p) ∈ C(p) for some p. A clause with empty support is unsatisfiable; a clause with C(p) = V⊥(p) is a tautology. Clause C subsumes D when C(p) ⊆ D(p) for every p: then every model of C models D.\n\nSix readings of one shape cover everything a report says:\n\nclause reads as\n⟨A : V(A)⟩ A is installed\n⟨A : L ∪ {⊥}⟩ the query (or the registry) leaves only L of A\n⟨A : (V(A)∖R) ∪ {⊥}⟩ A cannot be any of R\n⟨A : (V(A)∖R) ∪ {⊥}, B : S⟩ A@R requires B@S\n⟨A : (V(A)∖R) ∪ {⊥}, B : S ∪ {⊥}⟩ A@R constrains B@S\n⟨A : (V(A)∖R) ∪ {⊥}, B : {⊥}⟩ A@R leaves no version of B\n\nThe difference between the verbs is only what the consequent admits: requires brings the package in, constrains permits its absence, and a consequent of {⊥} alone rules the package out. Whether a statement forces is data in the clause, never a flag beside it — which is why either package may be taken as the consequent, and the verb follows (Section 9).\n\nOne rule. Clauses compose by resolution on a package. It is stated for any finite set of clauses at once, because literals are sets and more than two clauses can be needed to empty one:\n\nRule (resolution on q). For clauses C₁, …, Cₙ, the resolvent Res_q(C₁,…,Cₙ) has literal ⋂ᵢ Cᵢ(q) at q and ⋃ᵢ Cᵢ(p) at every other p.\n\nLemma 1 (soundness). If ι ⊨ Cᵢ for every i, then ι ⊨ Res_q(C₁,…,Cₙ).\n\nProof. If some Cᵢ is satisfied at a package p ≠ q, then ι(p) ∈ Cᵢ(p) ⊆ ⋃ⱼ Cⱼ(p). Otherwise every Cᵢ is satisfied at q, so ι(q) ∈ ⋂ᵢ Cᵢ(q). ∎\n\nIn particular every resolvent is entailed by the clauses it came from — soundness of everything derived is inherited, never re-established.\n\nElimination. To eliminate package q from a clause set Γ: let Γ_q be the clauses whose support contains q and Γ' the rest, and put\n\nElim_q(Γ) = Γ' ∪ { Res_q(S) : S ⊆ Γ_q minimal with ⋂_{C∈S} C(q) = ∅ }.\n\nEach such resolvent has ∅ at q, i.e. does not mention q. Restricting to minimal emptying subsets loses nothing: the resolvent of a larger emptying subset is subsumed by that of a minimal one inside it. This is the coverage condition of the design, stated as the rule itself: eliminating a package resolves every minimal set of clauses that leaves it nothing to be — pairs do not suffice, because three set-valued literals can have empty intersection while every two overlap.\n\nTheorem 2 (elimination preserves satisfiability). Γ is satisfiable if and only if Elim_q(Γ) is.\n\nProof. (⇒) A model of Γ satisfies every resolvent by Lemma 1, and since a resolvent's q-literal is empty it is satisfied at some other package; so the model, unchanged, models Elim_q(Γ).\n\n(⇐) Let ι model Elim_q(Γ); we choose a value at q. Let U = { C ∈ Γ_q : ι(p) ∉ C(p) for every p ≠ q } — the clauses that must be satisfied at q itself. If ⋂_{C∈U} C(q) = ∅, pick a minimal S ⊆ U with empty intersection; its resolvent is in Elim_q(Γ), so ι satisfies it at some p ≠ q, i.e. ι(p) ∈ C(p) for some C ∈ S ⊆ U — contradicting C ∈ U. So the intersection is nonempty; set ι(q) to any member (if U is empty, any value at all). Then ι ⊨ Γ. ∎\n\nProjection. For a keep-set K of packages, proj_K(Γ) eliminates the packages outside K one at a time, in any order. By induction on Theorem 2 it preserves satisfiability, and every clause of it has support inside K. Different elimination orders may produce different (mutually subsuming) clause sets; the theory licenses any of them, and the preferences of Section 6 choose among what they print.","title":"2. The clause logic"},{"category":"section","location":"theory/unsat-explanation/#3.-Reasons-and-repairs","page":"Explaining an unsatisfiable resolve","text":"Throughout, ℛ is fixed and satisfiability of a set X ⊆ Q means satisfiability of ℛ ∪ X.\n\nA reason is a minimal unsatisfiable subset of Q: it cannot hold, and every proper subset can. Reasons say why the query fails.\nA correction set is a W ⊆ Q whose withdrawal is satisfiable. A repair is a minimal correction set; F is the set of repairs, and F_min ⊆ F the repairs of smallest cardinality k. Repairs say what to change; F_min are the ones that give up as few of the user's facts as any repair can.\n\nSince ℛ is satisfiable, Q is a correction set, so F ≠ ∅. Since ℛ ∪ Q is unsatisfiable and everything is finite, reasons exist: every unsatisfiable subset contains one.\n\nTheorem 3 (hitting duality). W is a correction set if and only if W intersects every reason. Hence the repairs are exactly the minimal hitting sets of the reasons.\n\nProof. If some reason R is disjoint from W, then R ⊆ Q ∖ W, so the withdrawal of W keeps an unsatisfiable subset and is unsatisfiable. Conversely, if W meets every reason, then Q ∖ W contains no reason; were Q ∖ W unsatisfiable it would contain a minimal unsatisfiable subset — a reason — by finiteness. So it is satisfiable and W is a correction set. Minimality on both sides is the same condition on the same sets. ∎\n\nTwo distinct repairs are incomparable (both minimal), a fact used silently below.\n\nFinding F_min. One oracle suffices: is there an installation satisfying ℛ that violates at most k facts of Q, while for each blocked set M satisfying at least one fact of M? (In a solver this is the failed instance plus a cardinality bound over the fact indicators plus one clause per blocked set; but any oracle with this signature will do.)\n\nTheorem 4 (bounded enumeration). (a) The oracle with bound k and no blocked sets succeeds iff some correction set of size ≤ k exists; the least such k is the size of the cheapest repairs. (b) At that k, iterating with each found repair blocked enumerates exactly F_min and terminates.\n\nProof. (a) A model violating U, |U| ≤ k, witnesses that U is a correction set; a correction set C with |C| ≤ k has a model of its withdrawal, which violates a subset of C. (b) Let k be minimal. A found model's violated set U is a correction set with |U| ≤ k; it contains a repair, which has size ≥ k; so U is that repair and |U| = k, i.e. U ∈ F_min. The blocking clauses exclude exactly the already-found members (distinct repairs are incomparable, so no unfound member contains a found one... more precisely: U satisfies \"some fact of each blocked M holds\" exactly when M ⊄ U, and for distinct members of F_min that is automatic), so each iteration finds a new member, and while an unfound M ∈ F_min remains, the model witnessing M violates exactly M, contains no other member, and satisfies every block — the oracle succeeds. Finiteness terminates it. ∎\n\nTheorem 5 (one question decides whether anything larger exists). With no cardinality bound and all of F_min blocked, the oracle succeeds iff F ≠ F_min.\n\nProof. A model's violated set U contains no member of F_min; the repair inside U is therefore not in F_min, so F ∖ F_min ≠ ∅. Conversely for N ∈ F ∖ F_min, the model witnessing N violates exactly N, which contains no member of F_min by incomparability, so every block is satisfied. ∎","title":"3. Reasons and repairs"},{"category":"section","location":"theory/unsat-explanation/#4.-Menus","page":"Explaining an unsatisfiable resolve","text":"A presentation is a family of pairwise disjoint nonempty fact-sets D₁, …, D_m — the menus — one per conflict. A selection picks one entry from each menu; its pick-set is the set of picked facts. The presentation contract has three clauses:\n\n(C1) every selection's pick-set is a repair — the offer is sound;\n(C2) every selection's pick-set is in F_min — the offer is as cheap as any repair;\n(C3) if some member of F_min is no selection, the report says so.\n\nEverything a menu page claims follows from these three, and Section 5's theorems need only (C1). Two constructions meet the contract.\n\nThe product. Sometimes F_min is a product, and the check is cheap:\n\nTheorem 6 (a family that counts right is a product). Let C₁, …, C_k partition the facts F_min uses, suppose every M ∈ F_min contains exactly one fact of each Cᵢ, and |F_min| = ∏ᵢ |Cᵢ|. Then F_min = C₁ × ⋯ × C_k (as pick-sets).\n\nProof. Sending each M to the tuple of its facts is well defined by the first hypothesis and injective because a set is determined by its elements. An injection between finite sets of equal size is a bijection, so every tuple is some M. ∎\n\nLemma 7 (the partition is forced). If F_min = C₁ × ⋯ × C_k, then two facts share a repair in F_min iff they lie in different factors. Hence the factors are the connected components of the \"never share a repair\" relation on the used facts, and the candidate partition for Theorem 6 is unique and computable without search.\n\nProof. Each member has exactly one fact per factor, so same-factor facts never share. For c ∈ Cᵢ, c′ ∈ Cⱼ, i ≠ j, the product contains a member picking both. So \"never share\" holds exactly within factors; within a factor every pair is directly related, across factors no pair is, and the components are the factors. ∎\n\nOffering Cᵢ as the i-th menu satisfies (C1) and (C2) with every member of F_min reached, so (C3) has nothing to say. The conflict count k is not an artifact of any search: it is the size of the smallest fix. And the shape is not a choice:\n\nTheorem 8 (canonicity). Suppose F_min = C₁ × ⋯ × C_k and let D₁, …, D_m be any presentation in which every selection's pick-set is in F_min and every member of F_min is some selection's pick-set. Then m = k and the Ds are the Cs, up to order.\n\nProof. First, the factors Cᵢ are pairwise disjoint: a fact in two factors would let a tuple pick it twice, giving a pick-set of size k−1 in F_min, which has members of size k only. Menus are disjoint by definition, so every pick-set has exactly m elements; pick-sets lie in F_min, so m = k.\n\nNo two distinct facts of one factor Cᵢ are pickable at different menus: a selection taking both would have a pick-set in F_min with two facts of Cᵢ. Every fact of Cᵢ occurs in some member of F_min, hence in some menu; so all of Cᵢ lies in a single menu D_{σ(i)}, and no fact lies in two menus (disjointness). σ is injective: if Cᵢ, Cⱼ ⊆ D_p with i ≠ j, a member of F_min needs one fact of each, but both are available only as the single pick from D_p. So σ is a bijection of {1..k}. Finally D_{σ(i)} holds nothing beyond Cᵢ: an extra fact d would enter some pick-set, hence lie in ⋃ⱼ Cⱼ, hence in Cⱼ for some j ≠ i, hence in D_{σ(j)} — against disjointness. ∎\n\nSo in the product case the report's shape is forced by the repair family itself. This is the menu-side twin of the pivot theorem of Section 6: there the choice is proved entirely free, here it is proved entirely absent, and each is the strongest possible statement of its kind.\n\nEntries may be sets. Nothing above needs an entry to be a single fact. With entries as fact-sets and a selection taking the union of one entry per menu, (C1) and (C2) read verbatim, and Theorem 9 generalizes with \"contains some whole menu\" read as \"meets every entry of some menu\" — the proof is the same, picking per menu an entry disjoint from the reason. A menu whose entries are whole repairs is the degenerate case: one choice, compound entries, trivially a product. The primary presentation below keeps entries small — single facts wherever the family lets it — so everything in Sections 5–7 applies to it unchanged.\n\nTheorem 23 (the finest factorization). Call a partition P of the used facts factoring when F_min is exactly the unions of one trace per block — F_min|B = {M ∩ B : M ∈ F_min} — with every combination realized. Then: the trivial partition factors; if P and Q factor, so does their common refinement; hence a unique finest factoring partition exists. Moreover each block's traces have one size.\n\nProof. The trivial partition factors by definition. For the refinement: it suffices that any one block A ∩ B of the refinement swaps freely, and the general combination follows block by block. Given members M, M′, form M₁ as M′ with its A-trace replaced by M's — a member, since P factors — and then M₂ as M′ with its B-trace replaced by M₁'s — a member, since Q factors. M₂ is M′ everywhere except A ∩ B, where it is M. Factoring partitions are thus closed under common refinement, and the refinement of all of them is the finest. Sizes: were two traces of one block of different sizes, combining every block's smallest trace would give a member smaller than k. ∎\n\nThe finest factorization is the canonical presentation when it is fine: menus are its blocks, entries its traces, compound exactly where a trace is not a singleton, and the plain product is the case where every trace is. Measured on entangled registry queries, however, the families are bimodal: either the plain product holds, or the family is prime — no nontrivial factoring partition at all — and the finest factorization degenerates to the one-menu list of whole repairs. So structure, where it exists, is found by Theorem 23; where it does not, it is found inside F_min a piece at a time, and coverage is completed by layers.","title":"4. Menus"},{"category":"section","location":"theory/unsat-explanation/#Covering-the-cheapest-fixes-by-products","page":"Explaining an unsatisfiable resolve","text":"A generalized rectangle in F_min is a presentation M₁, …, M_j over pairwise disjoint groups of facts — the group of Mᵢ being the union of its entries, an entry a set of facts — such that every selection's pick-set is a member of F_min. It is exact: it offers nothing the family does not hold, so (C1) and (C2) come for free and only (C3) is left to answer. Its coverage is the number of members its selections reach, which is ∏ᵢ|Mᵢ| exactly, since the groups are disjoint and a set determines its elements. Three things follow immediately from the definition. Every member M alone is a rectangle, of coverage one — one menu, one entry. Any subfamily S ⊆ F_min that factors in the sense of Theorem 23 is a rectangle, its menus the blocks' traces. And F_min itself is a rectangle of one menu whose entries are whole members, which is the flat list.\n\nA product cover of F_min is a sequence of generalized rectangles — the layers — whose selections' pick-sets are together exactly F_min. Build one greedily: take a rectangle, emit it, delete the members it reaches, and recurse on what is left, which is again a family of size-k fact sets over the same facts.\n\nTheorem 29 (a product cover exists, is finite, and presents the family exactly). The construction terminates, and its layers present F_min: no selection of any layer is outside F_min, and no member of F_min is the selection of no layer.\n\nProof. Exactness is the definition of a rectangle, and a rectangle in the family that is left is a rectangle in F_min, since what is left is a subset of it. For termination, each layer reaches at least one member — a single member is always available as a rectangle — so the remainder strictly shrinks and the construction stops with it empty. Completeness is then the stopping condition: a member never reached would still be there. ∎\n\nWhich rectangle to take is an explainability preference among sound forms, not a soundness question — Section 5 needs (C1) alone — and the preference is what a layer costs to read against how much of the family it saves the reader from reading: a line for each menu, a line for each entry under it, and a phrase for each action an entry names. That price is what keeps the flat list from winning by default: it reaches everything, and it costs an entry, and every action of that entry, per member.\n\nThe first layer is constrained beyond that, and Corollary 25 says why: it is the one the proofs answer for. It is required to be a product of two or more menus wherever one exists, so the reader is shown the family's structure and not a list of it.\n\nSo a conflict is a menu of the leading layer. The leading layer satisfies (C1), so by Theorem 9 each of its menus owns a reason, and by Theorem 10 one of its very own; the layers after it own none (Corollary 25). A conflict is whatever a page can argue for, and that is exactly a leading menu: it gets a heading, the reason it owns as its chain, its own menu of fixes, and the verdicts on the actions those make tempting. A menu of compound entries earns a further owned reason wherever the first leaves a package the menu offers unspoken of — a page that asks the reader to change something its argument never mentioned is talking past them, which is what V3 of Section 8 checks.\n\nThe block's later layers are then not conflicts but alternatives to all of its conflicts at once, printed after them. Each is a product of menus like any layer, and every one of its selections is a cheapest repair the block's conflicts do not offer between them. What an alternative replaces is its own block and nothing else: by Theorem 30 the blocks are independent, so a repair of the query takes one selection from each block, and where the reader takes an alternative for one block the other blocks stand exactly as printed. That is the whole of what \"without any of the fixes for Conflicts 1–3\" means, and the label carries it.\n\nLemma 31 (an unreached member avoids a whole menu). Let M ∈ F_min be no selection of the leading layer. Then M is disjoint from some menu of that layer.\n\nProof. Suppose M meets every menu, say in the entry eᵢ ⊆ M of Mᵢ — a menu's entries need not be disjoint from one another, so fix one such eᵢ per menu. The selection S = ⋃ᵢ eᵢ is a member of F_min by exactness, and S ⊆ M. Members of F_min are minimal correction sets, hence pairwise incomparable, so S = M — and M is a selection after all. ∎\n\nSo every selection of a later layer declines a whole menu, and what the label may say the layer as a whole is instead of is the menus the layer shares no fact with at all — those its every selection declines. Where each of them offers a single fix, that fix is a single thing the reader is doing without and the label says it; where one of them offers a choice there is no single thing to name, and the label names those conflicts instead. Lemma 31 is per member, not per layer, so the set can be empty — the layer's selections declining different menus and none of them all — and then the label says only that the layer is another way.\n\nLemma 24 (what a layer leaves is a blocked instance's cheapest repairs). Add to the instance, for each member M a layer reaches, the clause that some fact of M holds. The blocked instance's minimal correction sets of size k are exactly the members left.\n\nProof. As in Theorem 5: a model of the blocked instance violates a set containing no reached member, and if that set has size k it is a minimal correction set of the original instance — a member of F_min — that is not reached; conversely a left member's witnessing model satisfies every blocking clause, since distinct members of F_min are incomparable. ∎\n\nSo what is left is defined by the family and not by any choice of fixes, and it is itself a repair family over the same facts — which is what licenses the recursion. Covers are not unique and least covers need not be; the order of construction is a stated preference, like the pivot tie-break of Section 6.\n\nCorollary 25 (reasons do not layer). The first layer satisfies (C1), so by Theorem 9 every reason meets every entry of one of its menus. The reasons — and with them the explanations of Section 6 — attach entirely to the first layer; later layers repartition repairs, not reasons, and owe the reader menus and witnesses, never proofs. (The blocked instance of Lemma 24 does have minimal unsatisfiable sets of its own, but they mix the query's facts with the refusal clauses — \"given that none of the fixes above is taken\" — and a proof from refusals is contingent on them. The page never prints one.)\n\nTheorem 30 (factoring composes, so nothing is left across factors). Let P = {B₁, …, B_r} be a factoring partition of F_min (Theorem 23) and give each block Bₜ a product cover of its own trace family F_min|Bₜ. Reading each block as one conflict, and a repair of the whole query as one selection from each, the presentation is exact and complete: the selections are exactly F_min.\n\nProof. P factoring says F_min is the set of unions of one trace per block, every combination realized. Each block's cover presents F_min|Bₜ exactly (Theorem 29), so the selections available at block t are exactly its traces; the combinations of them are therefore exactly the unions of one trace per block, which is F_min. ∎\n\nTwo consequences. Completeness of the whole presentation is the product of the factors' completeness, so where the finest factorization is nontrivial there is no cross-factor remainder to print: a page that covers each factor covers the query, and a residue enumerating combinations across factors is enumerating repairs the factors have each already offered. And a block that is prime is covered on its own facts alone: the search runs over that block's trace family, not over the whole of F_min.\n\nWhat a one-entry menu may say about itself. With F_min covered (Theorems 29 and 30) and Theorem 5 answered, the gap a menu's wording must confess has two sources: costlier fixes, and an enumeration cut short (the one way coverage can silently fail — and one further blocked solve at the cap decides even that, so it is known, not guessed). \"Only\" is a claim about the world:\n\nwhat exists beyond this entry a menu of one says\nnothing The only fix:\nonly costlier fixes The only minimal fix:\nan alternative to this conflict's block, or a cut enumeration One fix:\n\nNever derive \"only\" from the length of a vector; derive it from the decided questions. The alternative row is block-local: an alternative to some other block is another repair of the query and no repair of this conflict, so it takes nothing away from what this entry may say about itself, while an alternative to this conflict's own block is a cheapest repair that settles the block without this entry and denies \"only\" outright.","title":"Covering the cheapest fixes by products"},{"category":"section","location":"theory/unsat-explanation/#5.-Ownership:-which-reasons-a-conflict-answers-for","page":"Explaining an unsatisfiable resolve","text":"Conflict i owns a reason R when Dᵢ ⊆ R. Its page explains the reasons it owns. Everything below needs only contract clause (C1).\n\nTheorem 9 (ownership). In any presentation where every selection's pick-set is a repair, every reason contains some whole menu.\n\nProof. Suppose reason R misses an element of every menu: pick dᵢ ∈ Dᵢ ∖ R for each i. The pick-set S = {d₁, …, d_m} is disjoint from R, so after withdrawing S every fact of R is still held, and R is unsatisfiable — so the withdrawal is unsatisfiable, contradicting (C1). ∎\n\nNote what the hypothesis is not: no product, no equal sizes, no minimality of the pick-sets. Any presentation meeting the contract — product, rectangle, or anything future — inherits the theorem.\n\nTheorem 10 (privacy). For every conflict i and every transversal T of the other menus (one entry from each Dⱼ, j ≠ i), there is a reason disjoint from T that contains the whole of Dᵢ; and any reason disjoint from such a T is owned by conflict i alone.\n\nProof. Menus are disjoint, so |T| = m − 1 = k − 1 < k, the minimum repair size; hence T is not a correction set and the withdrawal of T is unsatisfiable. Take any minimal unsatisfiable subset R of Q ∖ T — a genuine reason, since minimality and unsatisfiability of a set do not depend on the pool it was found in. By Theorem 9, R contains some whole menu; for each j ≠ i it misses T's entry of Dⱼ, so the menu it contains is Dᵢ, and it contains no other whole menu. ∎\n\nCorollary 11. Every conflict owns at least one reason, and indeed one of its very own — containing no other conflict's whole menu — for every way the other conflicts might be settled.\n\nCorollary 12 (what is left is settled). Fix a transversal T of the other menus. Every reason of the query that avoids T — in particular, every reason of the T-withdrawn query — is owned by conflict i.\n\nProof. As in Theorem 10: it contains a whole menu and misses every other menu's T-entry. ∎\n\nCorollary 12 is why no further machinery is needed for requirements that fail \"for the same reason\" as one another: with the other conflicts settled, everything still wrong is this conflict's to explain, and each still-broken requirement sits in its own reason with this conflict's facts. Explaining one owned reason per page section, every such requirement gets its account.\n\nOwnership is not a partition. It is tempting to read Theorem 9 as \"the reasons divide among the conflicts\". They do not:\n\nExample (a reason owned twice). Facts a₁ a₂ b₁ b₂ x y z v; reasons exactly{a₁ a₂ b₁ b₂}   {a₁ a₂ x}   {a₁ a₂ y}   {b₁ b₂ z}   {b₁ b₂ v}(realizable: give ℛ one clause per line forbidding that conjunction — then the unsatisfiable subsets are exactly the supersets of these five). A hitting set must cover {a₁a₂x} and {a₁a₂y} — with one element only via a₁ or a₂ — and {b₁b₂z}, {b₁b₂v} likewise via b₁ or b₂; both together also cover {a₁a₂b₁b₂}. Any pair using x, y, z, v misses one of the five. So F_min = {a₁,a₂} × {b₁,b₂} exactly: a perfect product, two conflicts. Yet {a₁ a₂ b₁ b₂} is a genuine minimal reason containing both menus entire.\n\nSo a reason may be owned by several conflicts, and either conflict's action settles a shared one. The sound policy is to print a shared reason under every conflict that owns it — redundant, never wrong — and Corollary 11 guarantees the redundancy is never load-bearing: each conflict also has private grounds. (A deduplicating presentation is possible but buys little; measure the frequency of sharing before spending design on it.)\n\nCostlier fixes and second reasons are the same phenomenon. Two results pin down when repairs beyond the cheapest exist, and they meet exactly at the blocked-fix entries of Section 9.\n\nTheorem 26 (a second owned reason forces a costlier fix). In the product case, if a conflict owns two reasons, then F ≠ F_min.\n\nProof. Let conflict i own R₁ ≠ R₂ and let T be a transversal of the other menus. Every reason not owned by i contains some other menu whole and so contains its T-pick. Every reason owned by i contains Dᵢ properly — were Dᵢ itself a reason, the antichain property would leave it the only owned one — so the owned reasons' parts outside Dᵢ are nonempty, and some finite set S of facts outside Dᵢ meets them all. Then T ∪ S is a correction set avoiding Dᵢ entirely, and the minimal correction set inside it is no selection; since F_min is exactly the selections (Theorem 8), it is not of size k, so it is strictly larger. ∎\n\nTheorem 27 (a costlier fix forces a second owned reason). If some minimal correction set has size greater than k, then some conflict owns two reasons.\n\nProof. Let N be minimal with |N| > k. Minimality gives every e ∈ N a witness reason meeting N in {e} alone, and distinct elements get distinct witnesses — so more than k reasons exist. Each contains a whole menu (Theorem 9) and there are k menus, so some menu lies inside two of them. ∎\n\nThe consequence for the page: a blocked entry of the unless form is a costlier fix, named (Section 7) — so where one printed, the footer that would announce costlier fixes in the abstract says nothing the page has not already said better, and is omitted. Where only idle verdicts printed, or none, the footer speaks for what remains unshown.\n\nFinding the owned reasons. Reasons are enumerated by removal, over the full fact set — never over a restricted pool, since a reason can contain this conflict's menu and facts other menus offer, and a pool with those held out cannot contain it:\n\nTheorem 13 (the removal walk is complete). Define walk(Π): if Π is satisfiable, stop; otherwise take any one minimal unsatisfiable m ⊆ Π, record it, and recurse on Π ∖ {x} for each x ∈ m. Started at Π = Q, the walk records every reason.\n\nProof. Induction on |Π|: every reason R′ ⊆ Π is recorded by walk(Π). If the recorded m is R′, done. Otherwise m and R′ are distinct minimal sets, so neither contains the other, and some x ∈ m has x ∉ R′; then R′ ⊆ Π ∖ {x}, and the recursion records it by the induction hypothesis. ∎\n\nThe walk is exponential in the worst case and must be run with a budget. Truncation is sound — nothing below depends on the enumeration having finished; a conflict simply explains fewer of the reasons it owns — but it is never silent: when the budget stops the walk, the report says the account may be incomplete (Section 11).","title":"5. Ownership: which reasons a conflict answers for"},{"category":"section","location":"theory/unsat-explanation/#6.-The-explanation-of-a-reason","page":"Explaining an unsatisfiable resolve","text":"Each owned reason R gets one explanation. Its shape is fixed, and this section proves the shape always exists and always carries what the fixes need.","title":"6. The explanation of a reason"},{"category":"section","location":"theory/unsat-explanation/#Blame-before-proof","page":"Explaining an unsatisfiable resolve","text":"The facts of R were determined against the registry as it is — ℛ inviolable, only Q withdrawable. This order matters: ask which facts are at fault while the registry's statements are also withdrawable, and a minimal set is free to drop a registry statement and blame a requirement in its place — \"you require C\" is no part of why a query fails when something else requires C anyway. Minimal, and a lie about what the user can do. So: blame first, on the real instance; proof second, with the blame held.\n\nThe proof half asks the same kind of question one level down. Fix R; a core for R is a minimal C_R ⊆ ℛ with C_R ∪ R unsatisfiable. (To extract one, the registry's statements must be individually assumable — see Substrate obligations. Note a core may be empty: a requirement whose package the query's own constraint leaves nothing of contradicts it with no help from the registry. Empty is a value here, not a failure.)\n\nLemma 14 (the combined set is minimal). Γ = C_R ∪ R is a minimal unsatisfiable clause set.\n\nProof. Unsatisfiable by construction. Drop a registry clause: satisfiable by minimality of C_R given R. Drop a fact f ∈ R: were C_R ∪ (R ∖ {f}) unsatisfiable then, since C_R ⊆ ℛ, the set ℛ ∪ (R ∖ {f}) would be too, contradicting the minimality of R as a reason. ∎","title":"Blame before proof"},{"category":"section","location":"theory/unsat-explanation/#The-pivot","page":"Explaining an unsatisfiable resolve","text":"Theorem 15 (the pivot theorem). Let Γ be a minimal unsatisfiable clause set and P a package in the support of some clause of Γ. Then proj_{{P}}(Γ) is a set of bounds P ∈ S₁, …, P ∈ Sₙ — unit clauses, each entailed by the subset of Γ it was derived from — with every Sᵢ nonempty and ⋂ᵢ Sᵢ = ∅.\n\nProof. By Theorem 2 (applied per elimination) the projection is unsatisfiable, and every clause of it has support inside {P} — a unit bound, or the empty clause. We rule the empty clause out. Since P is never eliminated, every resolution step unions the participants' P-literals, so by induction any derived clause d has d(P) ⊇ C(P) for every original clause C in its derivation. A derived empty clause e has e(P) = ∅, so its derivation uses only clauses with empty P-literal — a subset of Γ omitting the clause that mentions P, hence proper. By Lemma 1 that proper subset entails the empty clause, i.e. is unsatisfiable — contradicting minimality of Γ. So every projected clause is a bound with Sᵢ ≠ ∅; a set of nonempty unit bounds is unsatisfiable exactly when no value lies in all of them, i.e. ⋂ Sᵢ = ∅. ∎\n\nCorollary 16. (a) The choice of pivot is free: any package the argument touches supports the whole contradiction. (b) At least two bounds are always needed (⋂ of one nonempty set is itself). (c) At least one bound excludes ⊥ — otherwise ⊥ lies in the intersection. Read on the page: some line must force — a meet of sides that all admit \"not installed\" is no contradiction, and a report all of whose registry sides admit ⊥ has elided the registry's half of the argument.\n\nThe explanation of R at pivot P is: the facts of R, printed as the query's own lines, and a subfamily of the projection's bounds with empty intersection — the sides of the meet — each side carrying its support and route (below). Which subfamily: any with empty intersection is sound (each side is entailed; a set of true bounds with empty meet is a contradiction); prefer the smallest, then the preferences below. Theorem 18 will show no such minimisation can lose anything that matters.","title":"The pivot"},{"category":"section","location":"theory/unsat-explanation/#What-the-meet-can-look-like","page":"Explaining an unsatisfiable resolve","text":"Call a bound's version-part Sᵢ ∖ {⊥}, and call it convex when it is an interval of the version order. Registry compatibility bounds are usually convex; disconnected version-parts arise from multi-range compat entries, and from composition — a side derived along a chain can come out with a hole even from convex inputs.\n\nLemma 17 (Helly, on a line). If finitely many intervals of a total order pairwise intersect, they all share a point.\n\nProof. Let m be the greatest left endpoint, attained by interval I, and M the least right endpoint, attained by J. I ∩ J ≠ ∅ forces m ≤ M, and every interval contains [m, M]. ∎\n\nSay a family of bounds is irredundant when its intersection is empty but every proper subfamily's is not — the meets a report prints, since a redundant side would be dropped.\n\nProposition 18 (the shape of convex meets). In an irredundant family of bounds with every version-part convex: (a) there are at most three bounds; (b) if there are three, exactly one excludes ⊥, and the other two overlap only at ⊥ — their version-parts are disjoint.\n\nProof. At least one bound excludes ⊥, else ⊥ is in the full meet.\n\nAt most one excludes ⊥, given three or more bounds. Suppose S₁, S₂ both exclude ⊥ in an irredundant family of n ≥ 3. For any two indices a, b choose j ∉ {a, b}; the subfamily dropping j has a common element x_j (irredundancy), and since the subfamily retains S₁ or S₂ (it drops only one set), x_j ≠ ⊥; so x_j is a common version of all version-parts but possibly T_j, in particular x_j ∈ T_a ∩ T_b. So the version-parts pairwise intersect; by Lemma 17 they share a version, which then lies in every Sᵢ — contradicting the empty meet.\n\n(b) With n = 3 and (by the above) exactly one ⊥-excluding bound S₁: if the other two version-parts met at x, then — irredundancy giving S₁∩S₂ ≠ ∅ and S₁∩S₃ ≠ ∅, both ⊥-free since ⊥ ∉ S₁ — all three version-parts pairwise intersect, and Lemma 17 again contradicts the empty meet. So T₂ ∩ T₃ = ∅ and S₂ ∩ S₃ = {⊥}.\n\n(a) Let n ≥ 4. By the second paragraph at most one bound excludes ⊥; exactly one, say S₁. Drop any ⊥-admitting Sⱼ: the remaining family contains S₁ and at least two other ⊥-admitting bounds, and has a common element x_j ≠ ⊥. As before, for any pair a, b ≥ 2 choose j ∉ {a,b}: x_j ∈ T_a ∩ T_b, and x_j ∈ T₁ too. All version-parts pairwise intersect; Lemma 17 contradicts the empty meet. ∎\n\nSo in a convex world a meet has two sides, or the one three-sided shape: something must be installed, and the two constraints on it admit no common version — and any meet of four or more, or a three-sided meet of any other shape, is possible only through a disconnected version-part. That is why wide meets are rare, and where to look for them: only where a compat entry or a derived side has a hole. (Empirically, in the General registry, a few hundred of tens of thousands of bounded compat entries are disconnected, and genuine same-shape triples are countable on one hand. This is a remark, not a premise; nothing above depends on it.)\n\nThis is also what bounds how often a meet has to be displayed as one: two sides always read out as a chain and three need not (Proposition 22).","title":"What the meet can look like"},{"category":"section","location":"theory/unsat-explanation/#Sources:-what-a-side-rests-on","page":"Explaining an unsatisfiable resolve","text":"Say X ⊨ σ (\"X entails σ\") when every installation satisfying every clause of X satisfies σ. For a side σ of the meet:\n\na support of σ is an A ⊆ R with ℛ ∪ A ⊨ σ; minimal supports exist by finiteness (and A = ∅ is possible: a side the registry entails alone, e.g. \"the registry offers only these versions of P\");\nσ is rooted in fact f when ℛ ∪ (R ∖ {f}) ⊭ σ — no support avoids f.\n\nRootedness is a semantic notion, deliberately: a derivation that happened to use f is a witness that may over-attribute (the side might also be entailed without f), and implementations may track derivations as an approximation, but the theorems are about entailment, and entailment is one oracle query per check.\n\nTheorem 19 (source coverage — and no minimisation can break it). Let Π be any family of bounds on P, each entailed by ℛ ∪ R, with ⋂Π = ∅. Then for every fact f ∈ R, some member of Π is rooted in f.\n\nProof. R is minimal, so ℛ ∪ (R ∖ {f}) is satisfiable; let ι model it. Every side entailed by ℛ ∪ (R ∖ {f}) is satisfied by ι, and a unit bound is satisfied only at P: ι(P) lies in each such side's set. If every member of Π were entailed without f, then ι(P) ∈ ⋂Π, contradicting ⋂Π = ∅. ∎\n\nThis holds for whatever subfamily the presentation chose to print — the theorem quantifies over all of them — so trimming the meet to its smallest irredundant form cannot lose a source. Every fact of the reason is visible in every printed meet, as the root of at least one side. In particular the verification of source coverage is a sanity assertion, not a gatekeeper.\n\nA side with a minimal support of size one is separated: fully attributed to a single fact (\"requiring A forces P ∈ S, through …\"). A side whose minimal supports all have several facts is conditional, and is printed as the implication from all of them (\"with A at its forced version, B's chain forces P ∈ S\") — honest, attributed, and always available, since every side has some minimal support. So the form is total: separated where possible, conditional where not. A separating pivot is one where every printed side is separated; one need not exist, which is why separation is a preference and not an obligation.","title":"Sources: what a side rests on"},{"category":"section","location":"theory/unsat-explanation/#Choosing-the-pivot","page":"Explaining an unsatisfiable resolve","text":"Every package the core touches is a lawful pivot (Theorem 15), so the choice is made for the reader, by ranking what each candidate would print:\n\n(mandatory) the meet is printed whole — its sides listed together and their emptiness visible on its face — and includes a forcing side (Corollary 16(c) guarantees one exists);\nprefer pivots where every side rooted in a menu fact of this conflict is separated — the fixes are the sides the reader will trace, and a fix whose only side is conditional reads as a mechanism blurred;\nthen pivots where every side is separated;\nthen fewest sides;\nthen least version fracture and shortest routes (below).\n\nRanks 2–5 are preferences among sound pages; nothing below depends on how ties break, only that they break deterministically.\n\nTwo designs this replaces, and why, for the implementor tempted to rediscover them. Searching for a chain — walking from the requirements, narrowing each package by everything so far, printing one registry edge per link — cannot be made valid: each link's stated range is narrowed by packages that are no part of any minimal reason, so either the page states a narrowing no printed line accounts for, or it attributes one package's bound to another. The projection has no such gap, because elimination resolves every minimal emptying set. Deriving first and choosing afterwards — keeping a derivation graph and picking a best node out of it — confines the choice to statements the derivation happened to make and prices them by the work done rather than the page printed. Choosing the pivot first inverts both: the vocabulary is known before anything is derived, and the cost is the page.","title":"Choosing the pivot"},{"category":"section","location":"theory/unsat-explanation/#Routes","page":"Explaining an unsatisfiable resolve","text":"A side's route is a set of packages whose elimination derived it — a courtesy pointer (\"through X and Y\") telling the reader where in the registry the entailment lives. The route makes no claim: the side's truth is its supported entailment (Verification, V1), and routes carry no verification obligation of their own. Prefer short routes; order a route from the side's source toward the pivot; and where a route is long, that is the registry's length, not the presentation's — say it as a route rather than spending a line per hop.","title":"Routes"},{"category":"section","location":"theory/unsat-explanation/#Licensed-coarsening","page":"Explaining an unsatisfiable resolve","text":"Real cores carry parallel families: many statements about one set of packages, differing only in version thresholds — a staircase of couplings the argument may never need. Left as they are, the projection pays for their combinations; the report, if it survives, pays in lines.\n\nThe remedy is the resolution rule read as a merge. For a parallel family {C₁, …, Cₙ} over one package set, resolving on a package q of it — intersect at q, union everywhere else — yields one clause entailed by the family (Lemma 1), so printing it in their place keeps every line registry-true and inherits the union of their supports and routes. On the antecedent package this is exactly the join of implications: A@R₁ → B@S₁ with A@R₂ → B@S₂ gives A@(R₁∪R₂) → B@(S₁∪S₂), the antecedent literals being stored complemented.\n\nThe join is weaker than the family, so it needs a licence, and the licence is the whole criterion: the claim's clause set must still contradict with the join in the family's place — one satisfiability check, against that reason's facts and the rest of its core, per claim and never against a union of claims. No local rule can stand in for it: whether two thresholds may be treated as one depends on what happens links away, and a boundary the contradiction stands on must refuse to join while its neighbours collapse. Bisecting a family that refuses to join whole gives up exactly the boundaries the proof is standing on.\n\nTwo disciplines make the pass sound as a whole. Families are coarsened sequentially, each licence asked against the set as it stands — earlier joins included — so the invariant after every accepted join is that the whole current set still contradicts; licensed against the original set instead, two joins could each pass and jointly satisfy. And coarsening is not confluent, so the order of attempts is a stated preference, not an accident.\n\nWhere it runs: when no pivot's elimination finishes within budget, the core is coarsened and the projection tried once more — which is what makes a lockstep family of nine parallel edges projectable at all — and if the projection still fails, the coarsened core is the fallback, every join of it true and the set still contradicting.","title":"Licensed coarsening"},{"category":"section","location":"theory/unsat-explanation/#7.-Fixes-on-the-page","page":"Explaining an unsatisfiable resolve","text":"The two halves now connect, with no machinery at the joint.\n\nTheorem 20 (fix visibility). In any presentation meeting contract (C1), every entry of every menu is a fact of every reason its conflict owns; hence in every explanation printed under that conflict — at any pivot, after any minimisation — the entry roots at least one side; and the sides not rooted in it have nonempty intersection.\n\nProof. An owned reason contains the whole menu (Theorem 9), so the entry's fact f is in R; Theorem 19 gives a side of the printed meet rooted in f. For the last part: the sides not rooted in f are each entailed by ℛ ∪ (R ∖ {f}), and the model ι from Theorem 19's proof puts ι(P) in all of them. ∎\n\nThat is the reading a report should support typographically: each fix names a fact; that fact is the root of at least one visible side; cover those sides with a thumb and the remaining bounds visibly agree on a version (or on ⊥). Why each fix works is a corollary of the form, not a feature to build.\n\nTheorem 21 (witness coherence). Let a fix be taken: entry e of menu Dᵢ, together with one chosen entry of every other menu — a selection, whose pick-set W is a repair by (C1). Let ι be any solution of the withdrawn query (one exists), e.g. the resolved witness the report shows. Then for every explanation printed under any conflict, ι(P) lies in every side entailed by ℛ ∪ (R ∖ W); in particular the witness lands inside the meet opened by the withdrawal.\n\nProof. ι models ℛ ∪ (Q ∖ W), and R ∖ W ⊆ Q ∖ W, so ι satisfies every side entailed by ℛ ∪ (R ∖ W); a unit bound on P is satisfied only at P. ∎\n\nNote the theorem is stated against the full withdrawal W, not the single entry e — necessarily so: an owned reason can contain other conflicts' facts (shared reasons contain their whole menus; ordinary ones may contain other menus' unpicked entries), and the witness respects only the sides whose supports survive everything withdrawn. The display rule that goes with it: the versions shown for a fix (\"allows:\") always include the pivots of the conflict's printed meets, so the reader sees the witness land where the opened meet says it can.\n\nVersions come from a resolve. No version in a report — witness or otherwise — is ever read off the failed instance or any model found during diagnosis. A model witnesses satisfiability; which versions the user would get is the resolver's optimising answer for the withdrawn query, computed by the substrate (S4). Diagnosis decides what is true; the resolver decides what is chosen.","title":"7. Fixes on the page"},{"category":"section","location":"theory/unsat-explanation/#Blocked-fixes:-which,-and-how","page":"Explaining an unsatisfiable resolve","text":"The page's own lines make some actions tempting: the reader sees \"your compat leaves QuantumLattices 0.15.4\" and asks why relaxing it is not offered. A tempting action is an action on a package a conflict's lines or heading name that appears nowhere in the cover. The blocked-fixes section answers for exactly these — one sentence per action, so nothing is said twice and nothing tempting goes unanswered.\n\nWhat the sentence says is decided by solves, not judged, and the question must be well posed: x is load-bearing when it lies in some minimal repair, idle when it lies in none. Asking of one repair a solver happened to return is not enough — a tie between equal repairs could call the same action idle in one sentence and a rescue in the next, which is exactly the inconsistency this phrasing exists to avoid. So the search favours x: take a repair W ∋ x within the bound, shrink its other members while it still repairs, and ask whether x too can go. If it cannot, the shrunk W is a minimal repair carrying x and x is load-bearing; the sentence names its price, \"«x» would not help unless you also «W ∖ {x}».\" If x can go, that witnesses a smaller repair without it — block W and try again, a few rounds, before concluding idle and printing the flat \"«x» does not help.\" Because the cover holds every cheapest repair, a tempting x is in none of them, so a load-bearing x's minimal repair is always strictly costlier than k (Lemma 28), and idle means dead weight in every repair within reach.\n\nOne repair, said once. Two tempting actions can be load-bearing in the same minimal repair — each insufficient alone, the repair carrying both. Mirroring it from each end (\"relax A unless you also drop B\" beside \"drop B unless you also relax A\") says one thing twice and reads as a contradiction. So entries are grouped by the repair they exhibit: the actions that share one are one entry, said as the choice they are not: \"«x₁» or «x₂» would only help if you do both.\" An entry of this form still exhibits a costlier fix, so it suppresses the footer exactly as an unless-entry does.\n\nLemma 28 (completions never restate the cover). A load-bearing completion W ∖ {x} contains no printed fix.\n\nProof. A correction set's supersets are correction sets, so if W ∖ {x} contained a fix it would itself repair — and then x is idle in W, not load-bearing. ∎\n\nSo the unless-sentences and the cover cannot duplicate one another: the cover lists the cheapest fixes, and each unless-sentence exhibits a strictly costlier one in which its action genuinely participates. That sharpens the footer rule of Section 5: an unless-entry names a costlier fix, so where one printed, the costlier-fixes footer says nothing new and is omitted; idle verdicts name none, and do not suppress it.\n\nNone of this consults the reason walk: blocked fixes are questions about repairs, and the completed cover plus one bounded search per tempting action decides them. The walk's remaining duty is the conflicts' own stories.","title":"Blocked fixes: which, and how"},{"category":"section","location":"theory/unsat-explanation/#8.-Verification","page":"Explaining an unsatisfiable resolve","text":"What a checker checks, and what it need not.\n\n(V1) Side truth. For every printed side σ with printed support A: ℛ ∪ A ⊨ σ. One entailment query each (equivalently: ℛ ∪ A plus the negation of σ — the assertion that P takes a value outside σ's set — is unsatisfiable).\n(V2) Visible closure. The printed sides of each meet intersect emptily. Pure set arithmetic on the page; no solver.\n(V3) Source coverage. Every fact of the reason roots a printed side. Guaranteed by Theorem 19 once V1 and V2 hold; assert it as a sanity check on the rootedness bookkeeping, not as a gate.\n(V4) Cover contract. Every selection's pick-set is in F_min, and every member of F_min is some selection. What the page offers is defined over blocks: a block's own repairs are one entry from each of its conflicts' menus in every combination, together with every selection of each alternative to it, and a repair of the query is one of each block's, in every combination. By construction (Theorem 6's check; a generalized rectangle's selections are members by definition; Theorems 29 and 30 for the layers and the blocks) — and asserted per report, since it is a set comparison over at most |F_min| members and costs nothing.\n(V5) Witness coherence. For each fix's witness and each printed meet: the witness's pivot value lies in every side whose printed support avoids the withdrawn facts. Membership tests; no solver. Silent breakage here is invisible to every other check, which is exactly why this one exists.\n(V6) Disclosures. The menu wording matches the decided questions (Section 4's table), its alternative row read block by block; the alternatives appear exactly when some block's cover has more than one layer, one per layer after the leading one, and their entries' witnesses are checked like any entry's (V5); the headline's \", pick a fix for each\" appears exactly when there is more than one conflict; the enumeration-cut sentence appears exactly when the repair enumeration was truncated and the deciding solve found more; the costlier-fixes footer appears exactly when Theorem 5 answered yes and no unless-entry printed; the reason-walk truncation sentence appears whenever that walk was cut short.\n\nEvery check is per-explanation, never against a union. Where two explanations' line-sets are S₁ ∪ S₂ and S₂ alone is contradictory, the union stays contradictory whatever is deleted from S₁ — a union-level check would pass a page that silently destroyed the whole account of S₁. This is the one failure mode in this design that is silent by nature; the rule exists to make it impossible rather than detectable.","title":"8. Verification"},{"category":"section","location":"theory/unsat-explanation/#9.-Rendering","page":"Explaining an unsatisfiable resolve","text":"Rules, not preferences — each guards a truth-condition or an attribution:\n\nA side is stated from its support. The requirements the conflict answers for are said by its heading and nowhere else; the reason's other facts print once, as the query's own lines (\"your compat leaves A 1.2\"). Each side prints as the implication from its support's packages to its bound, with its route in parentheses. A conditional side names all of its support (\"A 1.2 and B ≥ 2 together …\"). This is what makes every fix traceable: the fact a fix withdraws is the stated antecedent of a visible side.\nThe user's constraints are named as the user's. Which constraint kinds removed which versions is read directly off the query (S5) — no solver, no license needed — and only query lines may say \"your\"; everything else is the registry's, and a registry statement never attributes a bound to a Project.toml it cannot see.\nThe verb is read off the printed consequent — see the verb table below. Never carried beside the clause: a line said the other way round re-derives its own verb, which is what makes contraposition and composition safe to print.\nDirection is chosen at print time. A clause has no direction; state a two-package statement from the side that declares the dependency edge where exactly one does, and symmetrically (\"X and Y are incompatible\") where neither. Among otherwise equal readings prefer the one whose stated consequent has versions to name: a consequent with an empty range prints shortest and says least, and choosing it throws away the very bound the reader was about to hold against another. A line that continues a chain overrides all of this: it is said to the package the chain has not reached, whichever that is.\nA range is printed when something narrowed it. An antecedent that is a package's entire offering prints as the bare package name; naming the full range reads as a narrowing that never happened.\nReading order: a proof is one chain. A proof prints as a polysyllogism — a root fact, then implications each arguing from what the line above it left, ending at the fact that contradicts the accumulated bound. The root is the heading's own subject, said as the user's compat where the reason narrowed it and taken as the heading's premise where it did not; a subject the statements only conclude about is where the chain ends instead, so a source is preferred to it. A line may argue only from packages the query named or a line above introduced, and the query's fact about a package prints where the package arrives: after the line that reaches it, before the line that argues from it. Prefer to close at a printed compat line where one contradicts the chain; otherwise the last line closes against the heading's implicit given the rest of the requirements, and the package it leaves nothing of is named in no further line. A line continuing through a package reads its antecedent against the bound the chain left there — widening its literal by everything already ruled out weakens the clause, so the line stays true and says what the reader is holding. Only a meet of three or more sides breaks the chain (Proposition 22), and that alone prints whole, its sides adjacent.\nThe heading names the primary reason's requirements, and claims nothing. \"Conflict 1: QuantumLattices\" — the colon form says these packages are the conflict's core and stops. A sentence would either claim too much (\"cannot be satisfied\" is false absolutely, true only under the unstated given the rest) or spell the context out at absurd length; the bare form is as clear, shorter, and never wrong. The one sentence heading that survives is the absolute truth: \"no version of X is available.\" Where two conflicts share a root requirement their headings would read as one conflict said twice, so each is extended with the package it contradicts at — the package its own lines name and the others' do not, taken where the chain closes — which leaves the heading a true bare list and stops it reading as a duplicate. The checkers still premise every requirement the conflict answers for: what shrinks is the sentence, not the claim set.\nBlocked fixes answer for actions, one sentence each. For every tempting action — named by the conflict's lines or heading, in no fix of the cover — the section prints its solve-decided verdict (Section 7): an idle action gets \"«x» does not help.\", a load-bearing one gets \"«x» would not help unless you also «W ∖ {x}».\" Action-indexed, so nothing is said twice; solver-licensed, so nothing is judged; and no proof prints — why a fix is not offered is second-order, and the verdict has already said it.\nThe headline tells the reader what to do. With more than one conflict it reads \"Unsatisfiable — N conflicts, pick a fix for each:\". That is an instruction, and a true one: by hitting-set duality every solution resolves every conflict (Theorem 9), and one entry from each menu, in every combination, is a cheapest repair (Theorem 6, or the leading layer's exactness). It is not a claim that those are the only cheapest repairs, and it must not be read as one where an alternative exists — an alternative is a cheapest repair taking no entry of some conflict's menu (Lemma 31). The page does not leave that to the reader: every alternative opens with \"Or, to fix …\", so the instruction and its exceptions stand side by side, the instruction first because it is the route the conflicts argue for.\nAn alternative prints after the conflicts, as fixes and not as a conflict. Where a block's cover has more than one layer, each layer after the leading one prints after the last conflict, introduced by what it declines: \"Or, to fix without dropping requirement Knet:\" where every menu it avoids offers a single fix (their gerunds joined by or: without either), \"Or, to fix without any of the fixes for Conflicts 1 and 3:\" where one of them offers a choice, and \"Or, to fix another way:\" in the case Lemma 31 does not rule out, where the layer's selections avoid different menus and none of them all. Then what it offers: its menus of one entry said as one thing to do, joined by and, since they are all to be done; a menu with a choice numbered like a conflict's menu, \"…, and one of:\", one witness under each entry, since an entry there completes the whole alternative; a single line and one witness where the layer leaves no choice at all; and, where several menus offer a choice, \"settle each of these:\" with one bullet per menu and its entries joined by or. Each witness is taken with the layer's other menus at their first entries and every other block settled the first way its menus offer. No proofs print there: reasons do not layer (Corollary 25), and the conflicts above have already explained every one. An alternative replaces the menus of its own block and nothing else — the rest of the report stands as printed, which is what its label says.\nA coupled factor prints as a choice of whole options. Where a menu's entries are compound — a factor the family couples, so that no one action of it is a choice on its own — each entry names the whole of what it asks for: \"relax both, or relax P and drop T, or drop both\", never a list of actions the reader has to recombine. An entry is a thing to do, and a menu is a choice between things to do, at every arity.\nFooters. Costlier fixes also exist. prints only when Theorem 5 answered yes and no unless-entry printed — an unless-entry exhibits a costlier fix (Lemma 28 says it never merely restates a cheap one), while an idle verdict exhibits none and does not suppress the news. There are more minimal fixes than are shown. prints only when the repair enumeration hit its cap and the deciding solve confirmed more. The reader is told about every gap in the page's own vocabulary — fixes, not solutions.\nMenu wording. Exactly the three-state table of Section 4.","title":"9. Rendering"},{"category":"section","location":"theory/unsat-explanation/#The-verbs","page":"Explaining an unsatisfiable resolve","text":"Four forms, and which one is printed is data in the clause rather than a flag beside it. Write S for the literal on the package a line is said to — its printed consequent — and read ⊥ ∈ S as \"this statement is satisfied by the package not being installed\".\n\nform when what it says\nyour ‹kinds› leaves X r the line is one of the query's own facts of X, the query's own constraints of those kinds left r — and only such a line may say your\n… requires X r ⊥ ∉ S X is installed, at one of r\n… constrains X r ⊥ ∈ S, some version in S if X is installed it is at one of r; the statement is silent if it is not\n… leaves no version of X S = {⊥} X is not installed at all\n\nLeaves is the user's; requires forces; constrains binds only what is there; leaves no version of is forced-empty. The bare package name is a consequent whose range is the package's whole offering, never a consequent with nothing in it — the fourth row exists so that a flipped line cannot print as the package arriving when what it says is the package's going.\n\nA trailing (through P, Q and R) is the whole of the difference between a statement the registry makes directly and one an elimination composed: the packages the route passed through, carrying no claim of their own (Routes, Section 6). Its absence says the statement stands as it is.\n\nBecause the verb follows the printed consequent, flipping a line re-derives it. That is what makes the next result safe to print.","title":"The verbs"},{"category":"section","location":"theory/unsat-explanation/#Linearization","page":"Explaining an unsatisfiable resolve","text":"Proposition 22 (two sides linearize; three need not). A meet of two sides prints as one chain. A meet of three pairwise-overlapping sides does not.\n\nProof. A clause has no direction (Section 2), so which of its packages is the printed consequent is chosen when it is printed, and the contrapositive is the same clause said the other way. Let the sides at pivot P be S₁ and S₂. Say S₁ forward, from the packages its support names to the bound it puts on P; the reader now holds P ∈ S₁. Say S₂ to the package it rests on, with P as its antecedent: what the chain has left at P — S₁, narrowed by the query's own line about P where there is one — meets S₂ in nothing, since the family closes, so everything still open at P denies S₂ and the line fires on exactly what the lines before it left. That is the resolution rule of Section 2, read out one step at a time, and the chain ends at S₂'s own root fact. With three sides the same walk cannot be arranged: one side opens the chain and one closes it, and the third has both of its ends already spoken for — its support is introduced by nothing the chain has said. The page is then two chains meeting at P, which is what the meet display says in one place rather than leaving the reader to find. And three sides can genuinely be needed: {1,2}, {2,3}, {1,3} overlap pairwise and have empty intersection, so no two of them contradict and no pair stands for the family. ∎\n\nThe pivot's own facts are not sides in this counting: a query line about P is not an implication needing a root, and prints where the chain reaches P — which is why \"the compat leaves P r, and the one side says P s\" is a chain of two lines and not a meet at all. So the meet display survives exactly at three sides and more, and Proposition 18 says how rare that is: in a convex world an irredundant meet has at most three sides, and three of them require a package one of whose bounds has a hole in it.","title":"Linearization"},{"category":"section","location":"theory/unsat-explanation/#10.-Substrate-obligations","page":"Explaining an unsatisfiable resolve","text":"What the surrounding resolver must provide. Each is one interface; none constrains how.\n\n(S1) Assumable query facts. Satisfiability of ℛ ∪ X for arbitrary X ⊆ Q, with each fact independently assumable, plus the bounded/blocked oracle of Theorem 4 and MUS extraction (any minimal unsatisfiable subset of an unsatisfiable assumption set).\n(S2) Assumable registry statements. A second instance over the same universe in which each clause of ℛ is individually assumable (e.g. via a selector per clause), for extracting cores C_R; the query facts are held, never selectable, while it is used. Built for diagnosis only — no resolve pays for it.\n(S3) Entailment queries. ℛ ∪ A ⊨ σ for A ⊆ Q and unit bounds σ (reducible to S1/S2-style satisfiability of the negation).\n(S4) Resolution of withdrawals. For any repair W, the resolver's actual solution of the withdrawn query — the versions a user would get — with the guarantee that the universe the diagnosis ran on answers for every withdrawal of Q it is asked about (in the present architecture this is the relaxation-stability of the filtered universe; whatever the architecture, the guarantee is the obligation).\n(S5) Attribution data. For each package, which of the query's constraint kinds exclude which versions — readable without any solver.\n(S6) The version order, per package, for rendering ranges and for Proposition 18's convexity talk.","title":"10. Substrate obligations"},{"category":"section","location":"theory/unsat-explanation/#11.-Boundaries-and-honesty","page":"Explaining an unsatisfiable resolve","text":"Three enumerations in this design can be cut short, and each has a required disclosure; nothing else in the design is allowed to be incomplete.\n\nThe reason walk (Theorem 13) is exponential. Truncation costs completeness of each conflict's account, never soundness — and the report must say the account may be incomplete when it happens.\nRectangle coverage can be below |F_min|; the report says other equally cheap solutions exist (C3), and a one-entry menu then says \"One fix:\", never \"The only\".\nBeyond the cheapest: whether strictly larger repairs exist is one oracle query (Theorem 5) and its answer sets the wording; exploring those larger repairs is out of scope by design, and the report says they exist without pretending to enumerate them.\n\nCosts worth knowing, none load-bearing: finding k is a handful of bounded solves (k is small in practice); enumerating F_min is one solve per member; one core per owned reason; one projection per candidate pivot, each a local elimination over a core's worth of clauses, with a work cap whose only correct response on overflow is to fall back to printing the core itself — a true, unshaped account being better than a shaped, partial one.\n\nFinally, the two free-choice results deserve restating together, because they bound the design space from both ends. The pivot theorem says the explanation's anchor is entirely free — any package the argument touches can carry it, so choose for the reader. The canonicity theorem says the menus are entirely forced — the repair family admits exactly one presentation, so there is nothing to choose. Everything else in a report lives between those two poles, and every preference in this document lives on the pivot side of the line.","title":"11. Boundaries and honesty"},{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Diagnostics","page":"API","text":"What a Diagnosis is made of. All of it is plain data, so a caller can render its own report without asking the resolver anything further.","title":"Diagnostics"},{"category":"section","location":"api/#The-statements-a-report-is-made-of","page":"API","text":"","title":"The statements a report is made of"},{"category":"section","location":"api/#How-a-report-is-arrived-at","page":"API","text":"","title":"How a report is arrived at"},{"category":"section","location":"api/#What-a-report-may-be-checked-against","page":"API","text":"","title":"What a report may be checked against"},{"category":"section","location":"api/#Internals","page":"API","text":"","title":"Internals"},{"category":"function","location":"api/#Resolver.resolve","page":"API","text":"resolve(data, prob::Problem; by = identity, diagnose = true)\n    -> Union{Dict{P,V}, Diagnosis{P,V}, Nothing}\nresolve(data, reqs; by = identity, diagnose = true)\n\nResolve the requirements reqs against the package universe described by data, returning the optimal solution as a dict mapping each needed package to its chosen version, or a Diagnosis when the requirements are not jointly satisfiable. The solution covers every requirement and is exactly the dependency closure of the requirements under its own chosen versions.\n\nA Diagnosis says which requirements cannot hold together, why, and what the user could change to make them; showing it prints the report. Pass diagnose = false for the bare nothing instead, which is what a caller that only wants the verdict should do — the diagnosis costs several more solves and one resolve per fix on the menus it offers.\n\nA Problem additionally constrains the admissible versions with user constraints; the answer is the one the same universe with those versions deleted would give. The requirements form is the unconstrained problem — a constrained resolve is spelled by building the Problem.\n\nThe returned solution is the layered solution: requirements are optimized first, in the priority order induced by by (each package maximized to its best feasible version given the choices already made), then the dependencies of the chosen versions, layer by layer along the dependency graph. See the manual's Theory section for the precise semantics and the guarantees it carries.\n\ndata may be a DepsProvider, a dict of PkgData, a dict of PkgInfo, or a SAT instance. The SAT method takes bare requirements and so has no constraints to attribute a failure to: it answers nothing, and accepts restore::Bool = true instead of diagnose — when true the instance's clauses are restored before returning so the instance can be reused, and restore = false is faster for single-use instances.\n\nThe other methods accept one more keyword:\n\norder: the version preference ordering, as a callable mapping a package to a lt comparator over its versions (\"is preferred to\"). The default, nothing, means the order the universe already lists them in — which for a T1 artifact (see pkg_info) is the canonical one the registry state fixes. The ordering is not a constraint: it selects among the valid solutions rather than changing which ones are valid, so it is a parameter here instead of part of the Problem, and one artifact serves every ordering.\nThe universe is built out of interchangeability classes — sets of versions nothing in the registry can tell apart (see pkg_info) — so order decides two things: which class the answer picks, and which member of that class it names, namely the best one the query admits.\n\n\n\n\n\n","title":"Resolver.resolve"},{"category":"function","location":"api/#Resolver.issatisfiable","page":"API","text":"issatisfiable(data, reqs) -> Bool\n\nCompute resolve(data, reqs; ..., diagnose = false) !== nothing more efficiently, with a single satisfiability check instead of a full optimizing resolve. Use this when you want to know whether the requirements can be satisfied but don't need an actual solution — or a report on why not.\n\ndata may be a DepsProvider, a dict of PkgData, a dict of PkgInfo, or a SAT instance, and a Problem may be passed in place of the requirements and keywords, as with resolve. The by and order keywords are not accepted since they affect which solution resolve picks, not whether one exists. The SAT method leaves the instance unchanged, so it can be reused.\n\n\n\n\n\n","title":"Resolver.issatisfiable"},{"category":"type","location":"api/#Resolver.Problem","page":"API","text":"Problem(reqs; compat = ..., pin = ..., $kind = ...)\n\nA resolution problem: the required packages reqs, plus the constraints that say which versions are admissible. A Problem carries everything that determines satisfiability; orderings (the by priority order, the order version rank order) are resolve parameters instead.\n\nEvery keyword is a constraint, and its name is the constraint's kind:\n\ncompat: per package, the set of allowed versions (queried with in).\npin: per package, the one version it is held at.\nanything else: a predicate (p, v) -> Bool, true for the versions that kind forbids — \"no prereleases\" is the one the resolver's own tooling uses. These speak about versions rather than about particular packages, so they reach every package.\n\nTwo constraints cannot share a kind, which needs no checking: a repeated keyword is a syntax error.\n\nConstraints for packages that don't exist, and constraints that exclude nothing, are allowed and have no effect.\n\nEvery constraint here is a constraint on a shared package universe rather than a deletion from a private one: that is what lets a single T1 artifact (see pkg_info) serve queries that admit different things, and what lets diagnostics eventually name the kind that ruled a version out.\n\n\n\n\n\n","title":"Resolver.Problem"},{"category":"type","location":"api/#Resolver.Diagnostics.Diagnosis","page":"API","text":"Diagnosis\n\nWhat resolve answers when the query cannot be satisfied: the conflicts, every one of which every solution resolves, the alternatives to whole blocks of them, and what all of that leaves out.\n\nThe conflicts fall into independent blocks, and a block's repairs are one entry from each of its conflicts' menus in every combination, together with every selection of each Alternative naming it. A repair of the query is one of each block's, and between them they are every repair as cheap as the cheapest — see selections. So others is about what costs more: :none when nothing outside them exists at all, :larger when what is outside gives up more, and :some when the enumeration of the cheapest repairs was cut short and one further solve found one it never reached. truncated records that the search for reasons was cut short, so the account of some conflict may be incomplete.\n\nshowing one prints the report.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Diagnosis"},{"category":"module","location":"api/#Resolver.Diagnostics","page":"API","text":"Resolver.Diagnostics\n\nWhy an unsatisfiable query fails, and what its user could change.\n\nA report owes three things: the fixes — how many independent things are wrong and, for each, the menu that settles it, such that any combination of choices, one per conflict, repairs the query while giving up as little as any repair can; the explanations — for each conflict, which of the user's own facts collide, at which package, and through which of the registry's statements; and the witnesses — what taking each fix yields.\n\nThe facts a query is made of are per package: requiring it, and the query's own limits on which versions of it are admissible. Package granularity is the finest grain that stays a user action — no edit gives back one excluded version while keeping its siblings excluded. A reason is a minimal unsatisfiable set of facts, a repair a minimal set whose withdrawal is satisfiable, and the two are hitting-set duals; the conflicts are the menus the cheapest repairs decompose into, each explaining a reason of its own, and where a group of them that stand or fall together has cheapest repairs their product does not reach, those print after the conflicts as alternatives to the whole group.\n\nSee the manual's Explaining an unsatisfiable resolve for the theory this implements, including the proof of every claim above.\n\n\n\n\n\n","title":"Resolver.Diagnostics"},{"category":"type","location":"api/#Resolver.Diagnostics.Conflict","page":"API","text":"Conflict(reqs, lines, versions, excluded, fixes, blocks = [])\n\nOne independent thing that is wrong: the requirements it answers for, the lines that prove it, the version list each named package is spoken of in, which of the query's constraint kinds exclude which of those versions, the menu of fixes that settles it, and the verdict on each action the page makes tempting.\n\nA conflict is one menu: choose one entry of fixes and this conflict is settled. An entry may ask for several actions at once, where the family couples them and no one of them is a repair without the rest. Where a block of the query's cheapest repairs is not the product of its conflicts' menus, what the product misses is not hidden inside a conflict: it is an Alternative on the Diagnosis, printed after the conflicts and replacing the menus of the block it belongs to.\n\nAn action is tempting when this conflict's lines or heading name its package and no fix anywhere on the page takes it: the reader can see it and cannot see why it is not offered. Each entry of blocks answers for one or more such actions at once: tried holds one action-bundle per tempting fact (a lift of several kinds is one bundle), and unless the rest of a minimal repair that carries them all, empty where the action is in no minimal repair the search could reach. One tried bundle prints \"does not help.\" or \"would not help unless you also …\"; several print, joined by \"or\", \"would only help if you do both\" — they exhibit one repair, each insufficient alone, and the page says the repair once rather than mirroring it from every end. Every verdict is the solver's: a bounded search for a minimal repair through the action, shrunk in the action's favour so a tie between equal repairs cannot call the same action idle in one sentence and a rescue in the next.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Conflict"},{"category":"type","location":"api/#Resolver.Diagnostics.Alternative","page":"API","text":"Alternative(conflicts, avoided, menus)\n\nAnother way of settling a whole block of conflicts at once — the cheapest repairs of that block which taking one entry from each of its conflicts' menus does not reach.\n\nconflicts are the indices of the conflicts it replaces, which are exactly one block of the query's independent factors; the rest of the report stands as printed, settled the way its own menus offer. menus is a product: settle each of them, one entry apiece, and every combination of those is a repair as cheap as any there is. avoided are the replaced conflicts — as indices again — whose menus every selection of this alternative misses entirely, which is what the page says the alternative is instead of.\n\nNo proof prints under an alternative: reasons attach to the conflicts and do not layer (Corollary 25 of the theory page), so what it owes the reader is its menus and a witness under every entry.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Alternative"},{"category":"function","location":"api/#Resolver.Diagnostics.selections","page":"API","text":"selections(c) :: Vector{Vector{Action{P}}}\n\nEvery repair c offers: its menu's entries, one by one.\n\n\n\n\n\nselections(a) :: Vector{Vector{Action{P}}}\n\nEvery repair a offers: one entry from each of its menus, in every combination.\n\n\n\n\n\nselections(d) :: Vector{Vector{Action{P}}}\n\nEvery repair of the query the report presents: one of each block's, in every combination. A block's own are one entry from each of its conflicts' menus, together with every selection of each alternative to it. Between them these are exactly the repairs as cheap as the cheapest — nothing offered that is not one, and none of them left unsaid.\n\n\n\n\n\n","title":"Resolver.Diagnostics.selections"},{"category":"type","location":"api/#Resolver.Diagnostics.Fix","page":"API","text":"Fix(actions, solution)\n\nOne entry of a menu: the actions to carry out, and what the resolver answers once they are — with everything else on the page settled the first way it offers, the other menus of this entry's own layer included. An entry may ask for several actions at once, where the family couples them and no one of them is a repair without the rest. The versions are the resolver's optimising answer for the withdrawn query, never a model found during diagnosis: diagnosis decides what is true, the resolver decides what is chosen.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Fix"},{"category":"type","location":"api/#Resolver.Diagnostics.Action","page":"API","text":"Action(kind, pkg)\n\nOne thing a user could do: :drop a requirement on pkg, or lift the constraint of kind kind for pkg. The kinds are the query's own (Problem), so an action is always something the reader can carry out by editing what they wrote.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Action"},{"category":"function","location":"api/#Resolver.Diagnostics.action_phrase","page":"API","text":"action_phrase(a::Action) :: String\n\nOne action, said as something the reader could carry out. Whatever a constraint kind is called inside the resolver, what it reads as here is an edit.\n\n\n\n\n\n","title":"Resolver.Diagnostics.action_phrase"},{"category":"type","location":"api/#Resolver.Diagnostics.Line","page":"API","text":"Line(clause, through, given, proof = 0, pivot = nothing)\n\nOne printed statement. clause is the whole of what it says; through names the packages an elimination reached it by — a courtesy pointer, carrying no claim of its own, since the page shows what a line says and not the elimination that reached it. given marks the query's own facts, which every proof on the page shares; proof numbers the reason a derived line argues for, and pivot the package its meet is taken at.\n\n\n\n\n\n","title":"Resolver.Diagnostics.Line"},{"category":"function","location":"api/#Resolver.Diagnostics.diagnose","page":"API","text":"diagnose(sat, prob, univ; by, order) :: Diagnosis\n\nWhy prob cannot be satisfied on univ, and what its user could change. The instance is left exactly as it was found, so a caller may go on using it.\n\nRequirements the universe holds nothing of are their own conflicts: nothing this query did took them away, so dropping them is all that could help. What is left is settled on the facts — the requirements and the query's own version limits — and one relaxation is resolved per fix on the menus offered.\n\n\n\n\n\n","title":"Resolver.Diagnostics.diagnose"},{"category":"module","location":"api/#Resolver.Clauses","page":"API","text":"Resolver.Clauses\n\nThe statements a report is made of, and the one rule that composes them.\n\nA literal on a package is a set of its versions, possibly together with ⊥ (\"not installed\"); a clause carries at most one literal per package and is satisfied when some package takes a value its literal admits. Every constraint either actor supplies — the registry's dependency edges and compatibility bounds, the query's requirements and its own version limits — is one clause of this shape, and the six readings of the shape are what clause_phrase prints.\n\nClauses compose by resolution on a package (resolve_on): intersect that package's literals, union every other package's. Whether a statement forces a package or merely bounds it is whether ⊥ is in the consequent — data in the clause, never a flag beside it — so a rule written for one reaches both, and no direction has to be carried: a clause has none, and which way it is said is chosen when it is said.\n\n\n\n\n\n","title":"Resolver.Clauses"},{"category":"type","location":"api/#Resolver.Clauses.Clause","page":"API","text":"Clause{P}(lits)\n\nOne statement: at most one literal per package, in package order, satisfied by an installation that gives some package a value its literal admits. Equal statements are ==, which the ordering and the dropping of empty literals is what buys.\n\nBuild one with clause, which normalizes; the constructor takes the normal form as given.\n\n\n\n\n\n","title":"Resolver.Clauses.Clause"},{"category":"function","location":"api/#Resolver.Clauses.literal","page":"API","text":"literal(n, versions, complement = false; absent = false) :: Lit\n\nThe literal on a package of n versions admitting versions (indices into its version list), or everything but them when complement, together with ⊥ when absent. The complement is taken over versions alone: ⊥ is put in or left out by absent, whichever way the version part was named.\n\n\n\n\n\n","title":"Resolver.Clauses.literal"},{"category":"function","location":"api/#Resolver.Clauses.resolve_on","page":"API","text":"resolve_on(c, d, q) :: Union{Clause{P}, Nothing}\n\nThe resolvent of two clauses on q, where there is progress to be had: nothing when either says nothing about q, when the intersection at q is no smaller than a parent's literal — the resolvent is then that parent, weakened, and a weakening is not a derivation — or when what comes out is a tautology.\n\n\n\n\n\n","title":"Resolver.Clauses.resolve_on"},{"category":"function","location":"api/#Resolver.Clauses.resolve_raw","page":"API","text":"resolve_raw(clauses, q) :: Union{Clause{P}, Nothing}\n\nThe resolvent of clauses on q: q's literal is their intersection, every other package's their union. Sound whatever the clauses are — a model of all of them models the resolvent — so everything derived inherits soundness rather than re-establishing it. nothing when the result is a tautology.\n\nA clause not mentioning q contributes the empty literal there, as it should: saying nothing about q is saying q can be nothing.\n\n\n\n\n\n","title":"Resolver.Clauses.resolve_raw"},{"category":"function","location":"api/#Resolver.Clauses.resolve_all","page":"API","text":"resolve_all(clauses, q) :: Union{Clause{P}, Nothing}\n\nresolve_raw over a whole family at once, declining the same two non-derivations resolve_on declines. Stated for families because literals are sets: three of them can have empty intersection while every two overlap, so pairs do not suffice to empty a package.\n\n\n\n\n\n","title":"Resolver.Clauses.resolve_all"},{"category":"function","location":"api/#Resolver.Clauses.subsumes","page":"API","text":"subsumes(c, d) :: Bool\n\nDoes c say at least as much as d — c(p) ⊆ d(p) at every package? Then every model of c models d, so printing both says nothing the stronger one did not.\n\n\n\n\n\n","title":"Resolver.Clauses.subsumes"},{"category":"type","location":"api/#Resolver.Clauses.Lit","page":"API","text":"Lit(bits)\n\nA set of values a package may take: bits[i] for its i-th version and bits[end] for ⊥. The trailing slot is why eachindex runs one past the version list — a literal is a subset of V(p) ∪ {⊥}, and ⊥ is a value like any other.\n\n\n\n\n\n","title":"Resolver.Clauses.Lit"},{"category":"function","location":"api/#Resolver.Clauses.clause","page":"API","text":"clause(pairs) :: Union{Clause{P}, Nothing}\n\nThe clause with these literals, normalized: literals nothing can satisfy are dropped (they are no disjunct), entries are put in package order so that equal statements are ==, and repeated packages are merged by union. nothing when some package's literal admits everything — the clause is then a tautology, and a tautology is not a statement.\n\n\n\n\n\n","title":"Resolver.Clauses.clause"},{"category":"function","location":"api/#Resolver.Clauses.isbottom","page":"API","text":"isbottom(c) :: Bool\n\nIs c the empty clause — the contradiction? A clause with no literal left has no way to be satisfied, which is what deriving one proves.\n\n\n\n\n\n","title":"Resolver.Clauses.isbottom"},{"category":"function","location":"api/#Resolver.Clauses.version_order","page":"API","text":"version_order(versions) :: Int\n\nWhich way a package's version list runs: 1 ascending, -1 descending, 0 neither. A range prints as ≤v or ≥v only where one end of the list is known to be one end of the order; where it is not, the versions are named.\n\n\n\n\n\n","title":"Resolver.Clauses.version_order"},{"category":"function","location":"api/#Resolver.Clauses.range_phrase","page":"API","text":"range_phrase(vs, sel) :: String\n\nThe versions sel picks out of vs, low to high. The empty string where sel is everything: a range that is the package's whole offering prints as the bare package name, since naming it in full reads as a narrowing that never happened.\n\n\n\n\n\n","title":"Resolver.Clauses.range_phrase"},{"category":"function","location":"api/#Resolver.Clauses.clause_phrase","page":"API","text":"clause_phrase(c, vers, names = letters; subject = nothing) :: String\n\nThe clause as one English sentence. vers maps a package to its version list and names to its name; subject names the package to say the statement to, which is otherwise chosen (see default_subject).\n\nSix readings of one shape cover everything a report says: a package that must be installed, a package a range of which cannot be, and — with two or more literals — one package's versions requiring another's, constraining another's, or leaving another none at all. Which verb is not a flag: requires is a consequent that excludes ⊥ and so brings the package in, constrains one that admits it and so binds only if it is there, and leaves no version of one that admits ⊥ alone. So a statement said the other way round re-derives its own verb, which is what makes contraposition safe to print.\n\n\n\n\n\n","title":"Resolver.Clauses.clause_phrase"},{"category":"function","location":"api/#Resolver.Clauses.letters","page":"API","text":"letters(p) :: String\n\nA package's name. Integers name themselves in spreadsheet columns — 1 is A, 27 is AA — so that a test can write packages and versions as different kinds of thing and have a wrong assertion look wrong.\n\n\n\n\n\n","title":"Resolver.Clauses.letters"},{"category":"type","location":"api/#Resolver.Diagnostics.VarMap","page":"API","text":"VarMap(sat)\n\nWhich packages a diagnosis can ask about, and how. haskey(vm, p) is whether the universe holds p at all — a requirement it does not is one nothing can give back. pkgs are the packages this query has emptied classes of, in a fixed order, with the literals that forbid them: those are the only packages a constraint can be blamed for, since a constraint that spares a member of every class it touches has changed nothing the registry can tell.\n\n\n\n\n\n","title":"Resolver.Diagnostics.VarMap"},{"category":"function","location":"api/#Resolver.Diagnostics.with_emptied_packages","page":"API","text":"with_emptied_packages(body, sat, vm) -> body(pkg_lits, pkg_of)\n\nRun body with one fresh literal per package this query emptied classes of, each of which says \"this query's limits on that package are in force\". The definitions live in a frame of their own and are retracted afterwards, so the instance is as reusable as it was found.\n\nMust be run inside with_classes_relaxed: the point of the literal is to impose by assumption what the query's own frame imposes unconditionally, and it can only do that once that frame is off.\n\n\n\n\n\n","title":"Resolver.Diagnostics.with_emptied_packages"},{"category":"function","location":"api/#Resolver.Diagnostics.project","page":"API","text":"project(lines, keep) :: Union{Vector{Line{P}}, Nothing}\n\nThe statements lines make about the packages in keep alone: every package outside keep eliminated, one at a time, by resolving every minimal set of clauses that leaves it nothing to be. Satisfiability is preserved, so what comes out contradicts exactly when what went in did.\n\nnothing when the elimination would cost more work than it is worth; the caller then has the clauses it started with, which are true whether or not they have been shaped.\n\n\n\n\n\n","title":"Resolver.Diagnostics.project"},{"category":"function","location":"api/#Resolver.Diagnostics.clause_of","page":"API","text":"clause_of(sat, r::Relation) :: Union{Clause{P}, Nothing}\n\nOne registry statement as the clause it is: the versions of r.pkg it speaks for, complemented and admitting ⊥ — the statement binds only where that package is at one of them — together with what it asks of r.other, which is presence (a dependency) or everything but a run of versions (a compatibility bound).\n\n\n\n\n\n","title":"Resolver.Diagnostics.clause_of"},{"category":"function","location":"api/#Resolver.Diagnostics.clause_versions","page":"API","text":"clause_versions(sat, p) :: Vector{V}\n\nThe versions of p a clause's literal indexes — the ones the universe this query was run against still holds. A literal has one more slot than this, for ⊥.\n\n\n\n\n\n","title":"Resolver.Diagnostics.clause_versions"},{"category":"function","location":"api/#Resolver.Diagnostics.clauses_satisfiable","page":"API","text":"clauses_satisfiable(sat, clauses) :: Bool\n\nCan these clauses hold together, over the versions the universe leaves each package they name? Asked of the clauses alone — no registry, no query — because whether a set of statements contradicts is a question about the statements.\n\n\n\n\n\n","title":"Resolver.Diagnostics.clauses_satisfiable"},{"category":"type","location":"api/#Resolver.Relation","page":"API","text":"Resolver.Relation{P}(dep, pkg, classes, other, others)\n\nOne registry statement, as a diagnostic instance can switch it off — and everything the statement says, so that a proof can be read off a core without asking the universe anything. These classes of pkg either need other installed (dep), or rule out its others classes (!dep):\n\nclasses — the classes of pkg the clause's own literals cover. Exactly the ones it speaks for: a conflict groups them by pattern, and a pattern need not be a run.\nothers — the classes of other a conflict clause forbids, which is one run of them; empty for a dependency, which forbids none and asks only that other be installed.\n\nIt is the unit a clause is emitted in, so it is also the unit a proof can be minimal down to.\n\n\n\n\n\n","title":"Resolver.Relation"},{"category":"function","location":"api/#Resolver.Diagnostics.print_conflict","page":"API","text":"print_conflict(io, c, index = nothing; others = :some)\n\nOne conflict's page: its heading (where it is numbered), the lines that prove it, what settles it, and the verdict on each action the page makes tempting and no fix takes. others is what the whole diagnosis knows about the repairs that cost more than the ones it offers, which is what a menu of one is entitled to say about itself; alone says whether this conflict's menu is the whole of what settles its block, which an alternative to that block denies. also is a package to name in the heading beside the requirements, which a page whose heading would otherwise repeat another's is given (heading_extras).\n\n\n\n\n\n","title":"Resolver.Diagnostics.print_conflict"},{"category":"function","location":"api/#Resolver.Diagnostics.report_problems","page":"API","text":"report_problems(d) :: Vector{String}\n\nEverything Section 8's checker can decide without asking the solver:\n\n(V2) visible closure — each meet's sides really do intersect emptily, so the contradiction is on the page rather than behind it. What the page claims is its heading's requirements together with its lines, and that is what closes;\n(V3) source coverage — every package a conflict's menu asks the reader to act on is named by a line, and every requirement the report answers for is named by its heading. An alternative repartitions repairs and not reasons (Corollary 25), and owes menus and witnesses only;\n(V5) witness coherence — each entry's witness lands inside every line its own withdrawal leaves standing, in the conflicts' menus and the alternatives alike. Silent breakage here is invisible to every other check, which is exactly why this one exists;\n(Theorem 29) an exact, once-only cover — a block's conflicts and its alternatives present that block's share of the cheapest repairs and nothing else, so no two of its selections ask for the same thing and none of them is inside another;\n(Lemma 28) blocked completions — no unless entry's completion holds a whole repair the page has already printed. Were one inside it, taking the completion alone would repair and the action it excuses would be idle, so the entry would be exhibiting a costlier fix that is not one.\n\nEmpty when the report is sound. The remaining obligation — that each printed line is true of the universe this query left (V1) — is one entailment query per line and belongs to whoever holds the instance. The disclosures (V6) are read off the decided questions when the page is printed and have no second place to disagree: the menu wording off Section 4's table, the enumeration-cut sentence off the cap's own deciding solve, and the costlier-fixes footer off Theorem 5 and whether an unless entry has already named a costlier fix concretely.\n\nEvery check is per explanation, never against a union: where two explanations' lines are S₁ ∪ S₂ and S₂ alone contradicts, the union stays contradictory whatever is deleted from S₁, so a union-level check would pass a page that silently destroyed the whole account of S₁.\n\n\n\n\n\n","title":"Resolver.Diagnostics.report_problems"},{"category":"type","location":"api/#Resolver.PkgInfo","page":"API","text":"PkgInfo{P,V}\n\nOne package's slice of the universe. The conflict matrix has one row per interchangeability class — a set of versions nothing in the registry can tell apart — and the partition saying which versions those are is carried with it.\n\nversions — every version the package offers, in canonical (provider) order. Untouched by the ordering a query asks for: the rank order is a property of the classes, and it is the rows that move.\nclasses / members — the partition, in both directions. classes[i] is the matrix row version i belongs to; members[c] is class c's version indices, ascending. Both are stored because both directions are hot; anything that drops or renumbers classes rebuilds them together.\nshadows — per class, the versions redundancy elimination removed because of that class, as values rather than indices, since they are no longer in versions. See below.\ndepends — the packages some class depends on, sorted; one matrix column each, in that order.\ninteracts — per partner package, the offset of its block of class columns in this matrix. Offset 0 is a real offset, not a sentinel: the first partner's block starts there whenever the package has no dependency columns of its own — which is every package whose only relations are weak. So haskey is how to ask whether two packages interact; get(interacts, q, 0) silently reads a real interaction as none.\nconflicts — (m+1) × (n+1) bits for m classes and n conflict columns: the dependency columns followed by one block of partner classes per partner, with the last row and column holding the in-universe flags (see BitKernels.jl for the layout).\n\nVersions share a class when nothing in the registry can tell them apart — see version_classes. Since class members are indistinguishable by construction, a user constraint cannot split a class; all it can do is empty one, which is why constraints never enter these matrices (see prepare_pkg_info).\n\nThe two per-class bits\n\nA class carries two independent facts, and the matrix holds only one of them:\n\nin-universe — \"this class is still part of the problem\". This is the flag row and column of conflicts. The passes that delete clear it (mark_installable!, mark_reachable!, mark_necessary!), and drop_unmarked! reads it and nothing else.\nactivated — \"this query admits some member of this class\". This is per-resolve state, not matrix state: it is reps[c] != 0 on the Universe built from this artifact.\n\nDeactivation never implies deletion: an emptied class keeps its row, its column in every partner, and its dependency columns. Keeping the two bits apart is what makes that possible, and BitKernels.jl's header spells out why one bit could not do both jobs.\n\nShadows\n\nmembers[c] and shadows[c] are both \"versions class c speaks for\", and they are deliberately not the same list. A member is indistinguishable from the class: it shares the row, so every statement the row makes is true of it. A shadowed version is one mark_necessary! deleted because c has a subset of its constraints — so what c rules out, the shadow is ruled out by too, but the shadow may be constrained in ways c is not. Folding the two together would put a version under a row that does not describe it, and would break the moment a constraint excludes c while admitting the version it shadows.\n\nSo the safe reading of a shadow is one-directional: whatever excludes the class excludes everything it shadows, and nothing more. A stated bound is not one of those things — \"class c requires q at 1.2\" may be false of a shadow, which can want q more narrowly still.\n\nShadows exist to be reported, not resolved over: they are in no matrix, no SAT clause names them, and no solution contains one. A class that leaves the universe for any other reason — uninstallable, unreachable — takes its shadows with it. That costs a report nothing: a shadow of such a class would have left for the same reason on its own, and unlike domination that reason is already somewhere a report can say it.\n\n\n\n\n\n","title":"Resolver.PkgInfo"},{"category":"function","location":"api/#Resolver.pkg_info","page":"API","text":"pkg_info(data, reqs = all packages; filter = true) :: Dict{P,PkgInfo{P,V}}\n\nThe T1 artifact: the dependency closure of reqs, encoded as one PkgInfo per package — one conflict matrix per package, indexed by its interchangeability classes (version_classes, collapse_classes!) — plus the one piece of preprocessing that depends on the registry alone: dropping the versions that cannot be installed whatever else is chosen, because they depend on a package with no installable version at all, applied repeatedly until nothing more drops (mark_installable!; the literature calls this arc consistency). Nothing here depends on the version ordering, on user compat or pins, or (beyond the closure) on the requirements, so the result can be computed once and shared by, or cached across, arbitrarily many resolves.\n\nfilter = false skips that prune, leaving the conflict matrices saying exactly what the data spells out.\n\nRequirement-specific and constraint-specific shrinking — reachability and redundancy elimination — happens per resolve instead, on the universe the query's representatives make of this one: see prepare_pkg_info.\n\n\n\n\n\n","title":"Resolver.pkg_info"},{"category":"function","location":"api/#Resolver.version_classes","page":"API","text":"version_classes(X, m) :: Vector{Int}\n\nThe interchangeability partition of the m versions whose conflicts matrix is X, as a class id per version — ids numbered 1, 2, … in the version order of each class's first member.\n\nPer the manual's Theory section (lemma on interchangeable versions), two versions of a package are interchangeable when they have the same dependency set and every compatibility entry in the problem — their own and every other package's — constrains them equally. In PkgInfo terms that is exactly row equality, and the test is purely local to X: the dependency columns carry the dependency set, and the interaction blocks carry every compatibility entry that mentions the package, incoming as much as outgoing, because pkg_info records each conflict symmetrically in both partners' matrices. (The incoming half is not optional — another package's bound can admit one of two versions and not the other, and then they are genuinely distinguishable.)\n\nThe partition is a property of the registry alone: it is independent of the requirements, of the version ordering, and of any user constraints. That is what puts it on the T1 side of the boundary, and what lets a PkgInfo's matrix be indexed by it: pkg_info computes the partition from rows it builds one per version, then emits the matrix with one row per class (collapse_classes!). Choosing which member of a class stands for it is the part that does depend on the query, and that is per-resolve state (see class_ranking).\n\nThe scan is word-parallel over columns: one pass computes, for every adjacent pair of versions at once, whether their rows differ anywhere, which yields the classes that are contiguous runs of versions — nearly all of them in practice, since dependency sets and compat bounds vary in blocks. A second pass hashes one row per run and merges runs whose rows compare equal, so the result is the exact row-equality partition, hence invariant under reordering versions.\n\n\n\n\n\n","title":"Resolver.version_classes"},{"category":"function","location":"api/#Resolver.class_members","page":"API","text":"class_members(classes, m = maximum(classes)) :: Vector{Vector{Int}}\n\nInvert a class-id-per-version vector into a version-index-per-class one. m is the number of classes, needed only when the last ones might be empty — which they never are for a partition, but the argument keeps the inverse total.\n\n\n\n\n\n","title":"Resolver.class_members"},{"category":"function","location":"api/#Resolver.no_shadows","page":"API","text":"no_shadows(V, m) :: Vector{Vector{V}}\n\nm classes that shadow nothing, at the cost of a single empty vector: every entry aliases the same one. That is safe only because a shadow list is replaced rather than appended to — the one place that grows one copies it first — and the saving is worth the discipline, since most classes shadow nothing and a registry-scale universe has tens of thousands of them.\n\n\n\n\n\n","title":"Resolver.no_shadows"},{"category":"function","location":"api/#Resolver.collapse_shadows","page":"API","text":"collapse_shadows(shadows, classes, members, m) :: Vector{Vector{V}}\n\nThe shadow lists of a coarser partition: the new class a set of old ones merge into shadows everything they shadowed. classes and m describe the new partition, members and shadows the old one.\n\npkg_info collapses before anything has shadowed anything, so in practice this copies empties; it exists so that merging classes is not a special case that has to remember shadows separately.\n\n\n\n\n\n","title":"Resolver.collapse_shadows"},{"category":"function","location":"api/#Resolver.collapse_classes!","page":"API","text":"collapse_classes!(info)\n\nThe last step of building an artifact. pkg_info starts with a row per version, because that is what the partition is computed from; this computes it (version_classes) and rebuilds every matrix with one row per class of the package and one column per class of each partner, which is the shape everything downstream reads.\n\nThe collapse is lossless in both directions. A class's members have identical rows by definition, so the class's row is any member's row. And members of a partner class have identical columns here: a conflict is recorded symmetrically in both partners' matrices, so partner rows that agree everywhere agree in particular about this package, and the columns they index are equal. Deleting duplicate columns cannot separate rows that agreed, so the partition this indexes is also exactly the partition it was computed from — collapsing does not have to be iterated to a fixpoint to be correct, only to be coarsest, and coarser is not what is claimed here.\n\nCalled once, at the end of pkg_info.\n\n\n\n\n\n","title":"Resolver.collapse_classes!"},{"category":"type","location":"api/#Resolver.Universe","page":"API","text":"Universe{P,V}\n\nThe universe a single resolve is run against: one PkgInfo per package, plus reps — per package, per class, the index into that package's versions of the member the class stands for under this query, or 0 when the query admits none of them.\n\ninfo carries the package universe (pkg_info) and reps carries everything the query determines about it. A user constraint acts by forbidding members of classes. Class members are indistinguishable to everything in the registry, so forbidding some of them only changes which one stands for the class; forbidding all of them leaves the class empty, hence deactivated — it cannot be selected.\n\nDeactivated classes are not deleted. They stay in the matrices as the columns a later relaxation of whatever emptied them would need, and the passes read the deactivation the way they read a conflict with an always-present version: reachability continues its prefix past a deactivated class while still following its dependencies, redundancy elimination neither deletes one nor lets one license a deletion, and the SAT instance forbids each of them with one relaxable unit clause. BitKernels.jl's header spells out how a class's in-universe flag in the matrix and its activated state here divide the work.\n\nWhy deactivation is read off reps\n\nreps has to exist regardless: a class's representative is what names the version in the answer, and it is what the class competes at when the classes are ranked. A class the query admits no member of simply has no representative, so \"unavailable for this query\" costs nothing extra to represent. A second flag bit in the matrix would work equally well; it would just encode a fact reps already carries.\n\nWhy the query's state is separate from the artifact\n\nThe invariant worth protecting is that the artifact info is query-independent: it is a function of the registry alone, PkgInfoFiles writes and reads it, and one of them answers every query, whatever that query constrains or prefers. reps is the opposite — one query's worth of state, recomputed per resolve.\n\nKeeping the two in separate values is what makes that invariant checkable rather than merely intended. Were reps a field of PkgInfo, the persisted type would contain query state: serialization would have to know to skip it, and a freshly loaded artifact would carry a field whose only honest value is \"not computed yet\" — with 0, which means deactivated, as its natural uninitialized value, so forgetting to fill it in yields a universe where nothing can be selected rather than an error. Whatever shape this ends up taking, that is the property it has to preserve: nothing a query decides may be reachable from the thing that gets cached and shared.\n\n\n\n\n\n","title":"Resolver.Universe"},{"category":"function","location":"api/#Resolver.prepare_pkg_info","page":"API","text":"prepare_pkg_info(info, prob, [info′]; order = nothing) :: Universe\n\nThe per-resolve half of preprocessing, run on a T1 artifact (see pkg_info) to produce the universe the SAT instance is built over. Two steps:\n\nRank each package's classes for this query (class_ranking): pick the member each class stands for and lay the classes out in the order those members put them in. Under the canonical ordering and no constraints this is the layout the artifact already has, and costs nothing.\nFilter the result for reachability and redundancy against the actual requirements and constraints (filter_pkg_info!).\n\nFiltering has to come second: both of its passes are stated in terms of the rank order, so the layout has to exist first.\n\nThe user's constraints reach the matrices through nothing but step 1's representatives. A constrained query and an unconstrained one produce the same matrices, up to the class reordering their representatives induce; the whole of what a constraint does is empty classes, and an empty class is deactivated rather than deleted, split or rewritten. That is what makes a class's deactivation the only thing there is to relax.\n\nThe result is a fresh universe — info is left untouched, so a T1 artifact stays reusable across resolves — unless the caller passes its own scratch dict as info′, or info itself when it owns it (which reordering rules out, since it rebuilds the matrices).\n\n\n\n\n\n","title":"Resolver.prepare_pkg_info"},{"category":"function","location":"api/#Resolver.version_permutations","page":"API","text":"version_permutations(info, order) :: Union{Nothing, Dict{P, Vector{Int}}}\n\nPer package, the permutation that puts its versions into the ordering order asks for: perm[r] is the index, in the T1 version list, of the r-th best version. Only the packages that actually need reordering get an entry, and the whole result is nothing when none of them do — so the canonical order, which is the one the T1 artifact carries, costs nothing.\n\norder is nothing for the canonical order, or a callable mapping a package to a lt comparator over its versions (lt(u, v) = \"u is preferred to v\"). The ordering is the one query knob that is not a constraint: it does not change which solutions are valid, only which valid solution is optimal. That is why it is a resolve parameter rather than part of the Problem, and why the T1 artifact can be built and cached without knowing it (see the manual's Theory section, on order-free deletion and on interchangeability classes being ordering-independent while the representative choice is not).\n\nThis is the ordering of versions; what the universe is laid out in is the ordering of classes it induces, which is class_ranking's business.\n\n\n\n\n\n","title":"Resolver.version_permutations"},{"category":"function","location":"api/#Resolver.class_ranking","page":"API","text":"class_ranking(info, prob, perms) :: (reps, perms′)\n\nWhat a query makes of an artifact's classes, before anything is materialized: per package, which member each class stands for (reps, a version index per class, 0 for a class with no admissible member) and how the classes have to be reordered to be in rank order (perms′, an entry per package that needs it, nothing when none does).\n\nRepresentative. A class's members are indistinguishable to the registry, so the only thing a user constraint can do to a class is forbid some of its members. What is left, the class admits; the best of those in the active version ordering — perms, see version_permutations — represents it. A class admitting nothing is empty, hence deactivated, and has no representative.\n\nRank. A class ranks where the member it stands for ranks, because that is the version choosing it yields: a class whose best member is forbidden competes at its best admissible member, and can therefore fall behind classes it outranks in the artifact. An empty class ranks where its best member would, the position it would compete from if the constraint that emptied it were lifted. Every class's key is a distinct version rank, so this is a total order.\n\nBoth outputs are per-resolve state: the partition they index is the registry's, but which member stands for a class, and hence how the classes are ordered, belongs to the query.\n\n\n\n\n\n","title":"Resolver.class_ranking"},{"category":"function","location":"api/#Resolver.relaxations","page":"API","text":"relaxations(prob) :: Dict{Symbol, Union{Nothing, Set{P}}}\n\nEverything prob constrains, as the relaxation that lifts all of it: per kind, the packages that kind names, or nothing for one that speaks about every package. This is what relax takes, so relax(univ, prob, prob.reqs, relaxations(prob)) is the bottom of the relaxation lattice — the empty query — and any part of it is a point between.\n\n\n\n\n\n","title":"Resolver.relaxations"},{"category":"function","location":"api/#Resolver.relax","page":"API","text":"relax(univ, Q, drop_reqs = P[], drop_constraints = ...; order = nothing)\n\nThe relaxation of Q that stops requiring drop_reqs and lifts the constraints drop_constraints, ready to resolve against univ — the universe prepare_pkg_info(info, Q; order) produced. Lifting nothing gives Q itself, which is a relaxation of Q; lifting everything (relaxations) gives the empty query. Anything Q does not carry is ignored, a constraint that is not there being already lifted.\n\ndrop_constraints maps a kind to the packages it is lifted for — or to nothing, which lifts it for every package it speaks about, and so lifts it entirely.\n\norder is the version preference ordering, as resolve takes it, and has to be the one univ was ranked in: it is what \"better\" means, and the universe and the relaxation have to mean the same thing by it. It is consumed here, so the permutations it induces are computed once however often the result is resolved.\n\n\n\n\n\n","title":"Resolver.relax"},{"category":"type","location":"api/#Resolver.RelaxedProblem","page":"API","text":"RelaxedProblem\n\nA relaxation of a query, together with what answering it on the universe that query was filtered for takes: resolve(sat, rp) returns what resolve(info, rp.prob) returns, without preparing again.\n\nBuilt by relax, which derives the relaxed problem by withdrawing demands rather than accepting one — so that rp.prob being a relaxation of rp.base is a consequence of how the value was made rather than a claim about an argument — and settles there what every solve would otherwise recompute: the member each class stands for under the relaxed problem, and the order those members rank the classes in.\n\nThose two are indexed by the class layout of the universe relax was given, so a RelaxedProblem is stale if that universe is filtered again afterwards. Nothing does that: drop_unmarked! is reachable only from prepare_pkg_info, which builds a universe rather than shrinking one already in use.\n\n\n\n\n\n","title":"Resolver.RelaxedProblem"},{"category":"type","location":"api/#Resolver.SparsePerm","page":"API","text":"SparsePerm <: AbstractVector{Int}\n\nA permutation of 1:n stored as the positions it moves: the pairs (i, p[i]) for every i with p[i] != i, ascending in i. A permutation close to the identity costs what it displaces rather than what it spans, and the identity costs nothing at all.\n\nIndexing is a search over those pairs — short, since there are few — and invperm is the pairs swapped and re-sorted, so neither direction ever touches the positions that did not move.\n\n\n\n\n\n","title":"Resolver.SparsePerm"},{"category":"function","location":"api/#Resolver.find_reachable","page":"API","text":"find_reachable(info, reqs, deact, kq, k0)\n\nFlag, in each package's matrix, the classes that some relaxation of this query could reach — where a relaxation is the same query with any of its requirements or constraint sources dropped. kq gives per class the rank of the best member this query admits (typemax if it admits none) and k0 the rank of its best member, period.\n\nRelaxing a constraint only moves a class earlier, so k0(c) ≤ key_R(c) ≤ kq(c) for every relaxation R. A class c can therefore be the best of a candidate set C under some relaxation exactly when no c′ ∈ C has kq(c′) < k0(c): otherwise c′ at its worst still precedes c at its best. Writing t = min{kq(c′) : c′ ∈ C}, those classes are {c ∈ C : k0(c) ≤ t}.\n\nA package's flagged classes are the possibly-best classes of whatever it has not been forced past, and being forced past one — a conflict with a flagged class, nothing selectable in it, a dependency on a package with nothing selectable — recomputes that set, to a fixpoint. The result already contains the classes this query alone would reach, so there is nothing to union it with — Theorem D of the manual's relaxation-stability page proves that.\n\n\n\n\n\n","title":"Resolver.find_reachable"},{"category":"function","location":"api/#Resolver.exclusion_masks","page":"API","text":"exclusion_masks(info, prob) :: AbstractDict{P, BitVector}\n\nThe versions of each package that prob's constraints forbid, as a mask over that package's version list in info. Only packages with a constraint that actually excludes something get an entry, so packages nothing constrains — the overwhelming majority, absent an admission knob — cost nothing downstream.\n\nThis is the only place a constraint is ever evaluated, and what reads it is class_ranking, which turns it into representatives. Masks are index-based, so they are read against the version list they were built from.\n\n\n\n\n\n","title":"Resolver.exclusion_masks"},{"category":"section","location":"theory/setting/#Setting","page":"Setting","text":"This page fixes the objects and notation used throughout the theory section. Everything downstream — the semantics the resolver implements, the correctness proofs for its filtering, and the Pareto-front analysis — is stated in these terms.","title":"Setting"},{"category":"section","location":"theory/setting/#The-resolution-problem","page":"Setting","text":"A resolution problem consists of:\n\na finite set of packages mathcalP;\nfor each package p a finite, totally ordered list of versions, written by rank: rank 1 is the best version, rank 2 the next, and so on (the meaning of \"best\" is supplied by the caller — newest, oldest, closest-to-installed — the theory only uses the order);\nfor each version pv a dependency set mathrmdeps(p v) subseteq mathcalP — note that dependencies are attached to versions, and different versions of the same package may have different dependency sets, a fact that turns out to carry a lot of weight;\na symmetric conflict relation on version pairs (in the package data this arises from compatibility constraints: pv conflicts with qw when w is outside pv's compatibility bound for q, or vice versa);\na set of requirements mathrmreqs subseteq mathcalP.\n\nA solution is a partial assignment s of versions to a support set mathrmsupp(s) subseteq mathcalP such that:\n\ncoverage: mathrmreqs subseteq mathrmsupp(s);\ndependency closure: for every p in mathrmsupp(s), mathrmdeps(p s(p)) subseteq mathrmsupp(s);\nconflict freedom: no two assigned versions conflict.\n\nWrite mathrmrank_s(p) for the rank of s(p).","title":"The resolution problem"},{"category":"section","location":"theory/setting/#Weak-dependencies","page":"Setting","text":"Dependencies and conflicts are separate pieces of data above, and nothing requires a conflict to be backed by a dependency edge. That separation is what a weak dependency is.\n\nA package that declares a weak dependency on q — Julia's [weakdeps], carrying an extension that loads when q happens to be present — is saying two things at once, and only one of them is a dependency:\n\nit does not need q installed, so there is no edge q in mathrmdeps(p v), and clause 2 above never forces q in;\nbut if q is installed, the extension will load against it, so the declared bound must hold: pv conflicts with every qw outside it, exactly as for a strong dependency.\n\nSo a weak dependency is a compat entry with no edge behind it, and the model needs no new machinery for it: conflict freedom is a condition on assigned versions, so a bound on an unassigned q is vacuous, and a bound on an assigned one binds. \"When both are present, they must agree\" is what clause 3 already says.\n\nTwo consequences worth stating, since both are relied on elsewhere:\n\nThe resolver core never learns which bounds are weak. Only a provider distinguishes them, by merging a version's weak compat into its compat while adding nothing to its deps. bin/Registries.jl is the production instance of this translation.\n\nThe dependency closure may be walked using strong edges alone. A package is installed only if it is required or some installed package depends on it, so the closure of mathrmreqs under mathrmdeps already contains every package that can appear in any solution. A weak target that nothing pulls in cannot be assigned, and by the previous paragraph its bound is then vacuous — so omitting it from the universe changes no answer. Pkg's own graph walk does follow weak edges, which is why its closures are larger than ours on the same requirements; the difference is in how much of the registry each walk carries, not in what either concludes.","title":"Weak dependencies"},{"category":"section","location":"theory/setting/#Tightness-and-pruning","page":"Setting","text":"The closure C(s) of a solution is the least set containing mathrmreqs and closed under following dependencies of s's chosen versions:\n\nC(s) = bigcup_k L_k qquad\nL_0 = mathrmreqs quad\nL_k+1 = L_k cup bigcup_q in L_k mathrmdeps(q s(q))\n\nA solution is tight if mathrmsupp(s) = C(s) — it contains nothing that isn't reachable from the requirements along its own chosen dependency edges. R^* denotes the set of tight solutions. Pruning pi(s) restricts s to C(s).\n\nnote: Lemma (pruning)\npi(s) is a tight solution with s's versions, and C(pi(s)) = C(s).Proof. Coverage and dependency closure hold because the closure construction adds exactly the dependencies of its members; conflict freedom is inherited from s; and the closure computation only ever inspects versions of packages already in the closure, which agree with s. ∎\n\nTwo structural facts about tight solutions recur constantly:\n\nSupport is a function of the versions. For a tight solution, the support is determined by the version assignment (it is the closure), so two distinct tight solutions with nested supports must already differ on a version within the smaller support.\nJunk. A valid solution need not be tight — it may carry packages (\"junk\") not justified from the requirements. Junk matters because the resolver works with a SAT solver, and SAT models are exactly the valid solutions, junk included.","title":"Tightness and pruning"},{"category":"section","location":"theory/setting/#The-SAT-encoding","page":"Setting","text":"The resolver encodes a problem instance with one boolean variable per package and one per version:\n\npackage implies some version: p Rightarrow bigvee_i pi;\nversion implies its package: pi Rightarrow p;\nversions are mutually exclusive: lnot pi lor lnot pj;\ndependencies: pi Rightarrow q for each q in mathrmdeps(pi);\nconflicts: lnot pi lor lnot qj for conflicting pairs.\n\nWith requirement unit clauses added, the models of this formula are precisely the valid solutions covering the requirements (with junk allowed). All constraints are dependency edges (positive, from versions to packages) and pairwise version conflicts (negative) — a structural fact several proofs exploit: any infeasibility ultimately traces to pairwise conflicts, because dependencies alone can always be satisfied by including more packages.\n\nBefore solving, the problem is shrunk by filtering — reachability analysis and redundancy elimination — whose correctness is the subject of the next page.","title":"The SAT encoding"},{"category":"section","location":"theory/relaxation/#Relaxation-stable-filtering","page":"Relaxation-stable filtering","text":"The previous page proves that filtering preserves the answer to the query it was run for. This page proves something stronger, which is what lets a failed resolve be explained rather than merely reported: the filtered universe answers every relaxation of that query too.\n\nA relaxation is the same query with some of its demands relaxed — a requirement dropped, a compat entry ignored, a pin released. Those are exactly the repairs a user can make to an unsatisfiable problem, so \"filtered for Q, valid for every R le Q\" is precisely the property under which a diagnosis may test candidate fixes on the universe the failed resolve already built, instead of re-filtering per fix.\n\nThe cost of the stronger property is that the filter deletes slightly less. Nothing else changes: the passes, their order, and the fixpoint they iterate to are the ones the previous page describes.","title":"Relaxation-stable filtering"},{"category":"section","location":"theory/relaxation/#What-a-relaxation-is","page":"Relaxation-stable filtering","text":"Fix an artifact D. A query Q is a requirement set together with a set of constraints — one per package a compat entry or a pin of the user's problem names, plus one per admission kind (prereleases, yanked versions), which names no package in particular.\n\nnote: Definition (the relaxation order)\nR le Q — \"R is a relaxation of Q\" — iff mathrmreqs_R subseteq mathrmreqs_Q and R's constraints are a subset of Q's.\n\nThis is a lattice with 2^mathrmreqs_Q + mathrmcons_Q points, Q at the top and the empty query at the bottom. Note Q le Q: every statement below about \"every relaxation\" includes the query itself, which is what makes the results here strengthen rather than replace the previous page's.\n\nWrite mathrmLay(D R) for the layered answer to R on D.","title":"What a relaxation is"},{"category":"section","location":"theory/relaxation/#Two-keys-per-class,-and-a-third","page":"Relaxation-stable filtering","text":"A class competes at the rank of the best member the query admits. This is the whole source of the difficulty: a compat bound that forbids a class's best member does not remove the class, it moves it, possibly behind a class it would otherwise outrank. Relaxing the bound moves it back.\n\nThree quantities per class c, all of them version ranks in the query's version order:\n\nmathrmadm_Q(c) — the members of c that Q admits. c is deactivated under Q when this is empty.\nmathrmkey_Q(c) = min mathrmadm_Q(c), and infty when c is deactivated. This is what the walk minimises over.\nmathrmkey_emptyset(c) — the rank of c's first member, ignoring every constraint. Its key under the empty query.\nkappa_Q(c) = mathrmkey_Q(c) for an active class, and mathrmkey_emptyset(c) for a deactivated one. This is the layout key: classes are laid out in ascending kappa_Q.\n\nThe distinction between mathrmkey_Q and kappa_Q matters and is easy to lose. A deactivated class has no admissible member, so mathrmkey_Q is infty — but it is not laid out last. It is laid out where its best member would put it, because that is where a relaxation reviving it would put it. class_ranking_keys computes exactly this: the first loop fills each class's key from its best admissible member, and the second fills the classes that got none from their best member, period.\n\nnote: Fact 0 (the keys are ordered)\nmathrmkey_emptyset(c) le kappa_Q(c) le mathrmkey_Q(c) for every class c.Proof. If c is active, mathrmadm_Q(c) is a subset of c's members, so its minimum is no smaller: mathrmkey_emptyset(c) le mathrmkey_Q(c), and kappa_Q(c) = mathrmkey_Q(c). If c is deactivated, mathrmkey_emptyset(c) = kappa_Q(c) by definition and mathrmkey_Q(c) = infty. ∎","title":"Two keys per class, and a third"},{"category":"section","location":"theory/relaxation/#The-one-order-fact-a-relaxation-cannot-reverse","page":"Relaxation-stable filtering","text":"note: Lemma 1 (monotonicity)\nFor every class c and every R le Q: mathrmkey_emptyset(c) le mathrmkey_R(c) le mathrmkey_Q(c).Proof. Adding constraints only removes admissible members, so mathrmadm_Q(c) subseteq mathrmadm_R(c) subseteq mathrmadm_emptyset(c). A minimum over a superset is no larger (with min emptyset = infty). ∎\n\nnote: Lemma 2 (definite dominance)\nIf mathrmkey_Q(c)  mathrmkey_emptyset(c) then mathrmkey_R(c)  mathrmkey_R(c) for every R le Q.Proof. mathrmkey_R(c) le mathrmkey_Q(c)  mathrmkey_emptyset(c) le mathrmkey_R(c) by Lemma 1. ∎\n\nTwo consequences the rules below lean on. The hypothesis forces mathrmkey_Q(c)  infty, so c is admissible under Q and hence under every R le Q — a deactivated class can never license anything. And this is the only order fact stable under relaxation: \"c precedes c in Q's layout\" is not enough, because R can reverse it.\n\nnote: Definition (possibly-best)\nFor a set S of classes of one package, c in S is possibly-best in S iff no c in S has mathrmkey_Q(c)  mathrmkey_emptyset(c). Write PB(S).\n\nSince mathrmkey_emptyset(c) le mathrmkey_Q(c), no class disqualifies itself, and the test collapses to one scalar: with t = min mathrmkey_Q(c)  c in S,\n\nPB(S) =  c in S  mathrmkey_emptyset(c) le t \n\nnote: Lemma 3 (``PB`` covers every relaxation's choice)\nFor every R le Q and every non-empty S: argmin_c in S mathrmkey_R(c) in PB(S).Proof. Let c^* minimise mathrmkey_R over S and suppose c^* notin PB(S): some c in S has mathrmkey_Q(c)  mathrmkey_emptyset(c^*), so mathrmkey_R(c)  mathrmkey_R(c^*) by Lemma 2, contradicting minimality. ∎\n\nBoth filtering rules are the same substitution: wherever the prefix filter said \"the first class\", the relaxation-stable filter says \"PB of what remains\", and Lemma 3 says that covers whatever any relaxation would pick.","title":"The one order fact a relaxation cannot reverse"},{"category":"section","location":"theory/relaxation/#Rule-A-—-redundancy","page":"Relaxation-stable filtering","text":"note: Rule A\nStrike class c_2 of package p only if some class c_1 of p has registry rows subseteq c_2's and mathrmkey_Q(c_1)  mathrmkey_emptyset(c_2).\n\nThe prefix rule asked only that c_1 precede c_2 in Q's layout — the fact Lemma 2's discussion shows a relaxation can reverse.\n\nnote: Theorem A (a struck class is needed by no relaxation)\nIf c_2 is struck by Rule A then mathrmLay(D R) does not use c_2, for every R le Q.Proof. Suppose the layered trace for R pins p at c_2, witnessed by a model M extending the pins so far. Build M from M by putting p at c_1 instead, at c_1's best R-admissible member — which exists, since mathrmkey_R(c_1) le mathrmkey_Q(c_1)  infty.M is a model: c_1's dependencies are a subset of c_2's and are already satisfied in M; c_1's conflicts are a subset of c_2's, so a conflict of c_1 with a member of M would be one of c_2 too, contradicting M's validity. M agrees with M everywhere else, hence with the pins.By Lemma 2, mathrmkey_R(c_1)  mathrmkey_R(c_2), so M gives p a strictly better rank under R. The trace pins the best feasible class, so it would have pinned c_1 or better, not c_2. Contradiction. ∎\n\nA struck class is not thereby forgotten: mark_necessary! records its members in the shadow list of c_1, which is what lets a diagnosis account for them — see Shadows on the previous page. The record is a side table; Theorem A quantifies over the same classes either way.\n\nwarning: Side condition\nThe domination test ignores conflicts whose partner class is absent from the universe. That exemption is sound only when evaluated against the conservatively kept classes — Rule B's output, or all classes — never against a single query's reach prefix. A partner absent under Rule B is, by Theorem B, needed by no relaxation, so conflicts with it constrain no relevant model; a partner that Q's prefix alone would drop may well matter under R. This is why the two rules are adopted together.","title":"Rule A — redundancy"},{"category":"section","location":"theory/relaxation/#Rule-B-—-reachability","page":"Relaxation-stable filtering","text":"note: Rule B\nLeast fixpoint of sets S(p) with mathrmpush(p) subseteq S(p) recording the classes the walk has been forced past. Writing t(p) = minmathrmkey_Q(c)  c notin mathrmpush(p), an unseeded package has S(p) = emptyset and a seeded one has S(p) = c  mathrmkey_emptyset(c) le t(p), subject to:p in mathrmreqs_Q ⇒ p is seeded.\nc in S(p) and c depends on q ⇒ q is seeded.\nc in S(p) deactivated ⇒ c in mathrmpush(p).\nc in S(p), c in S(q), c conflicts with c ⇒ c in mathrmpush(p) and c in mathrmpush(q).\nc in S(p) depends on q with mathrmpush(q) = mathrmclasses(q) ⇒ c in mathrmpush(p).\n\nnote: Theorem B (every relaxation's reachable set is kept)\nFor every R le Q and every package p, every class the prefix filter would keep for R lies in S(p).Proof. By induction over the derivation of R's reach fixpoint, maintaining the invariant(∗) mathrmpassed_R(p) subseteq mathrmpush(p) — the walk has been forced past everything R's walk was.Invariant. R passes c for one of three reasons, each firing the matching rule: (a) c conflicts with a class reachable under R, both in S by the induction hypothesis, and conflicts are query-independent, so rule 4 fires; (b) c is not selectable under R — and since mathrmadm_Q(c) subseteq mathrmadm_R(c), not under Q either, so rule 3 fires; (c) c depends on a package saturated under R, which by (∗) is saturated here too, so rule 5 fires.Seeds. p in mathrmreqs_R subseteq mathrmreqs_Q, so rule 1 seeds p, and R's first class of p minimises mathrmkey_R over all of p's classes, which Lemma 3 places in S(p). Dependencies are the same argument via rule 2.Frontier. Let c^* be R's next class after its passed set: c^* = argmin mathrmkey_R over mathrmclasses(p) setminus mathrmpassed_R(p). If c^* in mathrmpush(p) then c^* in S(p), since pushed classes are kept. Otherwise c^* lies in mathrmclasses(p) setminus mathrmpush(p), which by (∗) is a subset of mathrmclasses(p) setminus mathrmpassed_R(p); c^* minimises mathrmkey_R over the larger set and belongs to the smaller, so it minimises over the smaller too, and Lemma 3 puts it in S(p). ∎","title":"Rule B — reachability"},{"category":"section","location":"theory/relaxation/#The-query's-own-answer,-without-a-second-walk","page":"Relaxation-stable filtering","text":"Theorem B with R = Q already says S(p) contains what Q's own prefix walk would keep. That is worth stating concretely, because it is what licenses the implementation to run one walk: the possibly-best walk is not unioned with a prefix walk, and no prefix walk is run at all.\n\nNumber each package's classes 1 dots m_p in layout order, so kappa_Q(p cdot) is non-decreasing. The prefix walk W is the least fixpoint of r(p) in 0 dots m_p+1 under\n\np in mathrmreqs ⇒ r(p) ge 1;\nj le r(p) and pj depends on q ⇒ r(q) ge 1;\nj le r(p) and pj deactivated ⇒ r(p) ge j+1;\nj le r(p), k le r(q), pj conflicts with qk ⇒ r(p) ge j+1 and r(q) ge k+1;\nj le r(p), pj depends on q, r(q) ge m_q + 1 ⇒ r(p) ge j+1.\n\nwith saturation written r(p) = m_p + 1. This is find_reachable as the previous page states it, under the precondition that every class of the universe is active on entry — which drop_unmarked! establishes, since it ends by setting every surviving class's flag.\n\nnote: Lemma D1 (a pushed prefix keeps a prefix)\nIf p is seeded and 1 dots j-1 subseteq mathrmpush(p) for some 1 le j le m_p + 1, then 1 dots min(j m_p) subseteq S(p).Proof. If j = m_p + 1 then every class is pushed, so t(p) = min emptyset = infty and S(p) is all of p's classes.Otherwise j le m_p. Every c  j is pushed, so t(p) is a minimum of mathrmkey_Q over a subset of c  c ge j, hencet(p) ge min_c ge j mathrmkey_Q(c)\n      ge min_c ge j kappa_Q(c)\n      = kappa_Q(j)using Fact 0 for the middle step and monotonicity of kappa_Q in the layout position for the last. Then for every i le j, mathrmkey_emptyset(i) le kappa_Q(i) le kappa_Q(j) le t(p), again by Fact 0 and monotonicity, so i in S(p). ∎\n\nnote: Lemma D2 (the prefix walk's pushes are matched)\nEvery fact r(p) ge j derivable by W implies, at Rule B's fixpoint, that p is seeded and 1 dots j-1 subseteq mathrmpush(p).Proof. Induction on the derivation of r(p) ge j.(1) j = 1: rule 1 seeds p, and the claimed inclusion is empty.(2) The premise j le r(p) is a derived fact, so by the induction hypothesis p is seeded with 1 dots j-1 subseteq mathrmpush(p), whence j in S(p) by Lemma D1. pj depends on q, so rule 2 seeds q; the inclusion for q at j = 1 is empty.(3) As in (2), j in S(p). pj is deactivated, so rule 3 puts j in mathrmpush(p); with the induction hypothesis this gives 1 dots j subseteq mathrmpush(p).(4) As in (2), j in S(p) and k in S(q). Conflicts are query-independent, so rule 4 pushes both, giving 1 dots j subseteq mathrmpush(p) and 1 dots k subseteq mathrmpush(q).(5) By the induction hypothesis applied to r(q) ge m_q + 1, 1 dots m_q subseteq mathrmpush(q), i.e. q is saturated in Rule B's sense. As in (2), j in S(p), so rule 5 pushes it. ∎\n\nnote: Theorem D (containment: the union is never necessary)\nAt the two fixpoints, for every package p:1 dots min(r(p) m_p) subseteq S(p)Proof. If r(p) = 0 the claim is empty. Otherwise r(p) ge r(p) is derivable, so Lemma D2 gives p seeded with 1 dots r(p)-1 subseteq mathrmpush(p), and Lemma D1 with j = r(p) gives the conclusion. ∎\n\nSo Theorems 5 and 6 of the previous page transfer unchanged: whatever the prefix walk kept for Q is still kept, and the layered answer to Q survives filtering exactly as before. Running a prefix walk alongside and keeping the union would add nothing.\n\nnote: Remark (why one integer suffices)\nmathrmpush(p) only grows, so t(p) — a minimum over its complement — only rises, so S(p) = c  mathrmkey_emptyset(c) le t(p) only grows and is downward closed in mathrmkey_emptyset order. Every rule that pushes a class has that class already in S(p) as a premise, so mathrmpush(p) subseteq S(p) needs no separate enforcement and the kept set is exactly the final PB.A set-valued frontier is therefore not required: the kept set is a prefix, in mathrmkey_emptyset order rather than layout order, and one monotone pointer along each of the two orders represents it. That is what find_reachable implements.","title":"The query's own answer, without a second walk"},{"category":"section","location":"theory/relaxation/#Composition","page":"Relaxation-stable filtering","text":"note: Theorem C (relaxation-stable filtering)\nLet F_Q be the filter under Q using Rules A and B, iterated to a fixpoint. Then for every R le Q: mathrmLay(F_Q(D) R) = mathrmLay(D R).Proof. By Theorems A and B no pass ever deletes a class that mathrmLay(D R) uses, for any R le Q; deletions compose, so mathrmLay(D R) survives in F_Q(D). Theorem 4 of the previous page (reduction invariance) then gives equality. ∎\n\nnote: Corollary C1 (the point)\nA diagnosis may answer any relaxation of a failed query on the universe filtered for that query. Re-filtering per candidate fix is unnecessary.\n\nrelax builds that question and resolve answers it.\n\nBecause le is a lattice with the empty query at the bottom, the same argument run at Q = emptyset says a universe filtered for no constraints at all serves every query over the same requirements — which is what makes a filtered artifact worth caching.","title":"Composition"},{"category":"section","location":"theory/relaxation/#What-this-does-not-give","page":"Relaxation-stable filtering","text":"The guarantee is over relaxations of one query, not over arbitrary queries. A different query — one that adds a requirement, or tightens a bound — is not below Q in the order and is not covered; it needs its own filter run. In particular this says nothing about upstream fixes, which change the registry rather than the query, and therefore change D.\n\nNor does it make the class order stable. Relaxing genuinely reorders classes, and the layered answer depends on that order. What Theorem C says is that the surviving universe still contains everything the reordered answer needs — so a relaxation is answered by re-ranking and re-solving the filtered universe, not by re-filtering it.\n\nwarning: Formalization boundary\nAs on the previous page, the rule sets above are a faithful but hand-made reading of class_ranking_keys, mark_necessary! and find_reachable; the correspondence is checked by review and by the test suite rather than mechanically. test/relaxation_stable.jl checks the conclusions directly: for random universes and random queries it enumerates relaxations, re-ranks and re-solves the filtered universe under each, and compares against resolving that relaxation from the unfiltered artifact — an empirical check of Theorem C, and through it of the rules it rests on.","title":"What this does not give"},{"category":"section","location":"#Resolver.jl","page":"Home","text":"A package version resolver built on a real SAT solver (PicoSAT). Given package data — versions, dependencies, and compatibility constraints — and a set of required packages, resolve returns a single optimal solution mapping each needed package to a version, or a Diagnosis — which requirements cannot hold together, why, and a menu of verified fixes for each of them — when they cannot be satisfied.\n\nA diagnosis reads like this. Two requirements fail for unrelated reasons, so each gets its own menu, and any one alternative from each menu resolves the query:\n\nUnsatisfiable — 2 conflicts, pick a fix for each:\n\nConflict 1: DataFrames\n  • your compat leaves DataFrames ≥1.0.0\n  • DataFrames 1.0.0–1.8.2 requires PrettyTables\n  • your compat leaves no version of PrettyTables\n  Fix it by any one of:\n    1. relax your compat on DataFrames\n       → allows: DataFrames 0.21.8\n    2. relax your compat on PrettyTables\n       → allows: DataFrames 1.8.2, PrettyTables 3.4.8\n    3. drop requirement DataFrames\n\nConflict 2: Plots\n  • Plots requires RecipesBase\n  • your compat leaves no version of RecipesBase\n  Fix it by any one of:\n    1. relax your compat on RecipesBase\n       → allows: Plots 1.41.0, RecipesBase 1.3.4\n    2. drop requirement Plots\n\nEvery fix is verified: the version list after each one is what a resolve of the fixed query actually returns, not a guess. Between them the conflicts reach every fix as cheap as the cheapest, so nothing is left over: where a group of conflicts that stand or fall together has cheapest fixes that taking one entry from each of their menus does not reach, those print after the last conflict as the alternatives they are — \"Or, to fix without dropping requirement Knet:\", and then what to do instead. Whatever is outside the page even so is confessed: \"Costlier fixes also exist.\" when what was left out gives up more, \"There are more minimal fixes than are shown.\" when the search for them was cut short. When the menus are the whole of it, the report says nothing at all.\n\nWhat \"optimal\" means, precisely, and why the resolver's aggressive problem filtering provably does not change the answer, is worked out in the Theory section:\n\nSetting — the resolution problem, solutions, tightness, and the SAT encoding.\nThe layered solution & filtering — the semantics the resolver implements, and proofs that filtering preserves both satisfiability and the answer.\nPareto front optimality — an alternative, purely declarative notion of optimality that was fully worked out and ultimately set aside; its theorems and counterexamples illuminate why the layered semantics and its filter fit together as well as they do.\nRelaxation-stable filtering — the stronger property that a universe filtered for one query answers every relaxation of it, which is what lets a failed resolve be explained rather than merely reported.\nExplaining an unsatisfiable resolve — the complete theory a Diagnosis implements: reasons, repairs, the menus they factor into, the products that cover what does not factor and the alternatives that are what those products leave; the pivot-and-sides form of an explanation; and why the versions it shows come from a resolve rather than from the instance.\n\nFor the package's motivation and project status, see the README.","title":"Resolver.jl"},{"category":"section","location":"theory/front/#Pareto-front-optimality","page":"Pareto front optimality","text":"This page documents a fully worked-out alternative semantics for the resolver — necessity on the Pareto front — including its theorems, its pure-SAT implementation, and the reasons it was ultimately set aside. It is a digression from what the package implements, but a productive one: its impossibility results explain why the layered semantics and the filter fit together, its proof techniques power the theorems on the previous page, and it closes the question of a purely declarative optimality criterion rather than leaving it open. A reference implementation lives on the front-necessity branch.","title":"Pareto front optimality"},{"category":"section","location":"theory/front/#Motivation","page":"Pareto front optimality","text":"The layered solution is defined by a procedure. One can ask for something more declarative: a criterion picking out the optimal solution using only the solution set — no layers, no reference to the dependency walk, ideally nothing but satisfiability queries. The natural candidate is a necessity descent: repeatedly pin the highest-priority package that is necessary — present in every remaining solution — at the best version available. Necessity, however, is a universal property of a solution set, so its meaning depends on which set is quantified over:\n\nAll tight solutions (raw necessity): too weak — a package needed by every sensible solution counts as avoidable if one strictly-dominated solution technically avoids it, e.g. by taking a requirement's worst version merely because that version happens to drop a dependency edge. This distorts the descent, and it also forbids problem reduction entirely, which is ruinous at registry scale.\nModels of the filtered problem: fast, but then the answer depends on filtering details — sharpening or dulling the filter changes the result, which is unacceptable as a semantics.\n\nThe canonical middle ground is necessity over the Pareto front: quantify over the solutions that are not strictly worse than others.","title":"Motivation"},{"category":"section","location":"theory/front/#The-dominance-order","page":"Pareto front optimality","text":"For tight solutions s t:\n\nt trianglerighteq s quadiffquad\nmathrmsupp(t) supseteq mathrmsupp(s) quad\nmathrmrank_t(p) le mathrmrank_s(p)  forall p in mathrmsupp(s)\n\nwith strict improvement at some p in mathrmsupp(s) (coverage- monotone version dominance). This is a strict partial order on the finite set R^* of tight solutions, so every solution is either maximal or strictly dominated by a maximal one. The front F is the set of maximal elements.\n\nBoth design choices in trianglerighteq are forced:\n\nOrientation. An order in which leaner solutions dominate cannot work: the solutions that falsely break necessity are support-minimal (a requirement alone at a bad version), and a support-minimal solution has no leaner competitor, so no leaner-dominates order can remove one. Also, since a tight solution's support is a function of its versions, \"leaner at equal quality\" never occurs — leanness always trades against version quality, and the order must price the trade. trianglerighteq prices it as: a bigger solution wins only by covering everything at least as well and strictly better somewhere.\nJunk is excluded by the space, not the order. With the supseteq orientation, unjustified packages would ride along for free, so the candidate space must be the tight solutions. Tightness (well-founded justification, the same notion as stable models in answer-set programming) is not expressible by any dominance relation over versions — it is not monotone in the version lattice. Empirically, defining the front over junk-tolerant solutions changes answers on 92 of 7,464 small instances.\n\nThe front-necessity semantics is the forced descent over F: repeatedly pin the highest-priority package present in every front member consistent with the pins so far, at the best version among them, until no unpinned package is common.","title":"The dominance order"},{"category":"section","location":"theory/front/#Theorem-A:-pin-exactness","page":"Pareto front optimality","text":"The linchpin of everything that follows.\n\nnote: Theorem A\nLet pins be the pin state after any number of descent rounds. If f in F contains every pinned package at rank ≤ its pinned rank, then f agrees exactly with the pins.Proof. Suppose not; among pinned packages where f is strictly better, let hat q be the one pinned earliest, at rank hat r. On packages pinned before hat q, f is ≤ with no strict improvement, hence equal — so f was in the remaining front set when hat q was pinned. But then hat r = minmathrmrank_g(hat q) le mathrmrank_f(hat q)  hat r. ∎\n\nnote: Corollary A1\nIf t in F and t trianglerighteq s (or t = s) for any s agreeing with the pins, then t agrees exactly with the pins: dominating a pin-consistent solution can never buy pin-violating quality.","title":"Theorem A: pin exactness"},{"category":"section","location":"theory/front/#Theorem-B:-the-answer-is-a-front-member","page":"Pareto front optimality","text":"note: Theorem B\nThe descent terminates with the remaining front set equal to exactly mathrmpins; in particular the answer is itself a valid, tight, undominated solution.Proof. Requirements are in every front member, so they get pinned. If q is pinned, every remaining f has q at the pinned version, so mathrmdeps(q mathrmpins(q)) is common and gets pinned — the pins are dependency-closed. Now take any final survivor f and walk its closure: it starts at requirements (pinned), and whenever a closure node u is pinned, f(u) = mathrmpins(u), so the next closure nodes are pinned too. Hence C(f) subseteq mathrmdom(mathrmpins), and by tightness mathrmsupp(f) = C(f) subseteq mathrmdom(mathrmpins) subseteq mathrmsupp(f) — so f = mathrmpins. ∎\n\nThe same argument shape, one level down, shows the layered answer is trianglerighteq-undominated: a dominator would have to agree with every layered pin before its earliest strict improvement, contradicting that pin's optimality. So the layered solution always lies on the front; front-necessity differs only in which front point it selects (230 of 7,464 small instances) — front-necessity makes priority the only ordering principle among necessary packages, where the layered order lets requirements outrank higher-priority dependencies.","title":"Theorem B: the answer is a front member"},{"category":"section","location":"theory/front/#Theorem-C:-a-pure-SAT-implementation-exists","page":"Pareto front optimality","text":"Front-necessity is implementable against a SAT instance that encodes validity only — no tightness encoding, which matters because eager tightness encodings blow up (rooted-chain/level constructions), the naive support encoding admits unrooted cycles, and lazily this is exactly answer-set programming's unfounded-set problem. Instead, two cheap operations run outside the solver: pruning pi (a closure computation) and support comparison. The key sub-results:\n\nWitness transfer. A model lacking p prunes to a tight solution lacking p; conversely every tight solution is a model. So junk never distorts the existential side of a necessity query.\nDominator transfer. A tight solution s has a tight strict dominator iff some model d covers mathrmsupp(s) at ranks ≤ with a strict improvement and passes the prune-check pi(d) supseteq mathrmsupp(s) — junk can fake domination whose tight core doesn't actually cover s, and the prune-check filters exactly those fakes.\nChain-top admissibility. Climbing dominators terminates at a front member, which by Corollary A1 agrees exactly with the pins even though intermediate steps may not.\nCone blocking. When a chain-top t contains p, everything trianglelefteq t is either dominated or is t itself, so one clause excludes the entire cone without losing any witness.\nGreedy construction. In the implementation, candidates are constructed rather than searched for: the layered greedy runs under per-query assumptions, and a junk-free greedy answer is provably Pareto-maximal within its query space (the Theorem A argument again). Restricting all queries to supported models — one clause per package: present implies depended-upon — loses no tight solution and reduces junk to dependency cycles.\n\nWith these, the descent's two oracle types (is p in every pin-exact front member? what is the best rank among them?) are decided exactly, by sequences of ordinary SAT solves. Correctness of the composed pipeline was additionally verified against a brute-force implementation of the definition on roughly 19,500 instance/order pairs with zero deviations.","title":"Theorem C: a pure-SAT implementation exists"},{"category":"section","location":"theory/front/#Theorem-D:-no-version-level-reduction-is-faithful","page":"Pareto front optimality","text":"Here is the decisive negative result. One would like to run the necessity descent on a reduced problem — that is how the layered semantics gets its speed. For front-necessity this fails not just for the actual filter but in principle:\n\nnote: Theorem D\nThere is a problem on which the forced descent over models of the exact front-support reduction — keep a version iff some front member uses it, the strongest version-level reduction that could possibly be faithful — differs from the forced descent over the front itself.Proof. Requirements a, priority p  a  d  b, version 1 best:package dependencies conflicts\na a1 to d, a2 to b \nb b1 to p \nd d1 to p, d2 to  d1 otimes p1\np p1 to d, p2 to  R^* has four members and the front is F = f_1 f_2 with f_1 = a2 b1 p1 d2 and f_2 = a1 d1 p2 — both contain p, so the front descent pins p first at rank 1 and lands on f_1. But every version above appears in some front member, so the exact front-support reduction keeps everything — including the recombinant c = a1 d2 (f_2's a with f_1's d), a tight solution missing p. In the reduced problem p is no longer common; the descent pins a first, and rank-minimization at a eliminates f_1: the reduced answer is f_2 ne f_1. (Verified computationally.) ∎\n\nThe moral: necessity is a property of the solution set, and a version universe only bounds that set from above — recombinations of individually front-supported versions can erase necessity. Universal queries survive only when asked against the front itself. Note the contrast with Theorem 4 of the layered page: the layered semantics needs a reduction to preserve one solution; front-necessity would need it to preserve the entire front and nothing that changes commonality — and even the latter is unattainable at the version level. Such counterexamples require engineered structure (two dependency routes, a recombination orphaning the necessity package, a conflict pinching the diagonal): none arose in 12,000 random 4-package instances, which is also why smaller empirical sweeps had suggested the reduction was safe.","title":"Theorem D: no version-level reduction is faithful"},{"category":"section","location":"theory/front/#Why-the-filter-cannot-serve-the-front","page":"Pareto front optimality","text":"The actual reachability filter fails front-preservation in an even more basic way: it drops front members outright. On the Theorem D instance it keeps only a1 d1 d2 p1 p2 — never reaching a2, deleting b — yet f_1 uses both. The filter's designed invariant is \"a worse version is only chosen under conflict pressure against the better version,\" and a1 is never conflicted. But f_1 takes a2 for a different kind of reason: under a1, the only dependency edge that can justify p is d1 to p, and p1 otimes d1 — so on the a1 route the better p1 is unreachable-as-justified. Switching to a2 reroutes justification through b and frees p1. The downgrade at a is paid for by an upgrade at p — an opportunity-cost trade, mediated not by any conflict against a but by version-varying dependency sets rewiring the justification topology.\n\nThat is precisely the boundary of the filter's intuition:\n\nnote: Fragment lemma\nIf every version of each package has the same dependency set, then on the front every non-best version is blocked by a conflict with a co-installed package — the filter's invariant, as a theorem.Proof (one step). Let s in F with q at a non-best version and v better. The in-place upgrade s = sq mapsto v has the same support, and — dependency sets being version-independent — the same justification edges, hence remains tight. If s were conflict-free it would strictly dominate s. So some conflict between qv and a member of s blocks it. ∎\n\nPointwise (layered) optimality never makes opportunity-cost trades, which is why conflict-driven degradation covers everything it can ever choose (Theorem 5 of the layered page); the front additionally contains the trade-mediated points, and front-necessity quantifies over exactly those.","title":"Why the filter cannot serve the front"},{"category":"section","location":"theory/front/#Why-it-was-set-aside","page":"Pareto front optimality","text":" layered front-necessity\ncanonical, filter-free definition yes (Theorems 4–6) yes (by construction)\ndeclarative (procedure-free) statement no — defined by the trace yes\nSAT query character existential, answer-witnessed universal (necessity, dominance)\nadmissible reductions any preserving one solution none at the version level (Theorem D)\nregistry performance milliseconds–seconds small: ms; DataFrames: ~85 ms; DifferentialEquations (26k versions, unreduced): intractable\n\nThe costs are intrinsic, not implementation slack: universal queries must run against the unreduced problem (Theorem D), every semantically-avoidable candidate needs a witness construction plus a dominance query, and witness certificates are invalidated by later pins. Even under optimistic assumptions the descent is an order of magnitude or more behind the layered implementation — and end users do not observe the difference between the two Pareto points the semantics select. What the exploration contributes is what this page records: the impossibility theorems that close the declarative route, the proof machinery reused for the layered guarantees, and a precise understanding of what the filter does and does not preserve — satisfiability (provably), the layered answer (provably), the front (provably not).","title":"Why it was set aside"},{"category":"section","location":"theory/layered/#The-layered-solution-and-filtering","page":"The layered solution & filtering","text":"This page defines the semantics resolve implements — the layered solution — and proves the two guarantees that make the implementation trustworthy:\n\nfiltering preserves satisfiability — a solvable problem never becomes unsolvable by filtering; and\nfiltering preserves the answer — the solution computed on the filtered problem equals the layered solution of the raw problem, so the semantics is independent of filtering details.\n\nThe second result is the important one: it means the layered solution has a canonical, filter-free definition, and the filter is licensed by a theorem rather than trusted as part of the spec. It also yields a general contract under which future, more aggressive reductions are automatically safe.","title":"The layered solution and filtering"},{"category":"section","location":"theory/layered/#The-layered-solution","page":"The layered solution & filtering","text":"Fix a problem, requirements, and a strict total priority order prec on packages (from the by argument). The layered trace is the following deterministic procedure over the problem's models (= valid solutions covering the requirements, junk allowed):\n\nIf no model exists, the answer is unsatisfiable (nothing).\nOtherwise set the current layer L = mathrmreqs and start with no pins.\nFor each q in L in prec-order: pin q at the best rank feasible given the pins so far — the minimum mathrmrank_M(q) over models M that extend all current pins.\nLet the next layer be the not-yet-visited dependencies of the versions just pinned; if nonempty, go to 3.\nThe answer mathrmLay(D mathrmreqs prec) is the set of pins.\n\nnote: Theorem 1 (well-definedness)\nThe layered trace is deterministic — it depends only on the problem, the requirements, and the priority order, not on which models a solver happens to produce — and its answer is a tight solution.Proof. Each step-3 choice is a minimum over a set determined by the problem and the current pins; layers are determined by pinned versions' dependency sets; so the trace is a deterministic recursion. Feasibility is maintained at every pin (the minimizing model extends the pins), so the final pin set extends to a model; the pins are exactly the dependency closure of the requirements under the pinned versions (layer construction), hence a tight solution. ∎\n\nThe layered solution is pointwise optimal: each package, taken in layer order and priority order, is at the best version compatible with the choices already made. It is also Pareto-optimal in the sense made precise on the next page — no tight solution beats it on every package — though Pareto-optimality is a strictly weaker statement than the trace itself, which pins the unique answer.","title":"The layered solution"},{"category":"section","location":"theory/layered/#Filtering","page":"The layered solution & filtering","text":"Real problems are shrunk before solving by two passes (filter_pkg_info!, run per resolve from prepare_pkg_info):\n\nReachability (find_reachable) computes, per package, a version prefix to keep, by a fixpoint of conflict-driven degradation:\n\nrequirements' first versions are reachable;\nthe first version of each dependency of a reachable version is reachable;\nif a reachable version conflicts with a reachable version, both packages' prefixes extend one further;\nif every version of a package has been pushed past (the package saturates — meaning conflicts could make it uninstallable), packages depending on it have their prefixes pushed past the depending versions — and a saturated package keeps all its versions.\n\nRedundancy elimination (mark_necessary!) then deletes any version qj for which some better version qi (i  j) has a subset of its constraints — every dependency and every (surviving) conflict of qi is also one of qj — iterated to a fixpoint.\n\nnote: What the code runs is weaker than this\nBoth rules as stated are what the theorems below are proved against, and both are stronger than what find_reachable and mark_necessary! actually apply. The implemented rules replace \"the first version\" with \"every version some relaxation of this query could put first\", and \"i  j\" with a comparison that no relaxation can reverse, so that the filtered universe serves every relaxation of the query and not only the query — see Relaxation-stable filtering.Nothing below needs restating for that. The implemented reachability keeps a superset of the prefix above (Theorem D there) and the implemented redundancy deletes a subset of what the i  j rule deletes (its Rule A implies i  j), so every deletion the implementation makes is one this page already licenses, and Theorem 4 carries the answer through.\n\nThe intuition behind reachability is: an optimal solution only settles for a worse version for a reason, and the reason is a conflict. That intuition is exactly right for the layered (pointwise) semantics, and the theorems below make it precise. (It is not right for Pareto-front reasoning — see the next page — which is one of the sharpest ways to see the difference between the two notions of optimality.)","title":"Filtering"},{"category":"section","location":"theory/layered/#Theorem-2:-the-reach-fixpoint-invariant","page":"The layered solution & filtering","text":"Everything rests on one observation about what the reachability fixpoint guarantees. Call a kept version last if it is the highest-rank (worst) kept version of an unsaturated package.\n\nnote: Theorem 2 (fixpoint invariant)\nAt the reachability fixpoint, the last kept version pr(p) of every unsaturated package p:conflicts with no kept version of any package (saturated packages' versions included); and\ndepends on no saturated package.Proof. If pr(p) conflicted with a kept qk, then whichever of the two was processed later scans the other (already reachable) and fires the degradation rule, extending p's prefix past r(p) — contradicting that r(p) is last. If pr(p) depended on a saturated package, the saturation rule pushes dependents past the depending version, likewise extending the prefix. ∎\n\nIn other words: the cascade pushes prefixes exactly until the \"all-worst-kept\" assignment becomes self-consistent. That immediately gives the first correctness result.","title":"Theorem 2: the reach fixpoint invariant"},{"category":"section","location":"theory/layered/#Theorem-3:-filtering-preserves-satisfiability","page":"The layered solution & filtering","text":"note: Theorem 3\nIf the raw problem has a solution covering the requirements, so does the filtered problem. (The converse is trivial, since filtered models are a subset of raw models.)Proof. Let S be a raw solution. First suppose no requirement's dependency chain touches a saturated package. Build T as the dependency closure from the requirements choosing every package's last kept version. By invariant (2) this closure never enters a saturated package; by invariant (1) every chosen version conflicts with nothing kept, so T is conflict-free; it is dependency-closed by construction and covers the requirements. T is a filtered solution.In general, splice: for saturated packages use S's own versions — all kept, since saturation drops nothing, and S is dependency-closed so saturated dependencies of S-members are assigned in S — and for unsaturated packages use last kept versions. Conflicts between an unsaturated choice and anything vanish by invariant (1); conflicts among the saturated choices are absent because they come from the valid S. Dependency edges are satisfied branch by branch: unsaturated members' dependencies are unsaturated by invariant (2) and get last-kept versions; saturated members' dependencies are resolved inside S or by last-kept substitutes (dependency edges are package-level, so any assigned version satisfies them). ∎\n\nFor the redundancy pass, satisfiability is preserved by direct substitution: any solution using a deleted qj maps to a solution using its dominating qi (fewer dependencies to satisfy, fewer conflicts to avoid), and domination chains terminate at a surviving version because reachability keeps prefixes — a better version is never deleted while a worse one survives.","title":"Theorem 3: filtering preserves satisfiability"},{"category":"section","location":"theory/layered/#Theorem-4:-reduction-invariance-—-the-optimization-license","page":"The layered solution & filtering","text":"The next theorem is the general principle; the filter's correctness is an instance of it.\n\nnote: Theorem 4 (reduction invariance)\nLet D be obtained from D by deleting versions (so mathrmmodels(D) subseteq mathrmmodels(D)), and suppose the layered solution mathrmLay(D) survives in D — every version it uses is kept. Then mathrmLay(D) = mathrmLay(D).Proof. By induction along the trace, both runs make identical pins. At each step the choice for q is the best rank feasible given the (identical) pins. Deleting models can only make ranks harder to achieve, so the D-best is no better than the D-best. And the D-best is witnessed in D by mathrmLay(D) itself: it extends all current pins (its values on pinned packages are exactly the pins, by induction), assigns q its D-best rank (that is what the raw trace pinned), and survives in D by hypothesis. So the choices agree; identical pins induce identical layers. The satisfiable/unsatisfiable branch also agrees, since mathrmLay(D) witnesses satisfiability in both. ∎\n\nThis is the \"decoupling\" result: the layered semantics is defined once, on the raw problem, and any reduction whatsoever is licensed by exhibiting a single surviving solution — the answer. Two consequences:\n\nCertification. Answer preservation can be checked post hoc, cheaply: resolve the filtered problem, then verify each pin against the raw problem — feasibility is free (the answer is itself a raw model), and optimality of each pin is one unsatisfiable solve (\"no raw model with these pins and a better version of q\") per pinned package. A resolver can offer machine-checked raw-canonical answers for the cost of building the raw instance plus one probe per package.\nHeadroom. Future reductions can be far more aggressive than the current filter — anything that keeps the answer is safe, and the certification above detects violations per-resolve rather than trusting the reduction globally.\n\nThe deep reason a theorem this convenient is available is that every SAT query the layered trace makes is existential (\"is a better version feasible?\") and is witnessed by the final answer, which survives the reduction. Universal queries — \"is this package in every solution?\" — do not enjoy this: deleting models changes their truth value. That asymmetry is the fundamental obstruction explored on the next page.","title":"Theorem 4: reduction invariance — the optimization license"},{"category":"section","location":"theory/layered/#Theorem-5:-the-filter-preserves-the-answer","page":"The layered solution & filtering","text":"It remains to show the actual filter satisfies Theorem 4's hypothesis.\n\nnote: Theorem 5 (reach keeps the layered answer)\nEvery version chosen by the raw layered trace is kept by reachability filtering.Proof. By induction along the trace; assume all pins so far are kept versions. Consider the step for package q, and suppose first q is unsaturated. We claim q's last kept version qr(q) is feasible given the pins, so the best feasible rank — the trace's choice — is at most r(q), i.e. a kept version. To see feasibility, construct a model: take the pins (jointly feasible by the trace's invariant, and kept by induction), add qr(q), and complete with the last-kept dependency closure of everything present. By invariant (1) of Theorem 2, qr(q) and every last-kept version conflict with nothing kept — in particular not with the pins or each other; by invariant (2) the completion never needs a saturated package; pins' dependencies are satisfied by pinned or last-kept packages. The result is a model containing the pins and qr(q). If instead q is saturated, all its versions are kept and there is nothing to prove. ∎\n\nnote: Theorem 6 (redundancy keeps the layered answer)\nThe layered trace never chooses a version deleted by redundancy elimination.Proof. Suppose the trace chose qj and qj is dominated by a kept qi with i  j. Any model witnessing qj's feasibility given the pins converts to one witnessing qi's: swap the version; qi's dependencies are a subset of qj's (already present) and its conflicts a subset (already avoided). So qi was feasible and better, contradicting that the trace chose j. Domination chains terminate at a kept version, and the argument applies to whichever kept dominator terminates the chain. ∎\n\nComposing: the raw answer survives reachability (Theorem 5), so by Theorem 4 the reach-filtered problem has the same layered answer; that answer survives redundancy elimination (Theorem 6 applied on the reach-filtered problem), so by Theorem 4 again the fully filtered problem has the same layered answer. The implementation's answer is the raw layered solution.","title":"Theorem 5: the filter preserves the answer"},{"category":"section","location":"theory/layered/#Shadows:-what-a-deletion-leaves-behind","page":"The layered solution & filtering","text":"Theorem 6 says the answer does not need qj. It does not say a report does not need it. A reader asking why the resolver would not take qj is owed the reason, and the reason lives in a different version — qi — so a pointer to it has to be kept or the deletion becomes unaccountable.\n\nmark_necessary! keeps one: when it deletes qj it records qj in the shadow list of the class qi that dominated it (PkgInfo.shadows, one list per class, alongside members). Nothing else about the universe changes. The shadow lists are in no matrix, no clause names them, and no model contains one, so every theorem above reads the same before and after: the sets of classes, rows and columns the proofs quantify over are exactly the sets they were. Shadowing is a deletion that writes down its reason, not a deletion avoided.\n\nWhat the record is good for is fixed by the same subset that licensed the deletion. Every constraint of qi is a constraint of qj, so:\n\nwhatever rules out qi rules out qj. A statement of the form \"these versions are excluded, for this reason\" is true of a class's shadows whenever it is true of the class.\na stated bound does not carry over. qj may bound a dependency more tightly than qi does — that is a superset, and it is the ordinary case — so \"qi requires r at 12\" may be simply false of qj. The implication runs one way only.\n\nA shadow list belongs to a class and lives exactly as long as it does: a class deleted for being unreachable or uninstallable takes its shadows with it. That loses a report nothing, because a shadow of such a class would have gone the same way on its own account — it depends on whatever made the class uninstallable, and its mathrmkey_emptyset is worse than the class's, so no walk that stopped before the class reaches past it — and because both of those deletions are already explained by the chain being printed. Domination is the one deletion whose reason is somewhere else.","title":"Shadows: what a deletion leaves behind"},{"category":"section","location":"theory/layered/#Iterating-the-filter","page":"The layered solution & filtering","text":"It is natural to ask whether a single reachability pass followed by a single redundancy pass leaves anything on the table — whether iterating the two would prune more. The answer illuminates what each pass can and cannot see.\n\nReachability alone is a closure operation:\n\nnote: Proposition (reachability is idempotent)\nRe-running reachability on its own output keeps everything.Proof. The reach set is a least fixpoint, built by a derivation whose steps mention only versions that end up kept: requirements' first versions, first versions of kept dependencies, and conflict/saturation extensions among reachable versions. Reachability deletes only version suffixes and whole packages, none of which appear in any derivation step, so the entire derivation replays on the filtered problem and re-derives every kept version. ∎\n\nThe composite filter, however, is not idempotent: redundancy elimination can invalidate reachability's reasons. A minimal example:\n\nnote: Example (redundancy changes reachability)\nTake requirements p q r with two versions of each of p, q, r, one version of s, the single dependency p1 to s (p2 depends on nothing), and conflicts q1 otimes r1, q2 otimes r1, q2 otimes p1.Reachability: q1 otimes r1 extends both packages, making q2 reachable; then q2 otimes p1 extends p, so p2 is kept — that conflict is p2's only justification. Redundancy: q2's constraints strictly contain q1's, so q2 is deleted. But the now-orphaned p2 survives: domination by p1 fails because p1's dependency on s has no counterpart in p2. Re-running reachability on the filtered problem would drop p2, whose reason to exist is gone.\n\nThe general shape is: a deleted version was another package's only reason to degrade, and the orphaned version escapes the domination test. Two things can shield an orphan. One is a dependency-set difference between it and its better sibling, as above — version-varying dependency sets again, the same dividing line as the fragment lemma. The other is saturation: saturation pushes are dependency-driven and invisible to the domination test, and deleting versions can unsaturate a package on a re-run, vaporizing the pushes that justified its dependents' worse versions.\n\n(A note on the domination test: it ignores conflicts whose partner version was itself dropped by reachability — such conflicts constrain no model over the kept versions, so ignoring them is licensed by exactly the swap argument of Theorem 6. Counting them would spuriously block domination and orphan far more versions than the two genuine mechanisms above.)\n\nNone of this threatens correctness. By Theorem 4, any number of additional passes preserves the layered answer, so iterating the filter to a mutual fixpoint is perfectly legal — and since the passes are cheap, that is what the implementation does: redundancy elimination first (it needs no reachability marks, and on registry-scale problems it does most of the shrinking), then reachability and redundancy alternating until neither deletes anything. The returns diminish fast: a further round prunes anything at all in only about 0.007% of deliberately conflict-dense random instances with version-independent dependencies and about 1% with version-varying ones, and what it prunes is marginal. Essentially all of the reduction is captured by one reachability pass and one redundancy pass; the remaining rounds just collect the stragglers because they cost next to nothing.","title":"Iterating the filter"},{"category":"section","location":"theory/layered/#Preprocessing-for-caching","page":"The layered solution & filtering","text":"Everything above concerns one resolve. When many resolves share a registry state, the question becomes which artifacts can be computed once and reused — and the answer is organized by what each artifact depends on: the registry content, the version ordering, and the requirements. Three results pin down the tiers.\n\nnote: Proposition (filtering for a larger requirement set)\nLet mathrmreqs subseteq mathrmreqs. The filter run with requirements mathrmreqs keeps every version of mathrmLay(D mathrmreqs), so by Theorem 4 mathrmLay(F_mathrmreqs(D) mathrmreqs) = mathrmLay(D mathrmreqs).Proof. Reachability is monotone in its seed set on a fixed problem — every derivation from mathrmreqs is a derivation from mathrmreqs — so the mathrmreqs-reach keeps a superset of the mathrmreqs-reach, which keeps the answer (Theorem 5). Redundancy elimination is requirement-agnostic outright: Theorem 6's swap argument converts any witness model containing a dominated version into one containing its dominator without touching coverage, so on whatever problem it runs, the layered trace of every requirement set avoids the versions it deletes. Each round of the iterated filter therefore preserves the answer, and Theorem 4 applies after each. ∎\n\nnote: Corollary (whole-registry prefiltering)\nFiltering once with every package required yields a reduced problem on which any requirement set resolves to its raw answer, with satisfiability verdicts preserved (unsatisfiability trivially, since models only shrink; satisfiability because the answer survives). Filtering is, in this sense, a monotone function of the requirements, and the all-requirements filter is its safe upper envelope.\n\nThe prefiltered problem still depends on the version ordering — both reachability's prefixes and redundancy's better-dominates-worse are stated in terms of it. How much preprocessing can be shared across orderings has an exact answer:\n\nnote: Proposition (order-free deletion is exactly model-freeness)\nA version can be deleted without changing the layered answer for every version ordering and every requirement set iff it appears in no model.Proof. If a version appears in no model, deleting it leaves the model set — and hence every trace — untouched. Conversely, if pv appears in a model M, order p's versions with v first and require p: the trace pins p at the best feasible rank, and M witnesses rank 1, so the answer contains pv and deleting it changes the answer. ∎\n\nSo the strongest ordering-independent filter is exactly the removal of model-free versions (of which the arc-consistency test — a version one of whose dependencies has no compatible version left — is a cheap sound approximation, and per-version SAT probes are the complete one). One more piece of ordering-independent structure exists:\n\nnote: Lemma (interchangeable versions)\nCall two versions of a package interchangeable when they have the same dependency set and every compatibility entry in the problem — their own and every other package's — constrains them equally (equivalently: their constraint rows are identical). Interchangeable versions can be collapsed to whichever of them the active ordering ranks best, preserving that ordering's layered answers: the collapsed versions are dominated by the representative (identical constraints are in particular a subset), so Theorem 6's argument applies on the raw problem and Theorem 4 finishes. The classes are ordering-independent; only the representative choice is not.Beware that a package's own dependencies and compat matching is not sufficient: another package's compatibility bound can admit one of the two versions and not the other, and then they are genuinely distinguishable — solutions needing the excluded pairing must pick the right one. The equal-incoming-constraints half of the definition is what rules this out.\n\nTogether these give a three-tier cache: per registry state (parsed data, the model-free set, interchangeability classes), per ordering (the whole-registry prefilter), and per resolve (requirement-specific filtering, which is fast and not worth caching). A non-standard ordering — say, preferring versions closest to an installed manifest — invalidates only the middle tier, which is orders of magnitude cheaper to rebuild than the first.\n\nThe implementation draws the line between the first tier and the rest at pkg_info, whose output — conflict matrices indexed by the classes (version_classes), with the versions that cannot be installed whatever else is chosen already dropped — is exactly the registry-only artifact, and it merges the second and third tiers into one per-resolve pass, prepare_pkg_info, since the middle tier is cheap enough not to be worth storing.\n\nThe artifact is indexed by those classes throughout: a PkgInfo's conflict matrix has one row per class and one column per partner class. Building it takes one row per version first, since that is what the partition is computed from, and then merges them (collapse_classes!). Nothing is lost in the merge: members of a class have equal rows by definition, and — since conflicts are recorded symmetrically — members of a partner class index equal columns, so the merged matrix states exactly the same conflicts.\n\nWhat the per-resolve pass then does is choose, per class, which member it stands for. The user's constraints act only by forbidding members (exclusion_masks): a class keeps whichever of its admissible members the query's ordering ranks best, and competes at that member's rank, which is why the classes have to be laid out in the order those representatives put them in (class_ranking). A class the query admits no member of is deactivated — nothing can choose it — and that, not deletion, is the whole of a constraint's effect. Then the result is filtered (filter_pkg_info!).\n\nDeactivation is deliberately not deletion. A deactivated class stays in the matrices: reachability continues its prefix past one, since a class that cannot be selected is not somewhere a package can be installed, but still follows its dependencies, and redundancy elimination neither deletes one nor lets one license a deletion. What that buys is that the universe a later question about relaxing a constraint would need is still present — and, one level down, that such a question is even well posed, since a constraint cannot make two versions' rows identical: it does not touch the rows, and versions whose rows are identical are one class sharing one column, with no pair of separately deletable objects to fall between.\n\nThe ordering is a resolve parameter rather than part of the problem, which is what makes one artifact serve every query: it selects among the valid solutions instead of changing which solutions are valid. What does change the model set — the user's compat bounds and pins, and the admission of prerelease versions — is a Problem, and enters as constraints on the artifact's versions rather than as deletions from it. So there is one T1 artifact per registry state, full stop: every version the registry state offers is in it, in canonical order and in its class, and each query says which of them it will accept and in what order it prefers them.\n\n\"Every version the registry state offers\" is meant strictly, and it is where yanked versions come in. Yankedness is not a query fact but part of what the registry state says — a yanked version is one the registry has struck, so it is not on offer — and the default artifact is built with those versions filtered out at the provider (see bin/Registries.jl). That is a deletion, not a constraint, and it is the right shape: a resolve then cannot produce a yanked version or suggest one as an alternative, with nothing left to enforce per query. The cost is that --allow-yanked, the query that wants them back, is the one query the baked artifact does not serve: it asks for a universe the registry state does not describe, and so rebuilds T1. That is rare and cheap next to the alternative, which is carrying struck versions through every layer and every diagnostic in order to forbid them again at the end.\n\nwarning: Formalization boundary\nTheorems 2, 3, 5, and 6 are proved against the rule set as stated above, which is a faithful but hand-made reading of find_reachable and mark_necessary!; the correspondence of code to rules is checked by review and by the test suite, not mechanically. The package's tests compare the implementation against a brute-force reference that implements the layered trace on raw data across hundreds of thousands of random instances — a direct, ongoing empirical check of Theorems 4–6 — and, whenever the implementation reports unsatisfiable, verify by enumeration that no raw solution covers the requirements (Theorem 3).","title":"Preprocessing for caching"}]
}
